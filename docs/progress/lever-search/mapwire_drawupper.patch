# Map/MapWire drawUpper__8TMapWireCFv: exact
# levers: raw->acc all 6 sites: mStartPoint -> getStartPoint() + name-call L109: name receiver getStartPoint() const JGeometry::TVec3<f32>& + name-call L110: name receiver getStartPoint() const JGeometry::TVec3<f32>&
--- a/src/Map/MapWire.cpp
+++ b/src/Map/MapWire.cpp
@@ -106,10 +106,11 @@
 
 	GXBegin(GX_TRIANGLESTRIP, GX_VTXFMT0, (mNumActiveMapWirePoints + 2) * 2);
 
-	GXPosition3f32(mStartPoint.x + xOffset, mStartPoint.y,
-	               mStartPoint.z + zOffset);
-	GXPosition3f32(mStartPoint.x - xOffset, mStartPoint.y,
-	               mStartPoint.z - zOffset);
+	const JGeometry::TVec3<f32>& startPoint = getStartPoint();
+	GXPosition3f32(startPoint.x + xOffset, getStartPoint().y,
+	               startPoint.z + zOffset);
+	GXPosition3f32(getStartPoint().x - xOffset, getStartPoint().y,
+	               getStartPoint().z - zOffset);
 
 	for (int index = 0; index < mNumActiveMapWirePoints; index++) {
 		addPoint(mMapWirePoints[index].mPosition, xOffset, zOffset);
