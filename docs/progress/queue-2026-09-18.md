# Unit queue (2 units per agent, same directory, disjoint headers)

Header batch 10:
- JGeometry/JGProjection.hpp: orthographic(l, r, t, b, n, f) argument order + `+ n` on mMtx[0][3]/[1][3] (BathWaterManager only caller)
- JSystem/JMath.hpp: TRandom_<T>::get_float(f32) body `return get_float(0.0f, max);`
- JGRotation3.hpp: a transposed 3x3 multiply for row-major world->local (TBathtubData::getLocalPos parked)
- JSystem/JSupport: JSUOutputStream by-value `int write(u8)`; JSURandomInputStream `bool isDrained() const` (retail virtual getLength/getPosition on a local memory stream); JSUInputStream::JSUInputStream() emitted weak out of line inside loadBookmark (CardLoad MISSING)
- StageUtil.hpp: scScenarioNameTable/scNormalStageTable as function-local statics (retail CardLoad.o carries neither); SMS_getNormalStage clrlwi

Header batch 9: 
- Strategic/TakeActor.hpp: isTaken() as `if (mHolder) return TRUE; return FALSE;` (measured clean win, 0 regressions)
- MSound/SoundEffects.hpp: fix the MSD_SE_BS_UNG_* group to retail's 0x2180 / 0x2981-0x298d (bosseel parks BOSSEEL_SE_*); verify every user
- std-vector.hpp / NameRefPtrAry.hpp: TVector<void*>::begin() dereferenced at +4 in retail's TBossEel::perform
- JDrama/JDRFlag.hpp + GameSequence.hpp: TFlagT<u16> copy ctor must be a real call for TGameSequence::set's by-value parameter (MarDirectorDirect decideNextStage 63%); probe in a scratch TU
- Player/Mario.hpp TMario::checkStatusType(s32) callable from cameragc (weak 0x1c; +2 unit points; find the honest level)
- JGRotation3.hpp one-arg mult33(TVec3&) forwarding via a temp (cameralib +11)
- MathUtil.hpp MsClamp<f32> out of line at perform/changeCamModeSub_ depth (local 0x20 in cameragc/CameraChange)
- codegen-tells: depth-2 allowance is 10 (cameragc) vs 9 (scratch probe) — reconcile
- Player/Mario.hpp unkFC..unk102 -> s16[4] NOW UNBLOCKED (MarioDraw landed; drop the `&` at its two `&mario->unkFC` sites)
- M3DUtil/MActor.hpp: setMotionBlendRatioForBck(f32) must re-read mAnmBck for the call receiver (ROM lwz; cmplwi; lwz; bl); initNormalMotionBlend does not — per member
- JGVec3.hpp length() = sqrt(dot(*this))? (+8.8 evIsNearActors, -1.3 boid/Kazekun) — per-site story needed
- Strategic/spcinterp.hpp dispatchBuiltin: test mNativeCall then mr r12
- MSound/BackgroundMusic.hpp tail off by one (FANFARE_RACE 0x25 vs played 0x26; MONTEMAN_RACE 0x2e vs 0x2f) — verify against every user
- EventWatcher UNUSED stubs evSetEventStart 0x174 / evSetEventEnd 0x148
- Player/Mario.hpp unkFC..unk102 -> s16[4]: measured codegen-neutral, needs the two `&mario->unkFC` in MarioDraw.cpp to drop the `&` (blocked on the MarioDraw agent; do after it lands)
- MarNameRefGen_BossEnemy still open: DemoBossHanachan.hpp, SleepBossHanachan.hpp, BossEel.hpp items, TVec3::set<f> call-vs-expand

Header batch 8 (DONE, landed): ALSO
- MapObjBall.cpp: reorder definitions to the map's reversed emission order (TResetFruit/TBigWatermelon groups interleaved; TODO at top of the .cpp; watch the literal pools)
- JGVec3.hpp: TVec3::distance() may be sqrt(squared(other)) (retail batches a fourth load before the first fsubs) — lead only, measure with a sweep
- koopajr .rodata: two unreferenced 12-byte constants (@2659 zeros, @2661 three 1.0f) at file scope before koopajr_bastable
- Player/Mario.hpp: unkFC/unkFE/unk100/unk102 are one s16[4] (ROM forms addi this,0xfc once; MarioHeadCtrl/WaistCtrl)
- Player/Yoshi.hpp: TYoshi::onYoshi() is weak 0x1c emitted from MarioMove.o and CALLED by TMario::onYoshi (22%) — an in-class body retail calls at depth 1; find why (statement count? a `bool` materialisation?); drop the (void)0 hack
- JGVec3.hpp dot(): ROM length() sites fuse fmuls(y2), fmuls(z2), fmadds(x2,y2), fadds — named components; wireMove one instruction off
- Player nozzle hierarchy: checkController stores at nozzle + 0x714 (past TNozzleTrigger 0x388)
- Enemy/Graph.hpp: TRailNode::mSpeed u16 (lhz + unsigned 0x4330); TGraphTracer::traceSpline returns BOOL
- MSound/MSound.hpp: checkMarioVoicePlaying returns JAISound* (callers cast)
- sizeof fixes proven by the factories: TKazekunManager 0x64 (not 0x60), TKazekun 0x1d4 (not 0x1b4), TLimitKoopa 0x1c8 (not 0x1bc) — find the missing fields from the ctor stores
- split LimitKoopa.hpp's nine inline theNerve() bodies into LimitKoopaNerve.hpp (includers pay 12 B .bss each; MarNameRefGen_BossEnemy __sinit 0x6c off)
- identify the shared Player header carrying `static const char cDirtyFileName[]/cDirtyTexName[]` (0x34, (object,local) in every Player TU + MarNameRefGen*, MarDirectorPreEntry, emario, enemyMario, cameragc, MapObjFence); MarNameRefGen_BossEnemy parks a copy
- stale TODO paragraph in src/System/MarNameRefGen_BossEnemy.cpp lines ~43-60 lists header items now done; trim to the three sizeof notes only items below not marked DONE in rounds 5-7 (LiveActor non-const checkLiveFlag; JGVec3 operator*= depth + operator* by-value (both need per-site stories); string.h memset; Yoshi.hpp onYoshi hack; SoundEffects 0x405C; fmodf wrap helper .cpp work; Particles/CameraShake leftovers)

Header batch 7 (DONE, landed):
- REDO on top of the new MapObjCorona: TBathtubData::unk18 as TPosition3<TMatrix33<SMatrix33R<f32>>> (headers6 commit 58df9979 was skipped in the rebase because MapObjCorona had been rewritten meanwhile; it made the weak SMatrix33R ctor appear and TBathtub::__ct 86.8 -> 99.9). Reconcile the two agents' axis conventions (rows vs columns) by measurement.
- MSL C-mode audit: math.h's std::-only inlines (fmodf, sinf, cosf, tanf, atan2f, powf, fabsf) vs the C units, like sqrtf (hx_wiper +1.7); fake_tgmath.h is a fabricated shim with a different body
- MActor::getCurBckAnmPtr non-const (map has no C) costs TChuuHana::setBckAnm; trial table in MActor.hpp — find the site spelling
- TKukku::updateRotation needs 3+ statements that cost no stack slot (retail calls it at depth 1; frame already 0x18 over)
- JUtility/JUTColor.hpp: TColor(const GXColor&) instead of by value (ShadowUtil drawShadow: 12 of 48 frame bytes); global sweep
- LiveActor.hpp: non-const checkLiveFlag overload (CSE with onLiveFlag; hamukuri isHitValid x3) — sweep
- JGVec3.hpp operator*(TVec3, f32) returning const TVec3& — measured: +1 site, -15 functions/8 units; needs per-site story (do NOT land blindly)
- MSL string.h: memset declared __declspec(section ".init") blocks the inline 8-byte stb loop retail emits for memset(p, 0, 0x400) (GCConsole2 perform x2, ~52 instructions)
- Player/Yoshi.hpp: TYoshi::onYoshi() eight `(void)0;` anti-inline hack is a fakematch; GCConsole2 needs the unpadded predicate (parks isMountedYoshi) — find the honest spelling
- MSound/SoundEffects.hpp: 0x405C blue-coin counter tick
- JGVec3.hpp: operator*=(f32) two-statement body expands at depth 4 where retail calls it and inlines scale (Player wire functions, ~78-140 instructions each); a JGVec3 change + full sweep
- plus every item below not yet marked DONE

Header batch 6 (DONE, landed 679c6717; TPosition3 item skipped):

Header batch 5 (DONE, landed):
- Camera/CameraShake.hpp: name all CAM_SHAKE_MODE_UNK* from TCameraShake::mCamShakeNameSave (entry N = mode N .prm), zero risk
- MathUtil.hpp: MsMtxSetRotX/Y from MapObjPinna.hpp (remove MapObjRicco's include of MapObjPinna.hpp)
- JGVec3.hpp cross(): retail store order x, y, reload offset.x, z (TLimitKoopaJr::moveRun); marked "Incorrect!!!" already
- MathUtil.hpp MsAngleDiff: `180.0f + alpha` constant-first (operand-only, TAnimalBird::doLanding)
- validate-symbol-order.py: tolerate local template instantiations (MsWrap<f>, set<f>) placed by last source-order user
- Particles.hpp: PARTICLE_MS_PUKU_AWA = 0x178 (TabePuku emitEffects casts)
- Strategic/TakeActor.hpp: isHolding() returns BOOL (retail li/li/cmpwi in TTPHitActor::updateTerrainCollsion); needs full sweep
- Enemy/Enemy.hpp: const overload of TSpineEnemy::getTracer()
- JGVec4.hpp: make TQuat4::rotate's first temporary call the empty TVec4 ctor (weak __ct__Q29JGeometry8TVec4<f>Fv in TabePuku/coasterkiller/fireWanwan/Bird) — experiment
- MathUtil.hpp: MsMtxSetRotY(MtxPtr, f32 degrees) parked in MapObjFence.hpp (weak 0x7c in MapObjFence.o); merge with the MapObjPinna MsMtxSetRotX/Y item
- M3DUtil/SampleCtrlModel.hpp: define class SampleCtrlMaterial (J3DTevStageInfo mTevStageInfo[6] at 0x3c); parked in ModelGate.hpp
- MoveBG/ModelGate.hpp + Player/MarioParticle.cpp + MarioAutodemo.cpp: rename unk71/unk72/unk78/unkAC (gate index, center joint, MActor*, hold offset)
- MarioUtil/ScreenUtil.hpp: TAfterEffect radial-blur setter taking direction by value (screenBlur residual)
- AGENT_MATCHING_TIPS.md: correct the init$ guard claim (zero-initialised static ints get one too)
- Strategic/Spine.hpp: TSpineBase<T>::getLatestNerve() must be callable out of line (retail weak 0x1c from Bird.o; bosswanwan loses TBWPicket::moveRequest, TBWBinder::bind, perform) — HIGH VALUE, experiment with a wrapper level
- Strategic/ObjModel.hpp: TMActorKeeper::getMActorAnmData() bl-ed at all changeBck sites (caller-size family)
- MathUtil.hpp: MsPerpendicFootToLineR 73% vs retail body
- Particles.hpp: bwanwan ids 0xAD-0xB1, 0x167, 0x1EE; CameraShake.hpp CAM_SHAKE_MODE_BOWA_LAND1/2 = 0x16/0x17 (local enums in BossWanwan.hpp)
- JGVec2.hpp: enable the float copy ctor and operator= (SelectShine2 getAngle proves them); needs a full sweep
- StageUtil.hpp: static u32 scNormalStageTable[] = {0,1,2,3,4,0xD,6,8,9,0xA} after scScenarioNameTable; drop Guide.cpp's local copy
- JGMatrix34.hpp: fix TMatrix34::concat(a,b)/concat(b) bodies (Koopa calcRootMatrix writes them out); sweep all callers
- Enemy/LimitKoopa.hpp: LIMITKOOPA_ANM_* from koopa_bastable; getNeckFocus() returns f32
- CameraShake.hpp CAM_SHAKE_MODE_KOOPA_HIPDROP = 0x27; Particles.hpp KOOPA_JPA_MS_KP_HIPDROP = 0xF5
- MapObjCorona.hpp: TBathtub::getNextGrip return may be u8
- Spine.hpp: pushNerve stays a bl inside inlined stagger (Koopa) — same family as getLatestNerve
- Enemy/SmallEnemy.hpp: TSmallEnemy::initAttacker defined in the header (weak + UNREFERENCED DUPLICATE in map); move from smallenemy.cpp
- Enemy/Enemy.hpp: TSpineEnemy::getMaxHitPoints() returns u8 (pakkun proves it; costs TEffectEnemy::perform, TFireWanwan::moveObject — find the spelling that keeps both)
- bosspakkun.cpp: remove the eight duplicate pakkun nerve stubs when linking pakkun.o
- Particles.hpp: PARTICLE_MAP_MS_KP_KILL_SMOKE = 0x1BD (BathtubKiller #define)
- MathUtil.hpp: MsMtxSetRotZ(MtxPtr, f32) beside X/Y (no map symbol; hauntLeg static inline)
- math.h: expose the extern inline sqrtf body to C TUs too (hx_wiper: Hxs_FrBufferMorf2 exact, +10-18 on three more)
- J2D: J2DTextBox::setGradColor(TColor, TColor) next to setBlackWhite (Talk2D2 static inline)
- JSystem JMessage: TMessageLoader::EntryInfo is JMSMesgEntry (mangled name)
- MarDirector: unk124 is int/enum, not u8
- MoveBG/MapObjBase.hpp: define the nine declared-only virtuals in-class from bosstelesa.s (0x800C6E94+): getRadiusAtY 8, getTakingMtx 52, setModelMtx 56, loadBeforeInit 4, calc 4, draw 4, dead 4, getHitObjNumMax 8, touchWater 8 — HIGH VALUE (every MapObj TU's weak set)
- M3DUtil/MActor.hpp: getBckAnmPtr() is getCurBckAnmPtr() (UNUSED 0x1c)
- MoveBG/MapObjManager.hpp: getObjNumWithActorType returns u32
- CameraShake 0x1F-0x23 shakeBTelesa*; Particles 0xD7-0xE1, 0x19E-0x1A7, 0x1F0 ms_btls_*
- SCRATCH PROBE: does -inline deferred only inline callees defined later in the source? (bosstelesa lead)
- Enemy/Enemy.hpp: TSpineEnemy::turnToCurPathNode returns BOOL (bosspakkun FlyPivot/Pivot cmpwi) — sweep
- Particles.hpp: bosspakkun 0xA9-0xAC, 0x15D-0x164 ms_bopa_*, 0x52 pollution splash; CameraShake 0x0E-0x11 shakeBopa{Down,Popo,Getup,DownLoop} (parked in BossPakkun.hpp)
- System/MSoundMainSide.hpp: MSStageCubeFade::smInstance (static) + static setBgmVolumeForce() (map symbols; bossgesso BeakDamage 87->96, exact block in the TODO)
- StageUtil.hpp: scScenarioNameTable is the JP table; US values (70) in ConsoleStr.cpp TODO (shared with SelectMenu/Guide/CardLoad); SMS_getNormalStage() return type (u16 adds clrlwi, 0x1c vs 0x18)
- MSound.hpp: try `MSound : ... JAIGlobalParameter` (dead bl SMSGetMSound before setParamSoundOutputMode in Option::adjust)
- math.h RAD_TO_DEG: remove the +0.000005f fudge (ROM is exactly 180/pi = 57.295776f); sweep
- ExPane.hpp: remove the wrong "fabricated and incorrect" comment on setCenteredSize
- fmodf wrap helper (.cpp work) still open; Koopa has 6 more sites

Header batch 4 (DONE, landed):
- JGVec3.hpp: make TVec3<f>::set<f>(f,f,f) a non-inline member template so it is emitted local and called (retail: local symbol in 20+ objects). MWCC refuses out-of-class definition for the explicit specialisation; find the structure. Probes: wireTrap, Kazekun, MapObjBall, amiNoko, BeeHive nerves.
- std::fmodf wrap helper `l + fmodf((r-l)+(t-l), r-l)`: recover as a .cpp-level helper in koopajr/Koopa/BathtubPeach/MapObjCorona/wireTrap so fmodf sits at depth 4.
- MathUtil.hpp: MsSin(f32)/MsCos(f32) inline wrappers over JMASin/JMACos (currently in Killer.hpp; killer.cpp is the only TU emitting them)
- KoopaJr.hpp: TDirectionCalc::r2d/d2r static (ROM calls without this; limitkoopa)
- Particles.hpp: 0x1C0-0x1C3, 0x1F3 ms_kp_fire_* (limitkoopa raw hex)
- FlyEnemy members unk1A5/unk1A8 (chase-type flag, launch velocity) named once cannon.cpp is updated with them
- src/System/MarNameRefGen_MapObj.cpp: add the `WatermelonStatic` branch (new TWatermelonStatic, 0x138, name 固定スイカ) — caused the -0.05 on getNameRef_MapObj
- include/MoveBG/MapObjBall.hpp: TMapObjBall::getDepthAtFloating() returns f32 (`return unk18C;`), weak body `lfs f1,0x18c(r3); blr`
- new include/MoveBG/MapObjFlag.hpp: TMapObjFlag : THitActor (0xC0, vtable 0xa8, virtual draw; load/init/update/updateVertex; static mFlutterSpeed) — currently declared inside MapObjMamma.hpp; do with the MapObjFlag unit
- include/Camera/CameraShake.hpp: name shake mode 0xD
- include/MSound/MSoundBGM.hpp: stopTrackBGM(1, 0xA) / setTrackVolume(0, vol, 0xA, 0) contradict the guessed parameter names
- include/Map/MapMirror.hpp: TMirrorModelObj ctor zeroing unk28
- include/Map/MapCollisionEntry.hpp: TMapCollisionMove::moveMtx is MTXCopy(mtx, unk20) then move() in the ROM (32.8% in 25 Enemy TUs) — HIGH VALUE
- Particles.hpp: tinkoopa 0xEE-0xF4, 0x1AC-0x1BC, 0x1F2; elecNokonoko 0x17A-0x17C (0x17F done)
- GC2D/GCConsole2.hpp: balloon ids 0x9, 0xA, 0xB, 0x24
- JGRotation3.hpp setEular(f,f,f) UNUSED 0x100 missing from tinkoopa (only the dead makeEyeBeamEffect reaches it)
- include/MoveBG/MapObjBase.hpp: getObjCollisionHeightOffset() is `f32 ... const { return mYOffset; }` (retail weak lfs f1,0x108(r3); blr); ~350 bytes in TMuddyBoat::bind
- include/Enemy/Cannon.hpp: TCannon::isObject() returns bool (TMareCork::moveObject clrlwi.)
- Particles.hpp: MapObjMare 0x149/0x14A mareFallSplash/Smoke, 0x14C-0x14E ms_mare_gunwat_a/b/c
- include/MarioUtil/MathUtil.hpp: MsMtxSetRotX/MsMtxSetRotY(MtxPtr, f32) next to MsMtxSetRotRPH (weak in MapObjPinna; parked in MapObjPinna.hpp; MapObjRicco.cpp includes MapObjPinna.hpp to reach them — remove that include)
- fishoid/fireWanwan/coasterkiller missing symbols (TTakeActor::isTaken, getRadiusAtY, ArrayWrapper ops, params ctors) — pre-existing, unit work.

Header batch 3 (DONE, landed b380f33d):
- include/Enemy/WireBinder.hpp: getDir() returns const TVec3&, isStartWire() returns bool (only caller wireTrap.cpp; +2 exact, but `load` 84->80 must be fixed alongside)
- include/MarioUtil/MtxUtil.hpp: SMS_CalcToDirMatrix is global 0x23c emitted from Kazekun.cpp; declare in header, body in Kazekun.cpp; drop wireTrap's local declaration
- include/System/Particles.hpp: 0x190/0x191 ms_wrt_biri_a/b (LOOP_NORMAL); drop wireTrap's local enum
- include/Animal/fishoid.hpp: TRealoidActor::checkFlag(int) const; drop BeeHive's TU-local checkRealoidFlag
- include/JSystem/JGeometry/JGPosition3.hpp: TPosition3::setSQT(scale, quat, trans); drop BeeHive's SetSQT stand-in
- include/JSystem/JGeometry/JGQuat4.hpp: fix mul(const TQuat4&) _y sign (ROM: + z*ox - x*oz); setRotate register order; slerp folded constant
- include/JSystem/JGeometry/JGRotation3.hpp: setSQ load order qt.y, qt.z, qt.x, qt.w
- JGQuat4.hpp setRotate(const TVec3&, f32): sinf(0.5f * angle) constant-first (Kazekun, 3 sites)
- JGRotation3.hpp getQuat: regroup at(0,0)+at(1,1)+at(2,2)+1.0f for the sqrt argument (f3/f4 swap)
- Particles.hpp: 0xCF ms_kaze_appear, 0x189 ms_kaze_wind, 0x18A ms_kaze_blur (Kazekun local enum)
- Open: map lists weak SMS_EasyEmitParticle<E_SMS_EFFECT_LOOP_INDIRECT> for Kazekun.cpp, unaccounted (setVisible 0x2c vs 0x4c?)
- anything headers2 leaves open (fmodf wrapper helper is .cpp work in koopajr/Koopa/BathtubPeach/MapObjCorona/wireTrap)

Constraint: Enemy/pakkun shares include/Enemy/BossPakkun.hpp with Enemy/bosspakkun -> never concurrently.

In flight: Kazekun, wireTrap, BeeHive, headers2.

Next groups:
- grpa: Enemy/tinkoopa (54 fns, 16.8K, 6%) + Enemy/elecNokonoko (69 fns, 17.2K, 10%)
- grpb: Enemy/killer (54, 13.4K, 8%, header Killer.hpp / KillerNerve.hpp) + Enemy/limitkoopa (58, 12.3K, 12%)
- grpc: MoveBG/MapObjMamma (103, 20.5K, 10%) + MoveBG/MapObjMonte (54, 17.4K, 6%)
- grpd: MoveBG/MapObjMare (67, 15.1K, 9%) + MoveBG/MapObjBianco (74, 14.4K, 12%)
- grpe: MoveBG/MapObjPinna (68, 13.4K) + MoveBG/MapObjRicco (32, 7.8K)
- grpf: Enemy/TabePuku (48, 13.6K, TabePukuNerve.hpp) + Enemy/Kukku (37, 10.5K, KukkuNerve.hpp)
- grpg: Animal/Bird (52, 15.2K, BirdNerve.hpp) + Enemy/limitkoopajr (27, 6.5K)
- grph: MoveBG/MapObjFence (43, 8.7K, 0%) + MoveBG/MapObjFlag (15, 6K, no header) + MoveBG/ModelGate (11, 7.3K)
- GC2D: hx_wiper (45, 20.4K, 0%, no header), Talk2D2 (24, 18.4K), Guide (17, 16K), SelectShine2 (13, 5.8K)
- Bosses (single-unit batches): bosstelesa (118, 40K), bosspakkun (129, 40K) + pakkun (78, 18K) same header, bosswanwan (79, 32K), Koopa (79, 29K)
- Later: MapObjCorona (38%), BathtubKiller (46%), hauntLeg (52%), MarNameRefGen_* (factories; need many headers)

## Header batch 10 additions (from batch 56)
- include/Player/WaterGun.hpp: rename mHoverHeight -> mHHoverHeight (ROM typo, @4096 string); call sites MarioWait.cpp x2, MarioJump.cpp, MarioRun.cpp. Codegen-neutral.
- include/Player/NozzleTrigger.hpp: unk385 is u8 (cmplwi in movement); add an s32-returning spray-state accessor for isEmitting/MarioMove/Yoshi signed-compare sites. Measure each.
- WaterGun.hpp: TWaterGun::TDeParams no-arg ctor defined in .cpp between finalDrawInitialize and getEmitMtx (pool order); canSpray() now unreferenced.
- JGeometry TRotation3<TMatrix34<SMatrix34C<f32>>>::TRotation3() weak ctor is 4 bytes off in MarNameRefGen_Enemy (closure batch A diagnoses, header batch applies).
- (batch 58) MSound/MAnmSound.hpp: destructor in-class (UNUSED __dt__9MAnmSoundFv 0x60 in enemyMario).
- (batch 58) M3UMtxCalcSIAnmBlendQuat default ctor in-class (weak 0x24 in enemyMario, __construct_new_array).
- (batch 58) JGVec3 cross() store order x, y, reload, z — already queued, third confirmation.
- (batch 58) open: const TGraphTracer::getGraph() / TPathNode::getPoint() called out of line from TEnemyMario::consider.

## Header round 9 outcome (2026-09-18)
DONE: TakeActor isTaken; SoundEffects UNG group; BackgroundMusic whole enum; checkStatusType (applied by orchestrator in main); MActor setMotionBlendRatioForBck (no change needed; MarioDraw call site).
RULED OUT: mult33 forward; MsClamp level; length() via sqrt(dot); TFlagT copy ctor; TVector<void*>::begin facades (trial table in header).
LEFT: spcinterp dispatchBuiltin (untried: early-return shape of dispatchBuiltinDefault).
- (batch 59) STRUCTURAL: bossManta.cpp whole-TU non-weak ORDER failure (29+ symbols) — a definition-reorder job for a closure/structural batch; watch the literal pools.
- (batch 63, cosmetic) promote CameraCodeControl.cpp's four parked static inline accessors: Camera.hpp CPolarSubCamera::getCamMode()/getUnk70(); CameraMarioData.hpp getFramesSinceMarioStatusChange() + global SMSGetCameraMario(). Object byte-exact either way.
- (batch 60) MarioAccess: 11 missing UNUSED SMS_* wrappers (scaffolding task; blocks linking independently of SMS_IsMarioOnWire).
## Header round 11 (from closure batch 61)
- include/Map/JointModelManager.hpp: `TJointModel* getJointModel(int i)` level, but only on the getLayer(i) path — globally it breaks linked Map/MapEvent (100 -> 95.9). Needs a per-site story.
- include/System/TimeRec.hpp: startTimer(u32) named `TTimeArray* timeArray = inst->crTimeAry();` plus a compensating -8; endTimer must not change (SnapTimeObj byte-exact with the full mirror, but moves TLiveManager/TObjManager perform).
- NameRefPtrAry.hpp + std-vector.hpp: TVector_pointer<X> template argument is the pointer type (enemytable dtor symbol).
## Header round 10 outcome (2026-09-18): 11 accepted, rejected: transposed mult33, cross() store order, function-local stage tables. Open: MAnmSound dtor emission in enemyMario (kill 0x4c), consider's missing helper level.
## Header round 12 items (from closure batch 67)
- include/Enemy/EnemyManager.hpp: TEnemyManager::getObj(int) = (TSpineEnemy*)TObjManager::getObj(i), cast inside the accessor (clipEnemies 99.91 -> 100; AnimalManager parks AnimalManagerGetObj).
- JGPosition3.hpp: TPosition3::translation(const TVec3&) as { identity33(); setTrans(t.x, t.y, t.z); } — +8 on associateNPC, needs whole-tree measurement.
## Header round 11 outcome: accepted 1,3,4,9,10; rejected 2 (TimeRec); 5-8 already landed. Pending mechanical: src/System/MarDirectorPreEntry.cpp -> include MarioDirtyStrings.hpp (after closure 66 lands). Open: MAnmSound dtor 0x64 vs 0x60; TEnemyMario::kill 0x4c; MAnmSoundMario::startAnimSound r0/r5 swap.
## From closure batch 65
- Header round 12: promote MapModel_getUnderpassMaterial -> include/Map/MapModel.hpp; CameraBck_getMActor/getPosMtx/getLookatMtx/getFrame/getOffset -> include/Camera/CameraBck.hpp (both parked as static inline; codegen must stay identical). Do NOT add CPolarSubCamera::getSaveKindParam(int) (costs 8, breaks the Vec& overload).
- Research: `new` expression costs 8 bytes of frame retail does not pay (NpcColor case 0).
## From closure batch 66 (header round 12)
- Animal/fishoid.hpp: TRealoid::getBoidLeader(), TRealoid::getBoidNum() (forwarding), TRealoidActor::getMActor(); Animal/boid.hpp: TBoidLeader setters (mNeighborRadius, mBaseSpeed, mYawSpeed, mPitchSpeed, mMaxPitch, mAlignmentStrength, mFleeRadius, mFleeStrength) + onFlag — twelve parked static inlines at the top of src/Animal/Butterfly.cpp, load-bearing (unit linked; must stay exact).
- src/System/MarDirectorPreEntry.cpp: include Player/MarioDirtyStrings.hpp in place of its cDirty* copy (mechanical; unit unlinked, codegen-neutral).
- Research: JDRNameRefGen.hpp search<T> chain depth (preEntry needs ~2 more 24-byte expansions).
## From batch 72
- config/GMSE01/config.yml: dtk block_relocations for sFontData+0xd8, sFontData2+0xf8, Player::sTreTable+0x10, jdsp+0x18b8/+0x1938 (repo-wide re-split; do in a quiet moment with all worktrees landed).
- Research: per-call-site 8-byte object at gateCheck+startSoundActor sites (DebuTelesa::receiveMessage, TEffectEnemy::setDeadAnm [closed with a fabricated TVec3, TODO], TBossHanachan::emitOneTimeSandPillar_).
## From closure batch 70 (header round 12)
- include/MSound/MSound.hpp: MSound::getModBgm() accessor (BossHanachanNerve parks BossHanachanNerve_getModBgm; +8/site).
- JGVec3.hpp: operator- left operand by value? (NpcCallback const-ref binding evidence; measure project-wide).
## From closure batch 71 (header round 12)
- std-vector.hpp: TVector_pointer(const TAllocator<void*>& a = TAllocator<void*>()) : TVector_pointer_void(a) {} — measured: TMarDirector ctor + unit 100%, zero regressions. Plus MarDirector needs UNUSED __dt__Q27JGadget29TSingleLink<12TPerformLink,0>Fv defined to link.
- JDRViewObjPtrList.hpp: TViewObjPtrListT<T>::insert takes T* by value (MarDirector parks a helper).
- Mario.hpp: TWaterGun* TMario::getFludd() const (MarioUpper parks MarioUpperGetFludd; unit linked).
- Camera.hpp: inline CPolarSubCamera* SMSGetCamera() (Sky parks SkyGetCamera; unit linked).
## Header round 13, item 1 (from research batch 74) — HIGH VALUE
- include/MSound/MSound.hpp MSound::startSoundActor: `JAISound* sound = nullptr; if (gateCheck(id)) sound = MSoundSE::startSoundActor(...); return sound;` (named result binding = +4 slot). Whole tree: +54 improved / 38 to 100% (DebuTelesa::receiveMessage -> unit linkable; ~25 others) / 10 regress (2 from 100%: TNerveKazekunPreAttack::execute, TMapEventSink::control; plus TMareEventDepressWall::depressing, THaneHamuKuri::walkBehavior, THamuKuri::jumpToSearchActor, TEffectObjBase::moveObject, TMario::oilSlip, TYoshiTongue::movement, TItemManager::resetNozzleBoxesModel, TBossTelesa::forceHide) — those ten sites want the RAW spelling (gateCheck + MSoundSE::startSoundActor, instruction-neutral); hamukuri, MapEventMare, Tongue, MarioRun lack MSound/MSoundSE.hpp (BSS/sinit order — measure each). Also switch TEffectEnemy::setDeadAnm (delete its fabricated TVec3) and TBossHanachan::emitOneTimeSandPillar_ to the wrapper. Full table in the catalog (batch 74 section). trial.sh in scratchpad/grpbb.
## Header round 13 items (from closure batch 73)
- math.h: std::fmodf real body (0x5c; weak in wireTrap.o, dead duplicate in BathtubPeach.o) with enough wrapper depth so BathtubPeach `bl`s it.
- spcinterp.hpp: setDataFloat(const f32&) — reaches linGetSRT's count but regresses exact spcFloat (decide).
- JGVec3.hpp: distance() = { TVec3 diff = *this; diff.sub(other); return TUtil<f32>::sqrt(diff.squared()); } — measure project-wide (AnimalNerve evidence).
## Header round 12 outcome: 1,2,3,4,5,7,8,11 accepted (MarDirector linked); 6, 9, 10 rejected. Batch 75: bossManta/MarioAccess/WaterGun/Yoshi symbol order PASS; enemyMario (consider level), AnimalBase/Bird (caller-size at execWalk/WalkOnGround), PauseMenu2/MarioRun (caller budget) diagnosed.
## From closure batch 78 (header round 14)
- JUTColor.hpp: TColor(const TColor&) copy ctor question — conversion temp 8 vs retail 4 (Menu x3 functions); measure with the existing drawShadow trial table.
- PollutionManager.hpp: const getLayer(i) casting mJointModels[i] directly (one level) — cleanedAll frame; ~25 sites, measure.
- WoodBarrel::appear closes with the startSoundActor item (round 13).
## Header round 13 outcome: fmodf accepted; startSoundActor rejected (needs a second inlined level: research); setDataFloat/distance rejected; MAnmSound dtor 0x5c (subobject offset open).
## From closure batch 77 (header round 14)
- include/Player/WaterGun.hpp: s32 TWaterGun::getCurrentNozzleIndex() const (MarioCheckCol parks MarioCheckCol_nozzleIndex).
- BossHanachan headers: getChangeParams/getHead/getMarchSpeed/getSpine and TBossHanachanChangeSaveParams::getSL* (BHA_* parked in BossHanachanAnm.cpp; the two mSLWalkBckRate* stay raw .get()).
- Done in batch 77: BoundPane.hpp JUTPoint fields; ConsoleStr/GCConsole2 renames (codegen-identical).
## From closure batch 80
- Research seed (low-region PLACEMENT, not size): MovieSubtitle setupResource (12 B below buffer), ScrnFader load (temp order), NpcManager clipEnemies (+8 saturated), NpcCollision execNpcObjCollision_ (+32) / bind (operator- temp 0x10 vs 0x28), MarDirectorSetup2 setup2 (256 B = sizeof TGraphics).
## After research batch 82
- LINK BLOCKER: MoveBG/WoodBarrel.cpp is 100/100 but the DOL changes when linked (hash 44983953...). Diagnose like CameraInbetween (re-link with -map; .sdata2/.rodata order; weak-order warnings).
- MSound.hpp: three-argument startSoundActor(u32, const Vec*, JAISoundHandle*) sibling for TMareGate::control (MapObjDolpic.cpp:409); deeper binding for multi-site functions (TBellDolpic::control etc.).
## From closure batch 83
- Header round 15: std-vector.hpp TVector<T>::insert 32 bytes over (DrawSyncManager); Emario.hpp declare execKill/checkCollision UNUSED.
- Research: "x reloaded, y/z in callee-saved FPRs" (updateTrans, updateDemoCamera_, calcAttackGuideAnm, TBGTentacleMtxCalc::calc).
## From closure batch 81 (header round 15)
- JDrama/JDRDisplay.hpp TDisplay::getRenderMode(): one dead 4-byte non-trivial local closes RenderModeObj (4 functions, unit -> 100/100, zero regressions); only other user JDRFrmGXSet.cpp. Object unnamed — decide whether to apply with a TODO.
- include/Map/MapMirror.hpp: const int& TMirrorModelManager::getUnk18() const, isUnk18Present() in terms of it (MirrorActor parks two static inlines).
- Research: JGadget push_back temp-pool spacing (+8 per group gap) — SDLModel::entry, TMirrorActor::init; std-list.hpp / JDRViewObjPtrList.
## Header round 14 outcome: 1a,2,3,6 (WoodBarrel linked),7 (block_relocations) accepted; 1b,4,5 rejected.
## From closure batch 84 (header round 16)
- include/Strategic/TakeActor.hpp: TTakeActor(const char*) both inlined and emitted (UNUSED 0x50 in MapWireManager).
- include/System/FlagManager.hpp: SMSGetFlagManager() (SunGlass parks it; +4/site).
- include/Player/ModelWaterManager.hpp: s16 TWaterHitActor::getWaterHitCounter() const (BossHanachanEffect parks BHE_getWaterHitCounter).
- JGadget: TList_pointer<THitActor*>::insert 16 too big (MapWireManager) — same family as TVector::insert (round 15 item 3).
## Research batch 86 outcome: FPR-hoisted vector = named f32 locals of the caller's own body (last-declared gets f31; per-component use sites; a const& pass to an inlined callee suppresses it; zero frame). Applied to updateDemoCamera_ (99.73), calcAttackGuideAnm (98.9), updateTrans (97.75). Next: TBGTentacleMtxCalc::calc, updateMantaEscape (needs a TU-local distance helper).
## Header round 15 outcome: 2 (getUnk18) and 5 (emario UNUSED) accepted; 1, 3, 4, 6 measured and recorded, not applied.
## From closure batch 87 (header round 16)
- include/Camera/Camera.hpp: calcTowerCenterPos_ body in-class (weak, $localstatic0$ mangling) AND a private inlined ctrlTowerCamera_() so its single bl sits at depth >= 2 (ctrlNormalOrTowerCamera_ 98.1 must not drop). CameraNormal data 93 -> 100 when done.
- include/Player/Mario.hpp: M3UModelMario* getM3UModel() (MarioInit parks MarioInitGetM3UModel; +8/site); rename TDeParams::mHpMax -> mHPMax and restore PARAM_INIT.
- include/Camera/CameraOption.hpp: SMSGetCameraOption() (parked; CameraOption linked, must stay exact).
- Squared-distance helper with three named squares (CameraNotice parks CameraNoticeSquaredDist; third site).
- LINK BLOCKER: Player/MarioInit.cpp is 100/100 but the DOL changes when linked (hash 51962142...). Diagnose like WoodBarrel (weak flush order / cmp -l the DOLs).
## From closure batch 89 (header round 17)
- Strategic/ObjModel.hpp: promote ObjModelKeepModelData to a TModelDataKeeper member (the inline level above createAndKeepData).
- Shared copy-and-subtract distance helper (TEMario::perform, AnimalNerve::calcDist, riccoHookDistance): decide the shared home (JGVec3.hpp? MathUtil?) — measure project-wide.
- Data sweep idea (batch 93): vtables <100% whose differing word names a method absent from the map; units missing __sinit + fifteen 12-byte objects (MSound rogue pair); InfectiousStrings/MapCollisionManager prefix order.
## Header round 16 outcome: 1,2,3,5,6 accepted; 4,7,8 rejected; MarioInit link blocker solved (undefined weak inline) and MarioInit + TMarioGamePad dtor fixed. Lever sweeps queued: SMSGetFlagManager() over 194 raw getInstance() sites; SMSGetMarDirector()/SMSGetMSound()/SMSGetCamera() forks; getM3UModel remaining sites.
## From data sweep 93
- hinokuri2: replace getSaveParam2() wrapper with ((THino2Params*)getSaveParam())-> at the 83 sites (recovers Burst 97.4 -> 99.9).
- Pool-order float class (26 units): gatekeeper, MapObjItem2, Kumokun, MActor, Amenbo, fireWanwan, MapObjLib, Bird, PauseMenu2, Talk2D2, NpcAnm, Yoshi, Tongue, MarioMove, MapObjMamma, MapObjMonte, ModelGate, MarDirectorDirect/Event/LoadResource, Guide, GCConsole2, CardLoad, CardSave, DrawUtil, MtxUtil, CameraChange, namekuri, hamukuri, popo, bossgesso, MapMirror, NpcBase — batch 96.
- tobiPuku symbol order: 4 MISSING UNUSED (fallStart, flyStart, canBound, bound) + order/binding errors.
- PollutionCount: getCounterNo/getTokenNo declared in swapped order (unit owned by batch 91 at the time).
## After batches 91/94
- LINK BLOCKER: Map/MapEventDolpic.cpp is 100/100 but changes the DOL when linked (hash a0f227c2...). Suspect an undefined weak inline / externalised vtable (see header round 16) — diagnose with the detector.
- Header round 17: J3DPacket.hpp J3DCallBackPacket::CallbackT returns BOOL/int (ShapePacketCallBackFunc ends in li r3,1); MapCollisionData.hpp unk30/unk42 accessor fork (MapMakeList); a level above Map/MapData.hpp isWaterSurface (enemyAttachment parks one); LensFlare.hpp two fabricated inline member levels around the calc-anim block.
## Batch 97 outcome: MapEventDolpic linked (442); detector tools/check-weak-defined.py; tobiPuku/lensflare/CameraMultiPlayer symbol order PASS. Open: TEndingString class (MovieDirector); MarNameRefGen_MapObj missing factory branches (0x3278 vs 0x3734).
## From batch 98 (header round 17)
- NPC/NpcInitData / NpcParts.hpp: TNpcModelData first two words = const char* unk0[2] (NpcInitData.cpp tables keep bytes under brace elision).
- PollutionCount.hpp: TPollutionCounterLayer::getModelStampDrawBuffer(int) const (parked; +8, overshoots calcViewMtx alone).
## Batch 96 outcome: 16 units to 100% data; open: DrawUtil literal fold (needs an inlined helper), Guide 21200/25000 swap costs 3.8%, MtxUtil leading zero vector, six code-gated jump tables; StageUtil scShineTable layout (cosmetic).
## Batch 100 outcome: seven units to 100% data; open: TTelesaSlot/TSunGlass ctor refusal family (research: 25-45-instruction weak bodies retail calls), Application @2485 colour pair, limitkoopa two local statics, TItemNozzle::calcRootMatrix frame, string @NNNN pulled forward by inline analysis (rsetup 88%).
## From batch 101 — SWEEPS (high yield)
- search<T> -> (T*)JDrama::TNameRefGen::search2(name) at 306 sites (~40 units): fixes mr/addi copy form and 8 bytes of frame per site; header decision whether to delete the template.
- Mtx44 for every SMS_GetLightPerspectiveForEffectMtx caller buffer (bosseel, namekuri, bosstelesa, telesa, Shimmer [drop fabricated Vec lightPos], MapStaticObject, NpcParts, BathWaterManager TProjection3f) — 16-byte frame gaps.
- Header round 18: TMario::getGamePad(), TBaseNPC::getUnk1A0(), SMSGetMap() fork (parked).
## Research 104 outcome: TVec3 family `bl`s are depth measurements (3-statement members first bl at depth 4, 1-statement at depth 5); a class-template member defined out of class without `inline` is never inlined (weak + bl everywhere) — wrong scope for TVec3 (217 regressions). TTelesaSlot/TSunGlass ctor refusal still open (depth 1 after __nw__). Per-unit work: add the missing inline levels at the 184 set<f> / 130 sub / 64 scale sites (a sweep candidate: insert one/two thin static inline levels per site and measure).
## From batch 103 (header round 18)
- MSound.hpp: createRandPlayVec/registerTrans wrapper holding an uninitialised TVec3 (closes TMewManager::loadAfter, TAnimalBase::loadAfter, TAnimalBird::loadAfter, TAnimalBirdManager::loadAfter).
- JAIBasic.hpp: JAIData* getData() (MSoundBGM parks MSBgmGetAudioData; linked — must stay exact).
- Research: source local interleaved above compiler temporaries (AudioDecoderForOnMemory, TMapObjRevivalPollution::loadAfter).
## From sweep 106
- Header round 19: include/Enemy/PathNode.hpp TPathNode(THitActor*) zero unk4 via `unk4 = TVec3(0,0,0)` (BeeHive nerves).
- Research: repeat-callee heuristic — Koopa/limitkoopa perform expand set<f>(const&) at the first of five identical blocks and bl it at the other four.
## Header round 18 outcome: 1 (AnimalManager linked), 2, 3 accepted; 4 rejected (startTimer still needs (-8,0)); 5 liveness rule found.
## Header round 19 (from batches 106/108; all measured zero-regression except the last)
- Strategic/spcinterp.hpp: `void TSpcInterp::push(int v) { push(TSpcSlice(v)); }` level (NpcEvent PASS, evIsNpcSinkBottom 100).
- MoveBG/MapObjBlock.hpp: TTelesaBlock(const char* name = "テレサブロック") : TJuiceBlock(name).
- MoveBG/MapObjPinna.hpp: TMerryPole::unk138 is TPosition3f.
- JGeometry/JGMatrix34.hpp: identity() nine zero stores as one assignment chain in reverse store order.
- Enemy/PathNode.hpp: TPathNode(THitActor*) zero unk4 via a TVec3(0,0,0) temporary (BeeHive nerves).
- JDrama/JDRNameRefGen.hpp: search2 binding its result before returning (+8/+16 per expansion; whole-tree measure; initECDisp overshoots).
- Diff of items 1-4 saved: scratchpad/grpcj/header-items-batch108.diff.
## From batch 109
- Research: the `a = b - c` -> `bl sub` pool residue (16 dead below vs retail 12 above the operator temp); five functions blocked.
## Batch 110 outcome: SnapTimeObj, PollutionPos, TalkCursor linked (452). Header round 20 candidates (parked binding levels): TimeRec.hpp endTimer level; MapObjBase.hpp getMapCollisionManager(); MapObjRailBlock.hpp checkRailFlag/unk138 accessors; MSound.hpp getBgm(); TalkCursor.hpp getMActor(); MActor.hpp getModel(); SunModel.hpp isInBounds carrier. Do NOT edit Map/PollutionPos.hpp (breaks the DOL).
## Research 113 outcome: the `a = b - c` temp sits 16 above the local floor (retail 4): +8 from the operator-= level, +4 from the reference return copied out; only a destination-pointer helper `void f(V* d, V a, const V& b)` reaches the floor; void operator-= is refuted project-wide (-1 function, 35 regressions). 102 retail `bl sub` sites in 57 units, none exact — the biggest single lever left; needs a per-site hunt for the helper shape (or the sites are not `a = b - c`).
## From batch 114 (header round 20)
- MarDirector.hpp: TMarDirector::initECDisp is `static` (param_3->insert); zero regressions measured.
- LiveActor.hpp: getLatestNerve() forwarder (NpcEvent parks NpcEventGetLatestNerve).
- Nine MoveBG ctor-body initialisations listed in MarNameRefGen_MapObj.cpp's TODO (TMapObjStartDemo, TMapObjBillboard, TTurboNozzleDoor, TBigWindmill, TMapObjRootPakkun, TMapObjPuncher, TMareCork, TMareEventPoint, TPictureTelesa); TBalloonKoopaJr::mCenterPos and TAmiKing want assignment chains; TSandBlock/THideObj ctors should be inlined; TMapObjSteam's name string in small data.
## Sweep 112 outcome: +49 exact. Header round 20 = getters: TLiveActor::getSpine() (31 fns), TMario accessors (list above), const overloads (getHolder etc.), plus initECDisp static, TLiveActor::getLatestNerve(), batch-110 parked levels, nine MoveBG ctor-body inits.
## Research 116 outcome: the `bl sub` slot is a pool-ORDERING difference — retail allocates per statement (by-value temp first, then that statement's level bytes), we hoist all level bytes for the function to the bottom of the pool; prefix = 8 x reference returns copied out; 122 of 130 sites already emit the bl; no header spelling reaches retail's (4, 12) layout. Next research: what makes MWCC allocate per statement (a non-trivial temporary destroyed at the statement end? a scope per statement — e.g. each `a = b - c` inside its own block, or an inlined helper per statement?).
## Batch 115 outcome: bgpoldrop, NpcColor linked (454). Header items: MathUtil.hpp MsMtxSetRotX(Mtx, f32) (weak 0x7c, parked BGPolDropSetRotX); JDRNameRefGen.hpp search<T> binding its result (+12/expansion; PreEntrySearch parked) — tree-wide measure.
## 2026-09-18 10:00 EDT: weekly Opus limit hit; batches 117/118/119 killed mid-run and resumed via SendMessage. Research 119's finding before the cut: a user-declared `~TVec3() { }` collapses the whole dead pool (temp at the floor 0xc, frame 0x30, same 55 instructions) — retail is at 0x10/0x40, so it needs combining; measure project-wide (it also flips batch 69's triviality rule).
## Research 119 outcome: per-statement pool geometry = `operator-` returning TVec3 BY VALUE (+ one bound pointer inside it gives retail's exact layout) — but every by-value return costs +6 instructions (no NRVO/elision in MWCC 1.2.5); retail reserves the slot AND elides the copy. The 130-site `bl sub` population is blocked on that elision. `~TVec3() {}` is catastrophic project-wide (-2% matched code). Dead-local geometry law recorded. Consider closed as a research thread unless a new elision idea appears (e.g. `operator-` written as a non-member returning by value from a *reference parameter* it modifies in place, a compiler option difference, or the sites not being `a = b - c`).
## Batch 118 outcome: MarioWait linked (455). Header items: TMario::getYoshi(), SMSGetMarioOriginal() (Mario.hpp); TSmallEnemy::getJuiceBlock() binding (SmallEnemy.hpp). MarioParticle has 8 undefined UNUSED stubs (scaffolding task).
## Header round 20 outcome: +26 exact; items 1-8 mostly accepted; rejected bindings inside shared accessors (getModel, getMapCollisionManager, checkRailFlag). Sonnet calibration batch 123 running on PacketUtil/MSoundStruct/SelectDir/MapObjItem2/fishoid/MapObjFlag — compare against Opus closure batches (units closed, functions to exact, tokens, duration).
## Header round 21 (from batches 118/120/115)
- MoveBG/MapObjManager.hpp: SMSGetMapObjManager() binding its result (MarioReceiveMsg parks it; getGesso).
- Player/Mario.hpp: a binding-level fludd accessor (+16/expansion; TMario::perform), TMario::getYoshi(), SMSGetMarioOriginal().
- Enemy/SmallEnemy.hpp: TSmallEnemy::getJuiceBlock() binding.
- MarioUtil/MathUtil.hpp: MsMtxSetRotX(Mtx, f32) weak 0x7c (BGPolDropSetRotX parked; bgpoldrop linked — must stay exact).
- Camera/SunModel.hpp: isInBounds carrier (dead 48-byte non-trivial local; +0x30 on TLensGlow::perform); J3DFrameCtrl::update carrier (untested).
- JGVec3.hpp: one level above TVec3(f,f,f) for TSleepBossHanachan's ctor (closes MarNameRefGen_BossEnemy) — per-site risk.
- JDRNameRefGen.hpp: search<T> binding its result (+12/expansion) — tree-wide measure (search2 binding was rejected).
- DO NOT LINK MarioGamePad (stackAlloc[83] padding).
## Batch 122 outcome: MarioSwim linked. Header item: LiveActor.hpp checkLiveFlag2 returns u32 (walker). Research population (by-value return): boid calcBoids/calcForces/calcGoalForce, walker bind, MarioCap perform.
## Sonnet calibration (batch 123): 6 units, 0 closed, 0 functions to exact (five lifted 0.4-13 points, 3 real structural fixes), 0 regressions, 0 bad commits; 439k tokens, 289 tool uses, 31 min. Opus closure batches of the same shape: 118 (1 linked, 17 exact, 388k, 62 uses), 120 (4 exact, 388k), 122 (1 linked, ~6 exact, 386k, 143 uses).
## A/B result: Opus second pass (128) on the six Sonnet units closed 6 functions (392k tokens, 168 calls, 42 min) where Sonnet (123) closed 0 (439k, 289 calls, 31 min). Header items: TSelectDir::getGamePad() binding; Enemy.hpp isReachedToGoal copy-and-sub; fishoid `__dt__15TFishoidManagerFv` weak in map / global in object (implicit dtor declared — remove).
## From batch 126
- config.yml block_relocations sweep for MSD_BGM_* (0x8001xxxx) literals misread as .text function addresses (startOpenWindow); ScreenUtil.hpp TAfterEffect TVec3 member at 0x5C + by-value setter; fishoid implicit dtor; NpcNerve getCurGraphIndex four-level chain (research).
## After pass 129: ObjModel, MapObjManager linked (462). LINK BLOCKER: Map/PollutionManager.cpp is 100/100 but changes the DOL (hash ca608a9a...) — diagnose per linking.md (cmp -l / re-link map; rogue-include order; weak flush order; undefined weak inline via check-weak-defined).

## Batch 132 outcome (structural): MSD_BGM `.text` relocations blocked by `target:` range (startOpenWindow exact); fishoid/Map/MtxUtil implicit ctor/dtor bindings fixed; check-weak-defined clean (NOBODY = ShadowUtil guards only); MSoundScene calcPos helpers are DEAD (pool-slot owners, values unrecoverable) — unit cannot link until reconstructed.
## Structural queue (batch 134): PollutionManager link blocker (100/100, DOL ca608a9a...); 17 weak-in-map/global-in-object bodies to move into the units' OWN headers (cameragc, sunmodel, PPCArch, odenotstub, coasterkiller, PauseMenu2, J3DClusterLoader, BathWaterManager, e_asin fabs, hyperbolicsf fabsf); static fixes in OS/printf/msghndlr/mem_TRK/msgbuf; J3DAnimation/J3DMaterial template-vs-static. MarNameRefGen's 15 (linked, DOL exact) — leave.
## Sweep 130 outcome: address-binding lever (+8/+16 per expansion, frame lever only) 7 exact; pointer/scalar and address families both exhausted on the 8/16-short population; residue = 100 site-free functions + 14 unparseable bodies. Reference form inert.
## LINK BLOCKER: Camera/CubeManagerBase.cpp 100/100, DOL 7165e897 (like PollutionManager). Lesson: objects.json entries are .cpp spellings; check ninja's exit code on every link test (a .o entry made configure fail and the stale DOL hash read as exact).
## Research 133 outcome: implicit derived-from-base conversion on a return reserves +16 frame (pool 20 low / 4 high) — spell iterator(Base::insert()) explicitly (std-list.hpp; MapWireManager insert exact). it++ cost = by-value return slot; retail walks are ++it. seal/MirrorActor/PerformList/SDLModel residues fully mapped, no header lever left.
## Pass 131 outcome: SunGlass, CameraWarp linked (464). Header items: JGVec3 cross() temporaries declared _z,_y,_x (FPR ranking; test tree-wide); J3DMaterial forwarder at SampleCtrlNode (3 exact spellings, judgement). Recorded: sunmgr (register swap), NpcCallback/spider/liveinterp (by-value residue), MarDirectorPreEntry/M3UJoint/MapArea/AnimalNerve/CameraNormal/CameraMultiPlayer (mapped). 389k tokens for 2 closures: no more third passes.
## Batch 134 outcome: PollutionManager + CubeManagerBase linked (466); bindings 40->11 (left: cameragc 2, sunmodel 1, PauseMenu2 2, BathWaterManager 3 = caller inline depth; J3DClusterLoader 3 = reconstruct readCluster's implicit instantiations). MarNameRefGen: ORDER errors reported post-.tpp (verify pre-existing). MarioGamePad: pool order + stackAlloc, do not link.
## Catalog contradictions found by the rules-card agent (resolve in a later docs pass): depth-1 inline "no limit" vs onYoshi/TTelesaSlot/sqrtf; depth-2 allowance 9 vs 10 (cameragc unit file stale); fp_contract locals-only vs MapObjWave/BeeHive; accessor 8-byte steps vs +4 forks; startSoundActor slot 4 vs +8; MsRandF overloads; search<T> vs search2 guidance; named pointer local prices; &getPosition() sound arg; stale worklist (MSBgm::init, TMapObjWave::perform linked; hamukuri checkLiveFlag const); translation(x,y,z) direction.
## Batch 135 outcome: __start, OSThread, dolphin_trk, JAIGlobalParameter, JAIGFrameStream linked (471). TODO now: OS.c `_stack_addr` at JP 0x80424008 (OSThread.c has US) — fix + OSInit likely closes + link OS. Scaffolding follow-up: MISSING UNUSED bodies in OSThread (7), J3DModel (22), J3DMaterialFactory (2); validate-symbol-order needs --map-tu for __start.c.
## OS linked (472) by the orchestrator (US stack/arena constants). Batch 138 = Sonnet lever sweep over unlinked library units (fresh population).
## Closure 136 outcome: MapObjOption, ScrnFader linked (474); wireBinder init + CameraDemo ctrlNormalDeadDemo_ exact. Recorded: MapMakeData (updateCheckData size proves setCheckData spelling; carriers illegal), MathUtil (trash[] paddings, not linkable), wireBinder bind (by-value), updateDemoCamera_ (pool order). 574k tokens / 2 links (old brief).
## Sweep 138 (Sonnet) outcome: library 8/16-short population has no lever sites (29/32 pure dead frame); 2 statement-order fixes; objdiff reloc-diff mode trap. Library closures need Opus reading, not sweeps.
## Header round 22 outcome: 2,4,5,6,10 accepted; 1 (cross z,y,x) REJECTED tree-wide; 3,7,8,9 rejected. checkLiveFlag2 is BOOL. Open header items: none queued.
## Batch 141: J3DClusterLoader reconstructed (PASS). TEndingString and MapObj factory items were STALE (done earlier). Verify queue items before spawning.
## Batch 140 outcome: 3 exact (JASHardStream, JPADraw, JALModSe), 0 links; library ne=2/3 residues are register/FPR classes. Parked header: JALSeModXxx ctor in-class bodies (append needs 0x68 pool).
## Closure 139 outcome: MapObjGrass, PollutionAction linked (476). Parked header: MActor::initSimpleMotionBlend frame=-1 defaulted wrapper (NpcParts). Known-open: riccohook (JGadget insert interior bytes), BossHanachanEffect (this-vs-pool rotation), NpcParts partsPerform (register permutation), MapWarp init (+4).
## Research 142: a = b - c is intra-statement allocation ORDER (4 below / 12 above the temp); inline-temp price = callee return type (ref 8, ptr 4, void 0); the 12 above are fed only by dead bindings declared after the subtraction. +9 functions if a legitimate spelling is found (TEnemyAttachment/TChuuHana/TAmiNoko/TKoopaJrSubmarine/TCoasterEnemy/TLimitKoopa/TLiveActor::bind, attackToMario, TLeanMirror::loadAfter). Bypassing operator-= loses __ami__.
## Research 144: callee-saved GPRs r31 downward in reverse order of value introduction (pool temps, then locals in reverse decl order, params, this last); named-scalar COUNT decides pool-vs-this — group scalars into an array. sunmgr 100/100 but DOL 934a10aa (LINK BLOCKER: section content). M3UMtxCalcBlendAux is a different rotation class (open).
## sunmgr link blocker data: target .text 0x7fc/.rodata 0x68/.data 0x40/.bss 0xb8/.sdata 0x08/.sbss 0x08/.sdata2 0x18 vs ours .text 0xd88/.rodata 0x67/.data 0x8c/.bss 0xb4/.sdata 0x18/.sbss 0xc7/.sdata2 0x14; extras: TViewObj dtor + JSUList<JALSeMod*> dtors (MSound rogue includes?). .rodata 1 byte short and .sdata2 4 short are the content leads.
## Closure 143 outcome: 2 exact (MovieRumble), MAnmSound bug fix, 0 links; remaining residues are known-open classes (JUTColor temp stride, sqrtf weak+bl, a=b-c, FPR). Small ne=3 units are exhausted for closure passes.
## Batch 145: sunmgr linked (477; DummyStrings include order was the blocker). Register-swap sites exhausted: declaration order moves ranking only among function-scope named locals; inert on this/params/pool temps/inner-block locals. updateAll's delta note corrected (only the 24-byte frame, header levers).
## Research 146: inline keyword buys only depth 1 (weak body: unlimited/9/6/2/0; plain: 14/9/6/2/0); true refusals = recursion, mutual recursion, varargs, inline asm only. sqrtf costs 8 (bl at depth 3 = depth measurement). Known-open narrowed to the three factory ctors (TTelesaSlot 0x98, TFence 0x4c, TSunGlass); TFence's is 2 statements so no budget reading reaches it — probably a different mechanism (candidate: something about the factory's call shape, e.g. the ctor called through new-expression in a switch-like strcmp chain?).
## Header round 23 outcome: JASDSPChannel linked (478); append/initSimpleMotionBlend rejected with measurements; getTranslation promoted.
## Depth pass 148: 12 lifted/3 exact, 0 links. Header lead: camera-position inline (gpCamera->unk124 via set(const Vec&) at depth 4) in cameralib.hpp for bosstelesa/sunmodel/lensflare (header round 24 item).
## Library re-pass 150: J3DMaterialFactory x2, JDRActor linked (481). Open: TPolarCamera::perform needs its own batch (named-scalar FPR setup).
## Closure 151: 1 exact, 0 links. Header round 24 items: TParamT<T>::get() by value for integer T (camerashake); camera-position inline (cameralib.hpp; bosstelesa/sunmodel/lensflare); JADPrm ctor 8-byte dead local (MSoundStruct 13x8); TPosition3f::TPosition3() UNUSED body (enemymanager).
## Closure 149: 2 exact, 0 links. Header items (round 25): JGVec3 member operator*(f32) const (MarioParticle); NpcBase.hpp dead local in an inlined ctor (NpcInitPrg 112 bytes); Mario.hpp declarations for MarioParticle's 8 UNUSED stubs.
## Header round 24: TParamT get() by value REJECTED (all variants); set(const Vec&) depth tell (8 sites); JADPrm carrier refuted (104-byte contiguous block); TPosition3 array tell. Round 25 items pending: JGVec3 member operator*(f32) const; NpcBase.hpp inlined-ctor dead local; Mario.hpp decls for MarioParticle UNUSED stubs.
## Re-pass 152: MapArea linked (482). Header lead: SunModel.hpp isInBounds carrier (+96/-8, lensglow); no 4-byte lever exists (MovieSubtitle, MarDirectorPreEntry).
## Batch 153: TPolarCamera::perform 99.69 (104 dead bytes = 96 + 8, nothing natural for the 8). JGRotation3 setEular chain fix landed.
## Header round 25: MarioParticle UNUSED bodies defined (PASS); operator* reference-return shape gains ~12 fns but loses ~13 + 4 weak copies (rejected; a per-site split may recover it); NpcBase carrier impossible; lensflare needs difference vectors as objects.
## Closure 157: 0 exact. Header items: JDrama::TDisplay::getRenderMode() caller-side dead bytes (RenderModeObj x4); JGadget TVector::insert frame 0x20 too large (remove a lever); Enemy.hpp isReachedToGoal calcDist copy 4 low. The four-function tier is mostly known-open classes.
## Research 159: operator* return type is a CONSUMPTION split (destination copy wants reference return, consumed temp wants by-value); only legal selector is operand order (v*k vs k*v), not applied. V2 (const TVec3& param, by-value return) = +12 improvements, -1 fn (__ami__ MISSING in Tongue) — retry if Tongue can keep __ami__.
## Library III (155): 4 exact, 0 links; JDRSmJ3DAct needs 48 bytes (concat spill), JASTrack/JASChannel residues recorded. Library ne=2..6 tier now swept once.
## Closure 158: 6 exact, 0 links. Header items (round 27): TPathNode::getPoint() raw read (costs TNerveBPTakeOff; per-site?); MapData.hpp ground-type predicates as == chains (bombhei forceKill).
## Header round 26: RenderModeObj linked (483); isReachedToGoal 100. Unit item: DrawSyncManager end() is a 0x80 method + dtor 0x20 short (reconstruct).
## Research 161: JGadget grouping = per-expansion block STRIDE (retail 8, ours 12), not the return-type ladder; iterator ops inert, rest of chain pinned. Named TSingleNodeLinkList begin/end lands perform but costs push_back x2. Class stays open (8 functions).
## Header round 27: free-carrier survey done (most rejected/parked); forceKill exact via raw receiver; DrawSyncManager end/dtor exact sizes.
## Follow-up 163: 0 exact. HEADER ROUND 28: Enemy.hpp setGoalPathMario pointer binding (+7 exact, -2 needing their own lever removed: TNerveWalkerTraceMario, TNerveTelesaFreeze).
## Re-pass 162: 1 exact, 0 links; MapMakeData has no legal carrier; two-function tier exhausted.
## Header round 28: setGoalPathMario binding +7 exact (10712). MarioAccess.hpp return-only bindings rejected.
## Land note: header round 28 landed with 7 tenth-point fuzzy drops on already-nonmatching functions (accepted: +7 exact, none lost from 100).
## Library re-pass II (165): 0 closed; the six ne=1 library units are research residues (dead class locals with empty ctors, register starts). bootDSP UNUSED 0x20 vs empty body (JAIBasic). J3DModel has 22 MISSING map symbols (link blocker later).
## Header round 29: shape exhausted (receiver bindings destructive); +1 exact. No header items queued.
## Land note: header round 29 landed with 1 fuzzy drop on a nonmatching function (accepted, +1 exact).
## Structural 166: RELOC-TARGET BUG CLASS found (invisible to objdiff): 11 exact. ~100 flagged sites unworked (GCConsole2, bossgesso, FlagManager, SelectMenu, MapObjMamma, sin/cos swaps) — next batch: reloc pass II with grpep/census_reloc.py, census_virt.py, census_calls.py.
## Closure 168: sunmodel 4->2 (perform needs a Vec-typed camera accessor; CLBScreenFPosToSPos 98). Header item: SleepBossHanachan.hpp level (MarNameRefGen_BossEnemy).
## Research 171: callee-saved FPR rule = inlined callee temps first (reverse decl order), then own named f32 locals in FORWARD decl order, then params reversed; volatile FPRs trade as blocks (knob: naming two values). drawJointObjStamp exact. MapObjCloud/expf/float_ exhausted.
## Library structural 170: JPADraw, JASHardStream linked (485); JAIGFrameSequence has checkPlayingSeqTrack left (4 dead bytes). Library structural well dry.
## Header round 30: getUnk124Vec sibling accepted (no exact gain, zero cost); SleepBossHanachan level refuted; MarNameRefGen_BossEnemy needs a different temp count.
## Linked ExceptionPPC, NMWException (487; symbol order FAILs are pre-existing, DOL exact — check later).
## FPR re-pass 174: startShake/keepShake exact (in-place parameter modification lever); camerashake needs execShake (56 low). Research seed: JPAGetRMtxTVecElement ranking (x f31, z f30, y f29) fits neither order with the sibling pinning the callee.
## Reloc pass II (172): tools/check-relocs.py landed; 8 exact; ~308 sites remain (mostly compensated swap pairs and inlining-difference call sets; real open: MapCollisionEntry::setMtx, TWireTrap::getWireDir depth 3, MapEventSinkBianco::loadAfter, TOptionControl). Untouched big units: GCConsole2, bossgesso, SelectMenu, MapObjMamma, MarioSpecial, ModelGate. Accepted 1 fuzzy dip (telesa sine/cosine swap, correctness).
## Structural pass II (176): 4 exact (3 bugs: movement inverted switch, electricDamage status, initLayerInfo FIRE||ELECTRIC). Census entries 61+ remain (640 total); next: structural pass III on 61-100.
## Batch 177: camerashake/PollutionCount not closable (no legal carriers). One-function tier for game units is now fully diagnosed.
## 4-byte re-pass 175: MovieSubtitle, MarDirectorPreEntry linked (489). +4 rung = direct-return fork nested inside a binder (global/static/member read); fork over a global must be used at every site of its wrapper family. Not reached: MapWarp init, isHitValid, checkSoundArea, calcGoalForce, CLBScreenFPosToSPos.
