# Codex orchestrator handoff

## State log

- 2026-09-23: Coordination infrastructure prepared in `wt/x-infra`; see `docs/CODEX_INTEGRATION.md` for claim and landing rules.
- 2026-09-23: Claim ledger and land lock are live on local/decomp-progress; Claude orchestrator should adopt tools/claim.py and tools/worktree.sh locked before its next landing.
- 2026-09-23: Trialed gpt-6-sol/low on MoveBG/MapObjDolpic and NPC/NpcBase, and gpt-6-luna/max on Camera/lensflare and GC2D/SelectShine2. Both agents followed the known-open stop rule; no safe changes or commits. All four claims were released and worktrees removed. See findings-codex.md.
- 2026-09-23: Three-agent repeat: gpt-6-sol/low fixed `Enemy/Kumokun::checkOnMovingWall` (93.36→95.55 fuzzy; operand-order bug, landed as 017508fa). gpt-6-sol/medium audited ten `Map/BathWaterManager` functions and gpt-6-luna/max audited eleven `Animal/BeeHive` functions; neither found another justified edit. All Codex claims released and worktrees removed. See findings-codex.md.
- 2026-09-23: Closure follow-up found no safe edits in Enemy/Amenbo, MarioUtil/MtxUtil, Enemy/rocket, Enemy/mameGesso, Player/MarioParticle, or Enemy/BathtubKiller. The BathtubKiller `setDeadBathtubKillerAnm` expansion is already present at both call sites, refuting the old missing-inline lead. All variants restored, claims released, and worktrees removed. Prefer a shared-cause research batch over another broad audit of documented residues.
- 2026-09-23: Operand scan led to three landed fixes: `Enemy/bgtentacle` sine inputs (33707b4e), `Enemy/bosswanwan` pull-velocity clear order (e74b5986), and `Camera/cameragc` hold-angle chase member (cfa6b3f5). Game 63.68%, 192/385 linked; All 70.04%, 524/732 linked. Sol/low found the Wanwan and camera fixes; Sol/medium found the tentacle fix; Luna/max found no edit but its scan exposed the tentacle clue. Koopa, MapObjMare, limitkoopa, and SelectShine2 operand candidates were refuted. All completed claims and worktrees were cleared.
- 2026-09-23: Follow-up Sol/low audits of `MoveBG/MapObjInit`, `MSound/MSound`, and `Enemy/hamukuri` made no safe edits. Every remaining fully matching code-and-data unit is already source-linked in `config/GMSE01/objects.json`. Claims and worktrees cleared; no Codex agent remains active.
- 2026-09-23: A targeted same-opcode operand pass with Sol/low landed three semantic corrections: `Enemy/fireWanwan` reads floor friction when grounded (f2e59f60), `Player/Tongue` calls the retail virtual `checkTaking` (51fbcfee), and `System/MarDirectorSetupObjects` calls `getTotalFreeSize` at the final heap site (2d090efc). Each passed zero `changes_all` regressions, full build, exact DOL SHA-1, and symbol order. Latest verified report: Game 63.71%, 192/385 linked; All 70.12%, 525/732 linked (the linked-unit rise includes Claude's concurrent work). No Codex claims remain. See findings-codex.md for refuted operand clues.
- 2026-09-23: Refreshed the operand scan across all 385 game units (102 candidates, zero errors) after prior scan clues became stale; artifacts are at `../sms-wt/x-scan-v2-scratch/`. Sol/low corrected `Animal/AnimalBase::execWalk` march speed versus acceleration frame-rate scaling (73d9cc33; 89.89→90.25% fuzzy). Zero `changes_all` regressions and exact DOL SHA-1; symbol-order failures were pre-existing. MapWire, MarioDraw, CameraBGCheck, CardManager, ShadowUtil, and hanasambo remained unchanged after targeted checks. Latest report: Game 63.78%, 193/385 linked; All 70.18%, 526/732 linked. Linked-unit gains include concurrent Claude work. All Codex claims and worktrees cleared.
- 2026-09-23: A fresh closure/semantic pool found no safe changes in GC2D/Menu, Map/MapMakeData, NPC/NpcParts, MoveBG/MapObjBianco, Enemy/TabePuku, or Enemy/tinkoopa; each remaining residue had a documented deep search. Follow-up operand research found two real corrections: GC2D/CardLoad's middle pane scale is 1.5f (1d645406) and Player/MarioAutodemo multiplies by 50 before 2 (bd64a8fa). Both landed with zero `changes_all` regressions, exact DOL SHA-1 and passing symbol order. Current report: Game 63.83%, 193/385 linked; All 70.22%, 526/732 linked. Each correction improved its function diff but changed aggregate matched-code by 0.00 percentage points and linked no unit. Concurrent Claude work accounts for the aggregate rise since the prior state entry.
- 2026-09-23: The next Codex pool tested literal, branch, CSE, stack-slot and library closure leads, then audited TRK support, JKRExpHeap, JALModSe, GC2D/SelectMenu, MoveBG/MapObjRailBlock and System/DrawSyncManager. No safe edit survived: each candidate was a previously documented compiler-layout residue or required a refused dead-local/header lever. Player/MarioAccess remains blocked by a CSE reload after 17 recorded variants; no unit was linked. See findings-codex.md for the specific refutations. Claims and scratch worktrees are to be cleared after this record lands.
- 2026-09-23: A follow-up research pool separated Koopa's true per-site `changeAnm` call from koopajr's constructor inline threshold, confirmed MarNameRefGen's `TCameraMapTool` assignment problem lacks a justified layout shape, and rechecked representative unclaimed sub-95% functions. No safe edit or new closure lead. Another broad unit audit should be preceded by a genuinely new binary/compiler hypothesis; source TODOs already cover the straightforward variants. Three research claims/worktrees are to be cleared after this record lands.
- 2026-09-23: Three further claimed audits of JALModSe, MarDirectorSetupObjects and AnimalBase made no safe edit. JALModSe's exact-layout probe still lacks a real type in the repository/map; setupObjects' apparent width/height call reversal stores the same values; AnimalBase's previous frame-rate correction is present and its remaining gaps are documented inline/matrix layout. The pool linked no unit. Claims/worktrees are to be cleared after this record lands.
- 2026-09-23: Corrected `JSystem/dsptask`'s embedded `jdsp` data so the object emits the retail relocation to `lbl_80004600` at offset 0x1938 (6e4228e3). Matched data increased by 7392 bytes, from 98.68970% to 99.84277% overall (+1.15307 percentage points); code stayed Game 63.92308%, 193/385 linked, and All 70.51359%, 527/732 linked. Zero `changes_all` regressions, exact DOL SHA-1 and symbol order PASS. CameraChange, CardManager, JPAEmitter, THPAudioDecode and JDRFrmGXSet audits found documented code-layout, register or shared-header residues and made no edit. All Codex unit claims and worktrees were cleared. See findings-codex.md.
- 2026-09-23: Follow-up pool made no source edit. JPAMath, MapObjPlane and ShadowUtil were already documented compiler-layout residues. A fresh branch/immediate scan found no unrecorded GMSE01 constant. The retail map corrected two stale premises: JPAEmitter's weak `JSUIosBase` vtable emission is genuine and discarded by the linker, and `JAIBasic.hpp` already declares the 0x94 previous-voice field as a word. All Codex claims and worktrees were released; see findings-codex.md.
- 2026-09-23: Removed artificial address-taker expressions that had no codegen effect in `Enemy/gesso::setPolluteGoal` (c57e63af) and `Map/BathWaterManager::calcWaterModel` (b97f680c). Both changes passed zero `changes_all` regressions and exact DOL SHA-1. Aggregate matched code/data and linked counts stayed Game 63.95862%, 193/385 linked; All 70.54167%, 527/732 linked; All data 99.84277% (each delta 0.00000 percentage points). `GC2D/ExPane`, `NPC/NpcNerve`, `Map/MapEventDolpic`, and `JSystem/JParticle/JPADrawVisitor` still require their addressability or exact source shape; removing their suspicious expressions regressed code or lacked a binary-supported replacement. All Codex claims and worktrees were cleared. See findings-codex.md.
- 2026-09-23: Corrected `Player/ModelWaterManager::splashGround`'s clean-size multiplier from 10.0f to the retail 32.0f (93c83343); its inline copies in `move` improved from 96.38% to 96.55% fuzzy and the extra source literal disappeared. Landing passed zero `changes_all` regressions, exact DOL SHA-1 and symbol order. Aggregate deltas were 0.00000 percentage points: Game code 63.95862%, 193/385 linked; All code 70.54167%, 527/732 linked; All data 99.84277%. EventWatcher's and BossHanachanMain's other extra literals trace to inlining or weak duplicates, while the old Application string-gap explanation is stale. All Codex claims and worktrees were cleared. See findings-codex.md.
- 2026-09-23: A 99%+ semantic scan found a real branch bug in `GC2D/SelectMenu::initData`: the source skips shared shine/menu setup when coins are below 100, whereas retail enters it. Three valid brace/condition shapes worsened this function's fuzzy match (direct fix ~109 to ~145 mismatch markers), so the fix remains parked under the no-regression rule. A second known bug in `NPC/NpcParts` likewise gains one marker under three valid spellings. Call-target and field/float scans found no other supported 99%+ correction. All Codex claims/worktrees cleared; no source change or aggregate score delta in this batch. See findings-codex.md.
