# Source linking

An object can match in code and data and still break the DOL when linked from source.
Promotion needs the full DOL SHA-1 check, not just `changes_all`.

## Weak emission order

`validate-symbol-order.py` downgrades weak disorder to a warning, so a unit with only warnings can still be unlinkable.
When a matching object perturbs the link, compare where its weak symbols are emitted against the map before suspecting code.

- **`Map/PollutionEvent`**: four event classes had no virtual members, so the compiler flushed its weak queue early and emitted `TPollutionTest::__dt` and `perform` before `loadAfter`. The map gives each a 0xa0-0xa4 vtable and virtual destructor; declaring the destructors moved the weak pair after `loadAfter` and it linked.
- **`GC2D/MessageUtil`**: `isNotDrained()` emitted `JSUMemoryInputStream::getPosition`/`getLength` between the two functions. The retail copies come from `MarioPositionObj.cpp`; picking ours shifted `SMSMakeTextBuffer` by 16 bytes. Calling inherited `getAvailable() != 0` gives the same instructions without emitting them.
- Placement follows source order reversed: a symbol that must come first goes last in the file.

## Duplicate globals: archive linking

The map lists unreferenced duplicate `cSunVolumeName`/`cSunsetVolumeName` in `MarNameRefGen_Map`.
Loose objects reject them; one archived object lands too late.
Archiving the complete ordered input list with MWLD reproduces retail handling. GMSE01 enables that build path.

## US `.rodata` boundaries

Application's two disc-error strings are 121/106 bytes in the US build, not the imported 124/109.
Seventeen later Application strings shift 4 bytes and section alignment moves the next `.rodata` boundary 8 bytes earlier, so every downstream symbol follows (1,273 shifted, 6,502 unchanged).
Corrected in batch 35; `MarDirectorCreateObjects` and `TargetArrow` then linked.
Correct only documented regional ranges. Do not repeat the boundary-only relink trial from batch 6, which left the linker producing no executable.

## `Camera/CameraInbetween` (unlinkable, open)

Matches 100% but shifts the DOL.
- DOL `.sdata2` (section 14, base 0x8040eba0) is 0x8c40 in ours vs 0x8c60: exactly 0x20 short, the TU's whole literal block.
- Ours emits 7 local literals; the original has 5 plus a dtk gap, and the map lists UNUSED `@1758`/`@1759`.
- The two extras are `0.5f`/`-0.5f` from `CLBRoundf<s16>` (`Camera/cameralib.hpp`), inlined through `CLBChaseConstantSpecifyFrame<s16>`. `CameraBGCheck.o` is the only extracted object defining `CLBRoundf<s>__Ff`, so retail dedup orphaned this TU's copies.
- `CameraBGCheck.cpp` is already before `CameraInbetween.cpp` in `configure.py`.
- `validate-symbol-order` passes, apart from two UNUSED stub size warnings (`setInbetModePosAngleY` 0xbc, `execInbetweenAndCalcPosAndAt` 0xec). The stubs are not the cause.
- `.sdata2` `d` vs `r` (writable vs readonly) is a red herring; `ansi_fp.c` has it and links.
- Unexplained: the DOL loses 0x20, not the 8 bytes the orphaned floats account for. Next step: inspect where our `.sdata2` block lands in the map/ELF.

## Other notes

- Identical caller instructions do not prove identical whole-file emission; inspect extra weak definitions when a promoted object shifts later code.
- A full executable match does not validate bodies in objects still linked from the original.
- Count already-matching unlinked library objects by absence from `config/GMSE01/objects.json`, not `complete_code_percent` (a zero-code unit reports 100 vacuously).

## Batch 72: data-only and link blockers (2026-09-18)

- **dtk false relocations look like data mismatches.** A data word whose value looks like a RAM address (`0x80xxxxxx`) becomes an `R_PPC_ADDR32` reloc in the *extracted target* object with zeroed bytes; objdiff then scores our correct literal as a mismatch (a table at 97-99% with exactly one differing word; `objdump -r -j .data` on the target names an unrelated function). Verify with a byte dump; the unit links unchanged. Seen on `JUTDirectPrint`'s font tables, `JASPlayer_impl`'s tremolo table and `dsptask`'s blob — all three now linked. Permanent fix: dtk `block_relocations` in `config/GMSE01/config.yml` (queued; re-splitting is repo-wide).
- **`CameraInbetween` is solved and linked.** Its earlier "0 bytes left but unlinkable" entry is superseded: the DOL lost 8 bytes of `.sdata2` because an 8-byte int->float double needs an alignment hole in front of it unless already 8-aligned, and pool order follows emission order (reverse source order), so the values first requested by the last functions in the file — the two empty UNUSED stubs — come first. Giving `setInbetModePosAngleY` a `1.0f / (f32)frames` and `execInbetweenAndCalcPosAndAt` a `0.0f` store reproduces retail's pool exactly (including the 45-id gap). Rule: a unit that matches everywhere but shortens `.sdata2` has a pool-order problem, and pool order is evidence about UNUSED stub bodies.
- **Include order controls `.rodata` prefix order, not just its presence** (DebuTelesa, data 64 -> 100): `Map/MapCollisionManager.hpp`'s zero/one vector pair must be included *after* `M3DUtil/InfectiousStrings.hpp`.
- **A non-covariant virtual return type silently appends a vtable slot** instead of overriding (`TEffectEnemyManager::createEnemyInstance` returning `TLiveActor*` where the base returns `TSpineEnemy*`): one wrong slot plus one extra slot with every function still 100% (effectEnemy, data 32 -> 100, linked).
- **Run `validate-symbol-order` before proposing a link**: it catches missing UNUSED symbols (`emitEffect`, `print`, `gpBeamManager`'s defining TU) and global-vs-weak dtors that objdiff cannot.
- Refuted: an argument-area explanation for frame gaps (6/7/8-word calls give the same frame); an 8-byte non-trivial local in `MSound::startSoundActor` closes `TDebuTelesa::receiveMessage`, `TEffectEnemy::setDeadAnm` and `TBossHanachan::emitOneTimeSandPillar_` at once but regresses ~15 exact callers — the object is per call site (research lead). `AudioDecode`'s `#pragma dont_inline` is load-bearing (retail body had 15+ statements); `AudioDecoderForOnMemory` is a pure callee-saved rotation (nine variants in the source). `beam::calcVertices` (95.6): 16 extra bytes redistributed, not appended.

## Header round 14 (2026-09-18): weak flush order and mechanical false-relocation search

- **Weak emission order is most-derived-class-first, then declaration order within each class**, and a virtual *override* can be moved freely in a derived class's declaration list because its slot belongs to the base — declaration position is a free lever on weak flush order. `WoodBarrel` was 100/100 but unlinkable because `TMapObjGeneral`'s `getLivingTime`/`checkIllegalAttr` pair flushed in swapped order (52 bytes: the 8-byte `.text` pair plus 25 vtable words); moving `checkIllegalAttr`'s declaration after `getLivingTime()` in `MapObjGeneral.hpp` fixed it (linked). Header body placement (in-class vs out-of-class `inline`) is not a lever.
- **A matching-but-unlinkable object can differ in content rather than layout**: when the re-link map shows every section at the same address and size, `cmp -l` the two DOLs — a handful of bytes plus the vtable words referencing them names the weak symbol (text section 1 is at DOL file offset 0x2540; the MWCC map's file offset is the ELF one). No 100/100 unlinked unit remains.
- **dtk false relocations are findable mechanically**: a data-section `R_PPC_ADDR32` in an extracted target object whose target function is not defined in that object and carries a nonzero addend is a blob word misread as a pointer. Six blocked in `config/GMSE01/config.yml` (`block_relocations`, spelled `source:`/`end:`): `JUTDirectPrint::sFontData`/`sFontData2`, `JASystem::Player::sTreTable`, ModelWaterManager's `tmp_data`, `DSPInterface::DSPRES_FILTER`, dsptask's `jdsp`. `splits.txt`/`symbols.txt` unchanged; JUTDirectPrint/JASPlayer_impl/JASDriverTables data -> 100, `dolphin/os/__start` 68 -> 99.99 (the `jdsp` word was mis-splitting `__init_data`), matched data 74.16 -> 74.79%.

## Header round 16 (2026-09-18): the undefined weak inline that externalises a vtable

- **A weak map symbol our tree never defines is a missing header inline body, and it silently externalises the class's vtable.** For a class whose virtuals are all inline the vtable is weak and emitted in every user TU; MWLD keeps dropped weak duplicates as zeroed dead space. One undefined virtual (`TTakeActor::getRadiusAtY`, weak 8 bytes = `return mDamageRadius;`) moved `__vt__10TTakeActor` (0xb4) out of `MarioInit.o`, whose `.data` was exactly 0xb4 short of the map's 0x224, so the link pulled ModelWaterManager 160 bytes early — invisible to objdiff (100/100), fatal to the link. Fixed: MarioInit linked, fishoid data 27 -> 100. Same defect found once more by sweep: `TMarioGamePad`'s weak dtor (0x64) with `__vt__13TMarioGamePad` (Application data 52 -> 57). Diagnostic chain: link it, compare the two DOL headers to find the shrunken section, `nm -n` both ELFs around the address to find the object that starts early, compare that object's section size to the map's contribution line. Detector: every `(func,weak)`/`(object,weak)` map symbol must be defined in some `build/GMSE01/src/**.o`. Sweep is clean as of this round.
- A single UNUSED map entry for a weak symbol means one emission (dropped weak duplicates appear as `>>> UNREFERENCED DUPLICATE`, not extra UNUSED rows); `TTakeActor(const char*)`'s UNUSED 0x50 in MapWireManager comes from `TMapWireActor`'s ctor losing its `#pragma dont_inline` (which also lands `TMapWireActorManager`'s ctor at 0x154) but that costs the exact `loadAfter` — the split is per site.

## Data sweep batch 93 (2026-09-18): matched data 74.9 -> 87.6%

- **`Map/MapCollisionEntry.hpp` (and `MapCollisionManager.hpp`) must come after `M3DUtil/InfectiousStrings.hpp`**: its `setUpTrans` compound literals emit the 12-byte zero + one vectors at parse time; earlier, they shift every later `.rodata` offset by 0x18 (MapObjInit 0.4 -> 100, bosseel 56 -> 100, Map 31 -> 100; MapObjHide/chuuhana needed the include added there). Exception: igaiga/koopajr's pair carries an id higher than the file's own strings (from a call site mid-`.cpp`, not the header); MapObjLib's target has no pair at all.
- **objdiff's `matched_data` is per-section all-or-nothing**: a section at 99.9% contributes zero matched bytes, so one partial symbol pins a unit's score (Map: one string with ASCII instead of full-width parentheses held it at 30.9%). Extra dead-stripped objects are harmless (MapObjInit hits 100% with 35 extras). Read the `nonmatching`/`missing` rows, never the percentage.
- **Byte-diff the extracted section** (`objcopy -O binary --only-section=.rodata` on both objects, first differing offset): MapObjInit's 52 KB blob was one 32-byte hole (`JuiceBlock_hit_data_table` + `JuiceBlock_obj_hit_info`), 757 partial objects collapsing at once. `secdiff.py`/`vtcmp.py` in the batch-93 scratchpad.
- **`TModelDataLoadEntry` arrays are zero-terminated**: a map entry of 0x18 is one entry plus `{ nullptr, 0, 0 }` (smallEnemy 67 -> 100).
- **The rogue-include prefixes are three distinct sets and the map says which**: 12 zero bytes + a 20-byte Shift-JIS string = `System/DummyStrings.hpp`; those plus four `MActorMtxCalcType_*` strings (47/53/49/36 bytes) = `M3DUtil/InfectiousStrings.hpp` (superset); fifteen 12-byte `.bss` objects + a 764-byte `__sinit` = `MSound/MSSetSound.hpp` + `MSound/MSoundBGM.hpp` (also made `__sinit_coasterkiller_cpp`/`__sinit_effectObj_cpp` exact). Inverse: MarDirectorEvent's target has no `.rodata`, so its InfectiousStrings include was wrong.
- **Unmangled data names in the map are plain C++ globals** (`Vec _posByCamera[256]` = `MSSceneSE::mTrans[256]`; MSoundScene 1 -> 100). **A class static in the map's `.sdata` (not `.sbss`) has a non-zero initialiser** whose value is in the target `.s` (`TTobiPuku::mLandAngle = 90.0f`, `mBoundSw = 1`, `mBoundVelocityY = 0.8f`, `TEnemyManager::mIsCopyAnmMtx = true`); check `(object,local)` before leaving a global non-`static`. A `$NNNN`-suffixed data name is a function-local static (`jIndexTable$3048` inside `TNerveHanaSamboDie`).
- Vtable sweep: fabricated declaration-only virtuals removed (`TRiccoHookManager::perform`, `TBEelTearsDrop::receiveMessage`, `THinokuri2::getSaveParam` -> `getSaveParam2()`, six in `TMoePuku`); four `createEnemyInstance()` in TobiPuku.hpp returned `TLiveActor*` (non-covariant); `getObjAppearPos()` is `const` (MapObjSirena 16 -> 100); `TJuiceBlock::touchActor` was a real missing override (0x6c exact). Follow-up: hinokuri2's `getSaveParam2()` wrapper costs one inline level at 83 dispatch sites (Burst 99.9 -> 97.4, no exact function lost) — spell `((THino2Params*)getSaveParam())->` per site instead.
- Remaining data classes: 26 units with a pool-order float/double in `.sdata`/`.sdata2` (needs the function that first requests it — a code-side item); missing factory/class bodies (MarNameRefGen x2, MenuDir, Application, MovieDirector's `TEndingString`, MSoundSE, MapStaticObject's `initUnique` jump table, `TItemNozzle::calcRootMatrix`); ShadowUtil's `$NNNN` counter; WaterGun's `@2685`/`@2687`.

## Structural batch 97 (2026-09-18): content blockers and the weak-symbol detector

- **A link blocker whose two DOLs have identical section tables is content, not layout**: `cmp -l` them, map the differing file offset back through the DOL header, and if it lands in a TU's own `.rodata`, `objcopy -O binary --only-section=.rodata` both objects — the rogue-include prefix order falls out. objdiff is blind to it because the prefix sits in unnamed head-of-section space (every named symbol still 100%). MapEventDolpic: `Map/MapCollisionEntry.hpp` included above `M3DUtil/InfectiousStrings.hpp` put the collision vectors first and left the section 5 bytes short; moved below, the unit links (442). The include-order rule is a *link* requirement, not only a data one.
- **`tools/check-weak-defined.py`** classifies every `(func,weak)`/`(object,weak)` map symbol undefined in `build/GMSE01/src/**.o` into NOBODY (no body anywhere: the round-16 vtable-externalising hazard) and INLINED (header body we expand everywhere while retail kept one out-of-line copy: a caller inline-refusal lead that `validate-symbol-order` reports per unit). 3400 weak symbols; NOBODY is down to `TEndingString` (a whole missing class in MovieDirector: ctor 0xe8, dtor 0x74, `perform` 0x140, UNUSED `startFadeIn` 0xe0, vtable 0x24) and ShadowUtil's two `$NNNN` guards; 48 INLINED across 26 groups. `TTelesaSlot(const char*)` was the one map-pinned missing body (read from the factory's asm; its `__construct_array(obj+0x1ac, TVec3::TVec3, 0xc, 4)` retypes `unk1AC` to `TVec3<f32>[4]`).

## Pool-order batch 96 (2026-09-18): matched data 87.8 -> 89.2%, 16 units to 100% data

- **Read `decomp-diff -t object -s nonmatching/missing` before dumping a pool**: 13 of 26 "pool-order" units were blocked by a `.data` jump table, four by a string or table, two by a `GXColor`. `pool.py` (per-object pool diff with alignment holes) and `jt.py` (group-normalised jump-table diff) are in the batch-96 scratchpad.
- **A jump table whose grouping matches but whose addends differ is code-gated** (the enclosing function is the wrong size; six units cannot reach 100% data until it matches); a different grouping is a wrong case label (`NPC_ANM_KIND_UNK10` = 16 where the table wants `MAD` = 10, NpcAnm 65 -> 100); one entry short is a missing case sharing the default (`case 12: default:` in `fireStreamingMovie`).
- **Literal ids increase with source position of first request**: moving `pos.y += 200.0f` above the x/z lines gave it the lower id (MapObjItem2); `set(x, y, z)`'s right-to-left evaluation put `7000.0f` before `-5000.0f` (bossgesso). Params-ctor literals interleave name-then-default in id order, so a `PARAM_INIT` list and its defaults read straight off `.sdata2` (`TMtxSwingRZ::TDeParams`: `mAcc = (0, -4, 0)`, `mBrake = 0.7f`).
- **`0x3c8efa35` vs `0x3c8efa36`**: pi/180 folded in double (`M_PI / 180.0f`, `DEG_TO_RAD`) vs the single-precision product retail uses (`0.017453294f`); `DEG_TO_RAD` is the wrong shape here. **A float literal in `.sdata` rather than `.sdata2` means `std::min`/`std::max`** (third confirmation; `fcmpo <literal>, <value>` = literal first argument). `/ 63` allocates `63.0f` where retail folded `1/63` (Amenbo). MWCC folds `literal - local` even for a named `f32` constant (DrawUtil's `300.0f`; retail subtracts at run time; open).
- `-str reuse` sharing is evidence (two `entry$` pointing at one `"default.bmd"` named the wrong array membership); a dead string or array's position is still pinned by id (hamukuri, Bird). `.bas` path typo (`.base`), `popo_bastable` holes at 1-4, model flags `0x10020000`. Real bugs: Kumokun damage radius 65, MapMirror `C_MTXLightPerspective` factors 0.5/-0.5, NpcBase `MsIsInSight` cone 120/800, MapObjMonte `GXSetChanMatColor {0,0,100,255}`, `MActor::setLightData`'s ground-height guard (`200.0f`), `thpInit`'s map guard and half-second spin (30.8 -> 100). Ignore rogue-prefix noise (`@137`, `dummyMactorStringValue1`, `SMS_NO_MEMORY_MESSAGE`, six leading `1.0f`) — diff per named object. `scShineTable*` in `StageUtil.hpp` lays out differently from retail in every includer but is cosmetic (extras).

## Structural batch 132 (2026-09-18): `.text` false relocations, silent ctor/dtor bindings, dead helpers in the pool

- **`block_relocations` also takes `target:`, and for `.text` it is the right field.** dtk's schema is `source`/`target`/`end` ("Blocked relocation must specify either source or target"). A `source:` range is wrong for code: a 16-bit relocation's recorded address is the instruction address + 2, an HA/LO pair can be hundreds of bytes apart (one `lis r27, 0x8001` serves eight `addi`s in `setMSoundEnterStage`), and legitimate `R_PPC_EMB_SDA21` relocations sit between them. One `target: 0x80010000` / `end: 0x80010031` entry blocks every `MSD_BGM_*` site at once (21 relocations in SelectMenu, CardLoad, MarDirectorDirect, MSoundMainSide; `startOpenWindow` 99.93 -> 100, +588 code bytes) and covers sites found later.
- **The `.text` false-relocation class is enumerable in one sweep**: resolve every `.text` relocation in every extracted target object (`objdump -r`) against `symbols.txt` and keep the ones with a nonzero addend; a `lis`/`addi` pair computing "function + offset" is never real code. This image has exactly 21, all BGM ids, because 0x80010001..0x80010030 falls inside `TBeeHive::doWait`. A nonzero addend in `.sbss`/`.sdata`/`.sdata2` is ordinary array-element addressing (133 of them) and must be left alone. `MSD_SE_*` ids are 16-bit (`li`), so they cannot be hit.
- **Only 4-aligned constants get relocated**, so "some operands relocated, some not" within one basic block that materialises several ids from a shared base is the false-relocation signature, not a code difference.
- **Iterate a `block_relocations` entry against a scratch split**: `dtk dol split --no-update <cfg> <tmpdir>` takes ~1.6 s and `diff -rq` against `build/GMSE01/obj` names exactly which objects the entry touched (here 4, `splits.txt`/`symbols.txt` unchanged), which is the real proof that the block is narrow enough.
- **A weak/global BINDING error on a ctor or dtor is a five-line fix with zero objdiff signal** (generalising batches 94/118): `TFishoidManager::~TFishoidManager`, `TMap::~TMap` (0x74, it destroys members) and `TRopePoint::TRopePoint` (4 bytes, non-trivial only because `TVec3<f32>` has a ctor) were all 100% at the map size before and after; only the linkage was wrong. Delete the declaration and the body so the implicit member is emitted weak. `changes_all` stays empty, so `validate-symbol-order` is the only detector, and removing fishoid's spurious dtor also cleared a weak *ordering* warning: a spurious global ctor/dtor perturbs weak flush order too.
- **Sweep of all 732 units: 43 BINDING errors**, 3 fixed above. Remaining: 17 header-inline bodies we compile out-of-line (weak in map, global in object: cameragc `calcSlopeAngleX_`/`ctrlGameCamera_`, sunmodel `calcDispRatioAndScreenPos_`, PPCArch `PPCHalt`/`PPCMtdec`, odenotstub `Hu_IsStub`, coasterkiller `setNormalFlyAnm`, PauseMenu2 `appearWindow`/`disappearWindow`, J3DClusterLoader 3x `JSUConvertOffsetToPtr<>`, BathWaterManager `calcCoord`/`makeHeightMap`/`makeNormalMap`, e_asin `fabs`, hyperbolicsf `fabsf`); 15 in linked `System/MarNameRefGen` (retail defined `load`/`loadAfter`/`searchF` out of line per `TNameRefAryT`/`TNameRefPtrAryT` instantiation, we have them in-class; the DOL is exact, so leave it); dropped `static` in `OS` (`OSDefaultExceptionHandler`), `printf` (`__FileWrite`/`__StringWrite`), `msghndlr` (`TRKMessageIntoReply`); missing `static` in `mem_TRK` (`TRK_fill_mem`), `msgbuf` (`TRKSetBufferUsed`); template-vs-file-static in J3DAnimation (2x `J3DGetKeyFrameInterpolation<>`) and J3DMaterial (`J3DGDWrite_u32`).
- **An UNUSED stub's literal ids say whether it was inlined or dead, and the two have different remedies.** MSoundScene's three `calcPos*` helpers own the lowest `.sdata2` ids (`@2051` 0.0f, `@2053` +pi, four dead-stripped slots, two dead doubles) while retail's `frameLoop` is byte-for-byte the same length as ours, so they were code-generated but never expanded: dead helpers can own pool slots and reorder the pool without contributing an instruction, and the `frameLoop` frame gap (0xc0 vs 0x90) cannot be explained by them. Our `frameLoop` requests -pi first exactly as retail's does; the +pi/-pi swap is entirely the dead bodies, whose literal values are unrecoverable, so the unit cannot be linked from source until they are reconstructed some other way. Corollary to batch 72: when a unit's pool order is wrong but every function matches, ask which dead helper requested the lowest id first.

## Structural batch 134 (2026-09-18): both link blockers solved, bindings 40 -> 11

- **Weak emission order within a class follows declaration order, and only a virtual *override* is a free lever.** A newly introduced virtual's slot is pinned; an override's slot belongs to the base, so its declaration can move. PollutionManager (100/100, DOL ca608a9a): section tables identical, `cmp -l` gave 84 bytes of `.text` (`getTexResource` 0x10 + `newJointObj` 0x40 + `getJ3DModelDataFlag` 0x8) plus three slots in every `TPollutionLayer`-derived vtable at stride 0x5c. Map order is `getTexResource, newJointObj, getJ3DModelDataFlag`; ours was declaration order. Moving the two overrides below `getTexResource` in `PollutionLayer.hpp` linked it. Generalises WoodBarrel's entry from "an override can move" to "only an override can move".
- **A template body that retail kept in a `.tpp`.** A weak symbol our object emits as an *extra* (objdiff is blind to extras) breaks the link when it wins the duplicate race: CubeManagerBase's `.data` was 0x9c against the map's 0x90 because `TNameRefPtrAryT<T,U>::load`'s local `JSUMemoryInputStream` dragged in the `JSUIosBase` dtor chain and `__vt__10JSUIosBase`, which the map's 29-entry duplicate list never attributes to that TU; resolved from our object it displaced every later `.data` object. Moving the bodies out of class in the *same* header changes nothing (MWCC still instantiates eagerly for any vtable the TU emits). Fix: `include/Strategic/NameRefPtrAry.tpp` and `NameRefAry.tpp` hold `load`/`loadAfter`/`searchF`, included only by `MarNameRefGen.cpp` (which explicitly instantiates the five the map has global) and `enemytable.cpp` (its `TStageEnemyInfo` triple is weak there). Object lands on the map's exact `.text 0xae4`/`.data 0x90`, links, six Camera units stop emitting a spurious weak `searchF`, and all 15 MarNameRefGen BINDING errors clear. The map says which instantiations were explicit: weak = implicit, global = `template class X<T>;`.
- **Correction to the round-16 "zeroed dead space" claim: dropped weak duplicates cost nothing in the output.** CubeManagerBase carried 816 bytes of dropped `.text` duplicates with the section size unchanged, and removing spurious weak `searchF` copies from two linked Camera objects left the DOL exact. Only a duplicate that *wins* moves bytes; account for dropped duplicates in the input size but expect no output effect.
- **How MWCC yields each map binding**: `(func,local)` on a mangled template name = a `static` function template (`J3DGetKeyFrameInterpolation`); `(func,local)` on a header helper only one TU out-lines = `static inline` in the header (`J3DGDWrite_u32`); `(func,weak)` in a C file whose body nothing in the TU calls = `__declspec(weak)` (plain `inline` is dropped under every `-inline` setting; `Hu_IsStub`, `fabs__Fd`, `fabsf__Ff`, the idiom already in `trigf.c`/`inverse_trig.c`); `(func,weak)` on an `asm` function = `inline asm` (warns "cannot be inlined", emits one weak copy; `PPCHalt`, `PPCMtdec`). Dropped/missing `static` fixed in OS.c, printf.c, msghndlr.c, mem_TRK.c, msgbuf.c; `TCoasterEnemy::setNormalFlyAnm` moved in class.
- **`__declspec(weak)` reproduces weak-plus-out-of-line for a ~1 KB C++ member where `inline` cannot** (measured on PauseMenu2: weak, map size, callers still `bl`, symbol order PASS) and is *rejected* as a crutch for cameragc (2), sunmodel (1), PauseMenu2 (2), BathWaterManager (3): their real lever is the caller's inline depth (an `inline` body expands without limit at depth 1 and is refused deeper, so retail's callers sit one inlined level deeper than ours). J3DClusterLoader's three `JSUConvertOffsetToPtr<>` are global because the TU is a stub of explicit instantiations; retail got them weak by implicit instantiation from `J3DClusterLoader_v15::readCluster` (six UNUSED bodies listed in the file).
- **Pool-order link blocker signature**: section tables identical and a handful of single differing bytes that are all `lfs` displacement low halves. MarioGamePad (DOL e4e0508c): retail `1.0f, 0.0f` at `@1806/@1807` and `0.25f, 0.5f` at `@2115/@2116`; the ~300-id gap says the first pair came from a header inline and the second from the `.cpp`, the inverse of ours (the fabricated `resetMeaning()` inline owns 0.25/0.5). Also a second int-to-float magic double the map has UNUSED. Earlier ruling: must not be linked while `updateMeaning` carries `u32 stackAlloc[83]`. Superseded in "Links 279": linked with the padding declared as an open debt, since the padding violates the rules whether or not the unit is linked and the DOL check guards any later replacement of it.
- Open after this batch: MarNameRefGen now reports ORDER errors between the explicit instantiations and the map positions (DOL exact, so cosmetic to the link; check whether pre-existing).

## Header round 22 (2026-09-18): explicit instantiation order

- **An explicit `template class` instantiation emits its members in reverse *declaration* order; definition order in a `.tpp` is inert.** MarNameRefGen's `validate-symbol-order` went from 3 MISSING + ORDER to PASS with 18/18 UNUSED sizes by (a) ordering the `template class` directives to the map's reversed `.text` layout, (b) reordering `TViewObjPtrListT`'s member declarations in `JDRViewObjPtrList.hpp` to `load, loadSuper, loadAfter, loadAfterSuper, searchF, perform` (safe: only the two new virtuals' relative order matters), (c) adding `template class TNameRefPtrAryT<TStageEventInfo>;` whose members are UNUSED in the map. All byte-neutral.

## Closure batch 139 (2026-09-18): alignment attribute

- **A 100/100 unit that breaks the DOL with a *shorter* section in `objdump -h` is a missing alignment attribute**: MapObjGrass's `color_table` (`.rodata`, `align:32` in symbols.txt, feeds `GXSetArray`) needs `__attribute__((aligned(32)))`; the hole before it is invisible to objdiff. Check symbols.txt's `align:` for every data object in a unit that will not link.

## Targeted batch 145 (2026-09-18): sunmgr linked

- **`sunmgr` linked (DOL exact, 476 -> 477 objects).** The blocker was the `.rodata` order, and only the order: retail's block opens with `System/DummyStrings.hpp`'s twelve zero bytes and 20-byte Shift-JIS message, then `Camera/SunModel.hpp`'s `"/scene/sun"`/`"/scene/sunset"`, then the unit's own three strings; ours had the pair third and fourth because the two `static const char*`s were spelled out at the top of the `.cpp`, i.e. *after* every include. Replacing them with `#include <System/DummyStrings.hpp>` above `#include <Camera/SunMgr.hpp>` makes the section byte-identical and the DOL exact. Generalises batch 97: the rogue-prefix include-order rule applies to *any* header that also defines string literals, not only to `InfectiousStrings.hpp`/`MapCollisionEntry.hpp`.
- **Most of sunmgr's section bloat was a red herring, and `objdump -h` cannot tell you which part.** Ours is `.text` 0xd88 against 0x7fc, `.data` 0x8c against 0x40, `.sdata` 0x18 against 0x08 and `.sbss` 0xc7 against 0x08, and all of it links: the extra `.text`/`.data` are weak duplicates (`JDrama::TViewObj::~TViewObj`, fifteen `JSUList<...>::~JSUList`, `__vt__Q26JDrama8TViewObj`) that the map itself lists as `>>>` under `Camera.a sunmgr.cpp` and drops, and the extra `.sdata`/`.sbss` are unreferenced file-scope pointers and `JALList<T>::smList` instantiations that the linker dead-strips individually. Two linked units (`Map/MapEventDolpic`, `Animal/AnimalManager`) already carry the same 12 bytes of dead `.sdata` with an empty `.sdata` in the target. **String literals are not strippable** — the map keeps a TU's `.rodata` as one `...rodata.0` lump — which is exactly why the order is the only thing that matters there.
- **Discount dtk's gap symbols before reading a section table.** sunmgr's `.bss` 0xb8/0xb4, `.sdata2` 0x18/0x14, `.sdata` 0x08/0x04 and `.sbss` 0x08/0x04 differences are entirely `gap_NN_<addr>_<section>` filler in the extracted target; the real contributions were already equal, and `.sdata2` was byte-identical. Only `.rodata` 0x68/0x67 was real, and even there the byte is section padding.

## Batch 145 (2026-09-18): sunmgr

- **String literals are the one unstrippable part of a unit's data, so `.rodata` *order* blocks a link that section sizes cannot explain**: sunmgr's target block opens with `System/DummyStrings.hpp`'s 12 zero bytes + Shift-JIS message and then `SunModel.hpp`'s `"/scene/sun"`/`"/scene/sunset"`; ours had the pair third because two `static const char*`s were spelled in the `.cpp` after every include. `#include <System/DummyStrings.hpp>` above `SunMgr.hpp` made `.rodata` byte-identical and the unit links (477). The other section-size gaps were red herrings: dtk `gap_NN_*` fillers in `.bss`/`.sdata`/`.sbss`/`.sdata2`, weak duplicates the map lists as `>>>` and drops, and dead-strippable `.sdata`/`.sbss` that two linked units also carry.

## Data pass 197 (2026-09-18): matched data is section-granular

- **`matched_data` counts a section's bytes only when that section is 100% exact, otherwise zero.** One wrong byte in a 4824-byte `.data` cost ModelWaterManager 72.8 points of data (two mis-transcribed bytes at 0xC10 of the pasted sphere display list: retail `80 01`). Triage a data batch by listing each unit's sub-100 sections from `build/GMSE01/report.json`, not by its data%: a unit at 27% is often one byte away. Five units to 100% data (ModelWaterManager 27 -> 100, MSoundSE 82 -> 100, Item 88 -> 100 with +4 exact functions, MtxUtil 55 -> 100, hinokuri2 99.6 -> 100).
- **A float literal in `.sdata` rather than `.sdata2` was bound to a `const T&` parameter**, i.e. `std::min`/`std::max`; the `@NNNN` ids come in pairs with one id skipped (`@3326`/`@3328`). A TU-local clamp template puts them in `.sdata2` (hinokuri2, confirmed against bosspakkun/fruitsboat).
- **A jump table (`.obj "@NNNN"` whose body is `.rel <fn>, .L_...`) is never a data fix**: it follows its function. Eleven units' only data defect is that (CameraChange, SelectMenu, ModelGate, Tongue, JPAEmitter, MarDirectorDirect, Guide, Talk2D2, Yoshi, hx_wiper, MapObjMamma); recheck them only after the owning function matches.
- A string that pools with an existing one is a wrong string (`TShine::makeMActors` compared against the 100-coin shine name; retail's `@3944` is the Mani-shop shine name). `MSoundSE::construct`'s nine kamome entries walk ids 0x3813-0x381B and the three `_ENSB` ones have radius `180.0f`. MtxUtil's `.rodata` opens with `dummyMactorStringValue1`'s 12-byte zero string (`@1490`).
- Header items: `JDRCamera.hpp` `TOrthoProj` needs a name parameter (`MarDirectorSetupObjects`' `@3755` is the blur-camera name passed to `TViewObj(const char*)` where five other TUs carry `"<TOrthoProj>"`; measured `.rodata` 98.8 -> 100, unit data 16.8 -> ~89.7); `DummyStrings.hpp` must split in two (retail's ids are 620 apart in MarDirectorSetupObjects and MtxUtil carries only the first string; a TU-local copy is parked in MtxUtil.cpp); `J2DPrint` takes the two-colour pair by value as one 8-byte object (Application `@2485`).
- Parked: DrawUtil's four run-time-subtracted literals (MWCC folds them through inlined parameters too); limitkoopa's `.bss` has 11 `__register_global_object` records against our 9 with 7 vs 9 `theNerve` statics (structural); bossManta lacks and igaiga misplaces the same `TVec3` zero/(1,1,1) pair (one emitter, two units); ShadowUtil's local-class `$NNNN` ids are all offset by 1265 (unfixable per unit, objdiff matches by name); tobiPuku's missing `0.5f` is an unreconstructed block in `TNerveTobiPukuLand::execute`.

## Data pass II and header round 36 (2026-09-18)

- **A `.data` pointer table whose bytes match but whose relocations all point 24 bytes low is a missing rogue include, not a wrong table.** `Map/MapCollisionEntry.hpp`'s zero + (1,1,1) `TVec3` pair (`@2685`/`@2687` in WaterGun) is emitted between the last header's named statics and the `.cpp`'s first strings; without it every later `.rodata` object shifts by 0x18 and the owning `.data` table scores ~76% with identical content (WaterGun data 13 -> 100, `TWaterGun::TWaterGun` exact). Diagnose with `objdump -r` on the `.data` object against the target's `.rodata` offsets. The same generator is the parked bossManta/igaiga zero/(1,1,1) pair: it must sit after `InfectiousStrings.hpp`.
- **A jump table is a data fix after all when its owning function is one flag accessor away**: in a small `.data` the table is the section (MarDirectorEvent 52 of 56 bytes). Retail re-reading a `u16` flag between the test and the `|=` (`lhz`/`rlwinm.`/`bne`/`lhz`/`ori`/`sth`) is the const-accessor tell: `checkUnk4CFlag()`/`onUnk4CFlag()` took `fireStreamingMovie` 72.8 -> 99.9 and the unit's data 54 -> 100.
- A name table one entry short of the map is NULL-terminated, and `local` in the dtk asm means `static` (`tstatestr`, bgtentacle; `jntidx`'s third joint is 28). A missing `.sdata2` literal inside a dead expression is not a data defect: MWCC strips it when the local it feeds is never consumed (Koopa `@6101`/`@6102`, `neckFocus`; the `KoopaNeckCallBack` tail is unreconstructed). `enemyMario`'s water-hit `startSoundSet` radius is `30.0f`, not `0.0f`.
- **`System/DummyStrings.hpp` is two headers**: 186 retail TUs open `.rodata` with the 12-byte zero object, three of them (MtxUtil, LightUtil, MarDirector) with no memory-message string, and where both appear the ids are 620 apart. `System/DummyMactorString.hpp` now holds `dummyMactorStringValue1` and `DummyStrings.hpp` includes it (zero-risk direction, no includer changes). LightUtil wants the new include somewhere after the top of its list (at the top it lands `@1490` but drops `className$1930`/`$1944`, -11.5 code); `MarDirector.cpp`'s hand-rolled `dummy(Vec*)` carrier at line 9 is the same object (linked unit, do not move without a DOL check).
- **An N-byte POD copied through two stack temporaries before landing in the object is a named local plus one by-value parameter level** (pool -> local, local -> parameter temp, temp -> members, each a `lwz`/`stw` pair). `J2DPrint::setEscapeColors(TColorPair)` by value with a named `colors` local reproduces `TApplication::drawDVDErr`'s three-copy sequence and takes Application's data 95 -> 100; two compound literals of the same 4-byte constant give two pool objects where one aggregate gives one 8-byte object. `TOrthoProj` takes a name (`const char* name = "<TOrthoProj>"`, 7th defaulted argument; MarDirectorSetupObjects passes the blur-camera name, data 17 -> 90).
- `getMaxHitPoints()` returning `int` is refuted tree-wide (matched_code -0.01, `TFireWanwan::receiveMessage` off 100; the `FireWanwanHitPoints` binder is load-bearing, frames 0x40 on both sides). Retail's `addi r3, r3, 0x6c; lbz r3, 0x10(r3)` is a reference-typed local bound to `getSaveParam()->mSLHitPointMax`, but spelling it adds a statement that breaks the linked effectEnemy (`TEffectEnemy::perform` 100 -> 95.6): the open lead is a form that binds the address without adding a statement.

## Data pass III (2026-09-18): include position, one-helper templates, shared switch slots

- **A rogue include can belong mid-file.** igaiga's zero/(1,1,1) pair sits at `.rodata` 0x1B0 between `"igaiga_model1.bmd"` and `"bosspaku_head_move"`; moving `#include <Map/MapCollisionManager.hpp>` out of the top block to just before `TIgaiga::perform` lands it exactly (data 14.9 -> 100 with a one-space string fix). This is the mechanism behind batch 93's parked "igaiga/koopajr exception": include position, not a call site. bossManta needed `Map/MapCollisionEntry.hpp` after `InfectiousStrings.hpp` (data 41.9 -> 100).
- **Two identical `const T[]` locals in one function are one inlined helper**: bossManta's `collidedWithWater` copies one `@3022` template into two stack temps, and the template's id sits just below `getTailAnimSpeed`'s blend table, i.e. a TU-local `static inline` declared just above it and expanded twice. The helper takes the actor by pointer, not the index by value (retail loads `mGeneration` after the array copy). Structurally exact now, +0x10 frame and one un-CSE'd reload left (fuzzy 99.8 -> 99.4, accepted for the data).
- **Sibling block-scope locals do not share a slot; one function-scope local does**: `TMario::checkGraffito`'s three `switch` arms all use retail's 0x48; hoisting one `TVec3 pos` above the `switch` gave every instruction and the shared slot (98.3 -> 99.8, MarioMove data 73 -> 100). LightUtil's hand-rolled `dummy1` array is `System/DummyMactorString.hpp` (its `@1490` now matched).
- **CardManager `setCheckSum` is a depth split**: `cmdLoop` reaches it through an inlined `readOptionBlock_`, so `CalcCheckSum` sits at depth 3 (budget 6) there and at depth 2 (budget 9) in the other four callers; retail calls it in `cmdLoop` and expands it elsewhere, so our `CalcCheckSum` body costs more than 9 statements. The `mWriteCount = wc; mCheckSum = CalcCheckSum(this, 0x1FFC);` spelling takes data to 100 but costs `writeOptionBlock_` 100 -> 84 and three others 10-15: not taken; the way in is a cheaper `CalcCheckSum` body that stays byte-exact (`ptr[i]` for `++ptr` breaks it).
- Remaining jump-table-only data units: bossgesso (`changeAttackMode` 98.1, frame 0x18 short plus the emergent `isThing` range), GCConsole2 (`perform` 14.7 KB at 88%, a rebuild), CardSave (`execMovement_` 13 KB at 98.5%, frame 0x88 short), CardLoad (two `perform` tables), CardManager (above).

## Header round 46 (2026-09-19): the `__sinit` node census, key-function vtables

- **A `__sinit`'s `JALList` block base is a node census**: `base / 0xC` is the number of `theNerve()`-style local statics with destructors in the TU, dead-stripped ones included, and the unreferenced node offsets name which are dead. `__sinit_limitkoopa_cpp` registered at +0x84 against our +0x6C: 11 nodes vs 9, i.e. two dead nerves (`TNerveLimitKoopaTurnR`/`TurnL`, `marioUS.MAP:93738-93741`) missing from the header, not an undersized MSound static as tell sweep 272 guessed. Node order is the header's class declaration order, independent of the `.data` vtable order (reverse definition order of the `execute` bodies). `__sinit` now exact; limitkoopa data 90.61 -> 100.00.
- **MWCC 1.2.5 applies a key-function rule to weak vtables**: a class whose virtuals are all declared and never defined in the TU emits no `__vt__`/`__dt__`, only the local statics of its inline members. Dead nerve classes can therefore be restored at zero `.data` cost by declaring `execute` without defining it. Only three `__sinit_*` remain non-exact tree-wide: `MSoundSE` (frame 0x38 vs 0x10) and `ConsoleStr` (volatile rotation) are other classes.
- `TSelectShineManager` is 0x120 (`TSelectDir::rsetup` `new`), every member to 0x108 pinned by the ctor and nothing reads past it: an uninitialised 0x18 tail like header round 45's `TMenuDirector`, added as `unk108[0x18]` with a TODO.
- Measured, not resolved: `TTalk2D2::checkBoardControler` has +0x3c dead low region and -4 above the point pool, its three `JUTPoint` argument temporaries are temps in both builds (right-to-left, rightmost lowest), so naming them is not the lever; `TCoasterKillerManager::loadAfter`/`load` are instruction-exact with +0x18 (one compiled-out assert) and +0x40 (two, with 0xc injected between two adjacent binder temps): not a fixed per-site cost, and no assert macro in the tree reserves anything.

## Links 278 (2026-09-19): objects.json deduplicated, MarioGamePad blocked

- `objects.json` listed `PowerPC_EABI_Support/Runtime/NMWException.cp` and `ExceptionPPC.cp` twice each, so the "493 linked" count was really 491; the list is deduplicated and the honest count is 491/732. Note the runtime's `.cp` extension: a census that strips only `.cpp`/`.c` reports these units as unlinked.
- `System/MarioGamePad` is source-linked (491 -> 492). The blocker was never the UNUSED body sizes nor the object-only symbol: UNUSED bodies are dead-stripped, so their size does not reach the DOL, and `CLBCalcRatio<s>__Fsss` is in the map as an `UNREFERENCED DUPLICATE` contributed by `MarioGamePad.cpp` itself (`marioUS.MAP:16299-16301`), i.e. the extra weak copy is correct.
- **The whole 10-byte DOL delta was `.sdata2` pool order**, four floats and the five `lfs` displacement bytes that follow them: retail is 1.0f, 0.0f, 0.25f, 0.5f (@1806/@1807/@2115/@2116) and ours was 0.25f, 0.5f, 0.0f, 1.0f.
- **`@NNNN` pool ids are handed out in code-generation order, which under `-inline deferred` is reverse definition order.** A value's slot is fixed by the *first body generated* that requests it, so a literal can be moved ahead of another only by putting it in a function defined *later* in the `.cpp`. Here 1.0f/0.0f had to be requested before `updateMeaning` expanded `resetMeaning`'s 0.25f/0.5f, and the only body between them is the UNUSED `considerMarioStick`, defined just after `updateMeaning`. Giving it the neutral-key stick-scaling block (`f32 stickScaling = 1.0f; ... = 0.0f; ... CLBCalcRatio<s16>(0x28, 0x3c, elapsed)`) put the pool in retail's order and the DOL went byte-exact. `CLBCalcRatio<s>` being the map's duplicate confirms the `s16` instantiation, so the helper's argument type is evidence, not a guess.
- Corollary for any unit with a pool-order gap: **an UNUSED body is a pool lever.** Conversely, adding a float literal to an UNUSED body that is generated early (`keepRumble`/`rumble` here, the last two in the file) would steal a slot ahead of the real ones and break a working link.
- Template-body literals are *not* pooled at parse time: reordering `0.0f`/`1.0f` inside `CLBCalcRatio` in `cameralib.hpp` left MarioGamePad's ids untouched. Only the instantiating body's generation order counts.
- Still open in that unit: `updateMeaning`'s `u32 stackAlloc[83]` is fabricated padding and the linked DOL now depends on it, and `considerMarioStick` is 0x94 against the map's 0x90 (one instruction over; the flat two-`if`, nested-`if` and local-counter shapes all measure 0x94).

## Data pass 284 (2026-09-19, Sonnet trial): koopajr `.data` closed, the rest code-blocked

- `koopajr_bastable` has a `nullptr` hole at index 2 (relocations `@2967, @2968, <none>, @2969`), the sparse pattern of `popo_bastable`; `.data` 100, unit data 14.90 -> 48.28. Residue: two 24-byte `entry$NNNN` objects in `.rodata` carry a non-relocated word `0x54220000` at +4 near the particle path strings, not guessed.
- Confirmed code-blocked (jump tables owned by a non-matching function): Yoshi `movement`, JPAEmitter `createParticle`, MarDirectorDirect `moveStage`/`nextStateInitialize`, CameraChange `changeCamModeSub_`, MarDirectorSetupObjects `setupObjects`/`decideMarioPosIdx`; tobiPuku's `.sdata2` order and a spurious `100.0f` come from the unreconstructed `TNerveTobiPukuLand::execute`.
- **New lead, not a data fix**: `MoveBG/ModelGate`, `MoveBG/MapObjMamma` and `Player/Tongue` are not jump-table residue; their vtables point at base-class slots (`TActor::getType`, `THitActor::receiveMessage`/`perform`, `TTakeActor::ensureTakeSituation`/`moveRequest`/`getRadiusAtY`) where retail has class-specific overrides (`TModelGate::receiveMessage`, `getTakingMtx`). Missing virtual overrides: a closure item.
- Untriaged leads: bossgesso `@7580` (32 B), enemyMario `@4667` (112 B plus a 256 B gap), bgtentacle `@4439` (380 B gap), MarioDraw `marioAnimeFiles` (1592 B at 81.7%).

## Closure 288 (2026-09-19): the override lead refuted, the zero-filled weak-duplicate cap

- Data pass 284's "missing virtual overrides" in ModelGate, MapObjMamma and Tongue was a misreading: every `__vt__` in the three units is byte-exact and the base-class slots it flagged are retail's own. Their `.data` is blocked by one jump table each, owned by a non-matching function (ModelGate `perform` 93%, Tongue `movement` 82%).
- **A losing weak-duplicate vtable caps `.data` below 100 permanently**: MWLD keeps the space of an `UNREFERENCED DUPLICATE` and zero-fills it (ModelGate's 184-byte `gap_07_803D4070_data` at `.data+0x138` is our `__vt__10TTakeActor` copy, winner `fishoid.cpp`, map line 8649), and the map gives it no name, so objdiff scores a nameless gap against an "extra" symbol. Any TU emitting a losing weak vtable has this cap; it does not affect the DOL, so such a unit can still link. Compare header round 45's zeroed dead space note.
- `TModelGate::loadAfter` 97.72 -> 98.70 on two tells: **`u16 h = x / 2` on a u16 emits `clrlwi; srawi; addze`** (MWCC 1.2.5 does not treat a promoted u16 as non-negative), retail's single `extrwi rD,rS,15,16` needs `x >> 1`; and three `ResTIMG` field stores through `tex->mResources[i].f = v` reload `mResources` per field where retail reloads once per plane and folds the offset into a `stbu`, reproduced by a TU-local `setGateTexRes(ResTIMG*, u16, u16)` binder.

## UNUSED sweep 299 (2026-09-19): 18 bodies sized, the frame-lever reading narrowed

- Census: 597 UNUSED symbols with a wrong object size across 129 unlinked game units (`scratchpad/grpfg/unused_mismatch.txt`, `ranked.txt`). MapObjLib 32 -> 9: `getJointRotateX/Y/Z` (map 8 = `lha; blr`, so the getters return **`s16`**, our 0x2c was the s16->f32 conversion: **an accessor 0x24-0x2c over an 8-byte map entry has the wrong return type**), the `setJoint*` family (0x50/0x58, the `J3DTransformInfo& info = joint->getTransformInfo(); ...; joint->setTransformInfo(info);` shape), `rotateJointX/Y/Z` (0x74 only with a named `int` plus `+=`, confirming fold-tell sweep 295 on an isolated body), `makeRootMtxTrans`/`setRootMtxTrans`, `getMapModel`/`getMapMActor`/`getMapModelData` (through `getModel()` for the last 4 bytes), `getNormalVecFromOffset`/`XZ` and `sendMsgToAll` (siblings minus the position subtractions or the type test: **sibling calibration sizes a body first try**).
- **Restoring an UNUSED body moves no caller by itself**: across 20 bodies in two units no caller changed a byte. `stopAnim` (research 296) moved its callers because the caller source was rewritten to call it; the size was the confirmation. The rule is: a caller rewritten to call a helper whose UNUSED body then sizes exactly is honest and can cost the caller -8; the body alone is bookkeeping.
- Not sized: `getVerticalVecFromOffsetXZ` (0xb4 vs 0xb0), `TTelesa::resetBaseGround` (0x1c vs 0x18, `return unsetUnk165();` committed as closest), `TKukku::getWaterPowerY`, `TKoopa`/`TLimitKoopa::getAnmEnd` (0x2c vs 0x48 in both), `TBossEelEye::setBckAnm`; `TMapObjVibration` has an empty header and blocks four.

## Header round 50 (2026-09-19): parked accessors promoted, the actor-type named step

- Added with zero movement tree-wide: `TParamT<T>::set(const T&)` beside `get()` (ParamInst.hpp; `TMario::checkWet` stays 100 without its parked setter; the setter is +4 only on top of another inline level, 0 over a plain member chain (`TNozzleBase::emitCommon`) or -8 plus a copy (`TNerveBPStompReact`); only 11 `.value =` sites exist, population spent), `THitActor::getActorTypeID()` returning `int`, `TLiveActor::getLinearVelocity()` beside `getVelocity()`, and `TWaterGun::getNozzleSpeedY()`/`getNozzleSpeedZ()`/`getHoverAngle()` in the class's existing accessor block. Parked helpers removed from MarioMove, NpcAnm, MarioSpecial, poihana and WaterGun; every affected function stayed exact.
- **`getActorType()` returning `u32` is retail's**: flipping it to `int` is -0.14% tree-wide (six regressions, one gain), so the signed reader is a second accessor. **The actor-type rung is the named-local step, not the function level**: a header `{ return mActorType; }` drops `TBaseNPC::npcWetIn` 100 -> 99.89, `{ int t = mActorType; return t; }` holds 100 (house style in `Mario.hpp`, `SmallEnemy.hpp`, `BeeHive.hpp`). Tail sweep 310's "TU-local vs header" distinction was really this step. `launcher.cpp`'s `LauncherSenderType` stays parked (its note records a different price).
- `TGraphTracer::unkC` is the per-rail-node jump speed (`prevNode->mYaw * 0.01f`); a `getSpeed()` plus `getTracer()` at both `flagJump` sites is +0x18 of the needed 0x20 and every further rung fails, so the siblings are structural and no accessor was added.

## WaterGun ladder 326 (2026-09-19): a rogue file-static in a shared header permutes `.sdata2`

- **`include/System/StageUtil.hpp` hosts ConsoleStr.cpp's file-local `scShineTable*` arrays** (the map lists them only in `GC2D.a ConsoleStr.cpp`); every TU including it gets 0x5c bytes ahead of its float pool, 4-misaligning every `double` slot and remapping the literal ids. Dropping the include from WaterGun.cpp (declaring `bool SMS_isDivingMap();` locally, as GCConsole2.cpp and ModelWaterManager.cpp already do) made its `.sdata2` byte-identical to retail's through 0x58 and closed `TNozzleBase::movement`/`TNozzleDeform::movement`. **Shared-header need: move the tables into ConsoleStr.cpp; 19 TUs include the header.**
- With the pool aligned, **a literal's slot reads the product's association**: `256.0f * x * 150.0f` and `150.0f * x * 256.0f` emit identical instructions but request the constants in opposite order; the retail `.sdata2` offsets pick the spelling.
- Debt noted: WaterGun.cpp still carries two pre-existing `#pragma dont_inline on/off` blocks (around `TNozzleBase::TNozzleBase` and `TWaterGun::getEmitMtx`), which the rules ban.

## Header round 51 (2026-09-19): the shine tables are a GC2D header, one pragma replaced

- The map lists the `scShineTable*` arrays in five `GC2D.a` objects (ConsoleStr, PauseMenu2, CardLoad, SelectMenu, Guide) and nowhere else, so they are a GC2D-scoped header: moved from `System/StageUtil.hpp` to a new `include/GC2D/ShineTable.hpp` (with `scShineConvTable`, `scScenarioNameTable`, `scNormalStageTable`, `SMS_getNormalStage`/`getShineID`/`isGetShine`), included at the former position in those five TUs; `StageUtil.hpp` keeps only the six `SMS_is*`/`SMS_get*Stage` declarations. Tree-wide effect: `TShine::calc` 99.86 -> 99.95 and nothing else moved; the misalignment is mostly free because objdiff scores the extra symbols as harmless and only a TU whose float pool feeds `lfs` displacements in a near-matching function pays (WaterGun and Item were the only payers). The ten TUs that had worked around the header (WaterGun, GCConsole2, MarioInit, cameragc, CameraChange, ModelWaterManager, MarDirectorDirect) include it again with byte-identical objects. `dolphin/thp.h` has the same shape but no other object emits its statics (unreferenced `static const` arrays are dropped).
- **A `dont_inline` replacement: expanding `TVec3::set(a,b,c)` into three component stores is a byte-free +2 statements** and crosses the depth-1 budget so MWCC stops inlining (`TWaterGun::getEmitPosDirSpeed` stays exact at 0xc0 out of line, `emitCommon` back to 99.79; the pragma removed). Naming a pointer local is not free (-0.15%). Two pragmas remain in WaterGun.cpp: `TNozzleBase::TNozzleBase` (size-exact at 0x800, a 14-instruction prologue scheduling residue, removal drops `TWaterGun::TWaterGun` 100 -> 65 and unit data to 19) and `getEmitMtx` (exact at 0xf8 with about 11 statements against a 14 budget; the map attests the out-of-line symbol, so retail's body is 15+ statements in a spelling not yet found).

## Header round 52 (2026-09-20): the chest joint ids, two refutations

- **`TMario` 0x3C5 is `jnt_chest` and 0x3C6 `chn_chest`**: `initModel` in both MarioDraw and enemyMario stores `chn_chest` at 0x3C6 then `jnt_chest` at 0x3C5 (the `chn_*`-first ordering of the foot pairs does not hold for the chest). Declarations swapped in `Mario.hpp` and every use site re-pointed so no emitted offset changed except the two stores; tree-wide only `TMario::initModel` moved (fuzzy), no drops. Method: `awk` the retail `.s` for `addi r4, r30, <rodata off>` followed by `stb r3, <member off>` to read a whole `getIndex` block's member/string pairing at once.
- Refuted: `marioAnimeFiles`' flag words (identical to retail; applying the "correction" dropped the object 81.7 -> 81.1; the residue is `.rodata` string-pool order on `MarioDraw.cpp`); `TPathNode(THitActor*)`'s `unk4.set(0,0,0)` as the source of pakkun's `bl set<f>` (the site is at depth 3, and every level added deepens all `TPathNode` sites). The stale TODOs in pakkun.cpp and PathNode.hpp are corrected.

## Pragma sweep 360 (2026-09-20): 4 of 37 `dont_inline` blocks removed, the rest priced

- Method: strip every candidate file's pragmas in one build and read `changes_all` once; the callers that drop are each pragma's protected set (27 files, one rebuild). Three pragmas protected nothing (LightUtil `TLightDrawBuffer` ctor, J2DPrint `J2DPrint_print_alpha_va`, tamaNoko `landEffect`, stale since batch 106) and are gone. **Honest replacement #3**: a single scalar product named out (`f32 phase = mFlyBobPhase; phase *= 0.9f; mFlyBobPhase = phase;`) is a byte-free +2 (telesa `reduceFlyForce` 13 -> 15, both nerves stay exact). 33 blocks remain, each with its statement count and protected set recorded beside it in the source.
- **MWCC expands a direct self-recursive body one level** (`TGraphWeb::calcGraphDirection` pastes into `initGoalIndex` without the pragma): RULES' "recursion is a true inline refusal" is wrong for direct self-recursion. `return <constant>;` counts less than a statement (gatekeeper `isHeadHitActive` at 14 by the model does not tip with +1). Refuted: an explicitly qualified call (`this->T::f()`) does not suppress expansion; `-inline noauto` as a per-object flag stops MWCC expanding plain bodies retail does expand (RumbleMgr 100 -> 16.7), so it is not the pragma's replacement. `TPollutionLayer::appearItem` is the sharpest anomaly: an empty body, map `(func,global)` at 4 bytes, `bl`'d from `cleaned` in the same TU. Best open lead: bosseel `forceShedTears` (13 statements) reaches 15 with `bool toggle` and a `TBossEelEye*& tearEye = mEyes[i]` reference (retail's `lwzu` plus reload is a reference binding), callers exact and the body at 97.5, blocked only by frame (0x30 vs 0x58: the binding displaces the two pool forks); a reference-returning fork over `mEyes[i]` should close it.

## Pad sweep 361 (2026-09-20): 8 `trash[]` pads deleted, 4 linked-unit pads priced as debt

- Of 20 real pad declarations in 11 files, 8 are gone: four with honest carriers at unchanged bytes (`TBGCheckData::getActiveJumpPower`: a binder over `getActor()` at 1 of 2 sites +8; `JKRSolidHeap::state_register`: a binder over the public static `getUsedSize` +8, and the fabricated dead `void* r28` local went too; `J3DMtxCalcBasic::recursiveUpdate`/`recursiveCalc`: a `J3DNode::getCallBack()` binder at both `if` tests each, +8 per test site, one site leaves every slot 4 low; J3DJoint now fully matching), four removed for TODOs in unlinked units as the rules require (`MsMtxSetRotRPH`/`MsMtxSetTRS`/`MsIsInSight` 100 -> 99.8-99.9 in MathUtil; `TSplineRail` ctor and `getPosAndRot` in graph), an honest -0.03 of total matched code.
- **Unremovable debt (source-linked units; removal changes the DOL)**: JUTConsole x2 (`JUTReportConsole_f`/`JUTWarningConsole_f`, one 4-byte word missing between the inlined `char buf[256]` and the `va_list`; forks over `JUTGetReportConsole()`/`getOutput()` 0), JUTRomFont x2 (`drawChar_scale`: two 4-byte holes inside the named block are two missing declarations, not pool; no accessor rung reaches them), JKRDvdRipper x4 (the `JKRDvdFile*` overload reaches frame-exact with an `isErrorRetry()` binder at 4 sites but leaves the two block-scope `u8 buffer[0x40]` bases 4 high; the two forwarders want `JKRDvdFile file` 4 higher), J3DMaterialAnm `calc` (a `getMatColor(i)` or `getTexMtx(i)` binder lands the frame leaving `u16 texNo` 4 high). Not reached: J3DMaterial.cpp:791 (0x10), JASChGlobal.cpp:26 (0x10). **Recurring shape worth a rung: a binder that pays +4 top / +4 bottom where +8 of pure bottom pool is wanted** (JKRDvdRipper, J3DMaterialAnm).

## Pad research 363 (2026-09-20): five linked-unit pads removed with honest carriers, DOL intact

- JUTConsole x2: the missing pool word between the inlined `char buf[256]` and `va_list args` is a named `u32 output = JUTGetReportConsole()->getOutput();` in the else arm of the shared `JUTReportConsole_f_va` (the 4-byte rung of research 312 in expansion form: a named scalar holding an inlined accessor's result inside an inlined callee is +4 of pool above an earlier `char[N]`); `JUTWarningConsole_f_va`'s `OSReport("%s", buf)` in the null-console arm was fabricated (map 0x6c is the report body's size), now exact. JKRDvdRipper forwarders x2: naming the forwarded result (`void* result = loadToMainRAM(...); return result;`) is +4 of pool when the frame holds a class object with a destructor (the mirror of "naming a `new` result is +4 of named block"). J3DMaterialAnm `calc`: **new +8 rung, a TU-local fork that binds the intermediate block and returns the indexed accessor** (`J3DColorBlock* b = m->getColorBlock(); return b->getMatColor(i);`), where the plain one-local binder is +0xc and the direct-return fork 0.
- Debt kept: `JUTRomFont::drawChar_scale` (both holes are dead slots, the body never touches `r1` outside the prologue except the buffer addresses; register-allocated named locals get no slot, so the carrier must be an array, struct or address-taken scalar); `JKRDvdRipper::loadToMainRAM(JKRDvdFile*)` (retail has a 4-byte slotted object between its two `u8 buffer[0x40]` blocks; every pool rung moves both buffers together; `u8 buffer[0x44]` in both blocks would reproduce the layout but is unjustifiable). A split declaration (`T* p; ... p = e;`) reserves no slot in either unit (NpcEvent 353's effect does not generalise). Not reached: J3DMaterial.cpp:791, JASChGlobal.cpp:26.

## ShadowUtil ladder 368 (2026-09-20): the `$NNNN` id counter is seeded by the PCH

- **ShadowUtil's `.data` deficit (70.77) and 19.6% of its `.text` are one cause outside the TU**: the six local classes in `drawShadowGD` (`TCylinder`, `TSetup1..5`) are byte-exact (all six `makeDL` bodies compared word-for-word against the DOL with relocated fields masked), and only the `$NNNN` MWCC mangles into the local-class name differs (ours `$906/907/925/930/942/951`, retail `$2171/2172/2190/2195/2207/2216`, identical relative spacing). **The id counter is a single parse-order counter seeded by the precompiled header**: an empty TU already reaches id 191 from `include/SMS.pch`; retail is +1074 before the TU's first aggregate `@136`, +181 more between `@136` and `@156`, +11 more by `@165`, then in sync to `TCylinder`. A header costs ids only for declarations not already included (measured: `Player/Mario.hpp` +279, `JPAEmitter.hpp` +151, `MSound.hpp` +47, `J3DMaterialAnm.hpp` +46, `MapObjBase.hpp` +23, `LiveActor.hpp` +17, `MarDirector.hpp` +13). objdiff rescues local data symbols positionally but not a mangled function name, so ShadowUtil is the only unit whose score is hostage to `SMS.pch`'s size: worth 4116 bytes of `.text` and 96 of `.data`. A header round item.
- A by-value TU-local accessor returning a member `TVec3` converts a named caller local into an unnamed inline temporary at zero instruction cost (`TMBindShadowManager::forceRequest`, the inverse of the usual accessor rung); forks placed in later statements still allocate below an earlier by-value return temporary, so a frame that lands with the temporary in the wrong slot is a false positive (read the temporary's offset, not only `frame:`). Retail's 0x28 of dead pool above the temporary there is `align8(sizeof(TCircleShadowRequest))`, probably an unconsumed request object in an inlined callee. Unit unchanged at 13.85 / 70.77; 5 unpriced (`ladder_tried.txt`).
