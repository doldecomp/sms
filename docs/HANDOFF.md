# Orchestrator handoff

Written 2026-09-18 by the session that ran batches 92 to 181.
Read this before `docs/ORCHESTRATION.md`; it is the judgement that document does not carry.
Keep it current: every orchestrator appends a dated "State" entry below and rewrites the advice when it stops being true.

## Where things stand (2026-09-18, late night, session 110dc638)

| | Matched code | Linked units |
| --- | ---: | ---: |
| Game | 45.03% | 162 / 385 |
| JSystem | 89.93% | 179 / 198 |
| SDK | 95.71% | 146 / 149 |
| All | 54.86% | 489 / 732 |

Matched data is 95.2% (from 92.8%); eleven more units have 100% data than this morning.

`docs/TIMELINE.md` has the per-commit history and per-day rates; regenerate it with `python3 tools/progress-timeline.py`.
Per-batch token costs are in `docs/progress/agent-costs-2026-09-18.md`; the batch-by-batch outcome log is `docs/progress/queue-2026-09-18.md`; the census scripts are under `tools/census/`.

## What the numbers say about yield

Measured over 52 batches on 2026-09-18 (about 14.5M agent tokens, 57 links, roughly 75 functions to exact); the second session's 27 batches (5.6M tokens, 0 links, ~60 exact, eleven units to 100% data) confirmed the verdicts and added the data-pass row:

| Batch type | Typical cost | Typical result | Verdict |
| --- | ---: | --- | --- |
| Research (one residue class, scratch TU, driver script) | 190k-240k | one reusable rule, then 2-10 closures in the follow-up passes | **Best value.** Every rule found today unblocked units within hours. |
| Header round (one agent owns shared headers, `changes_all` per change) | 110k-260k | 1-7 exact, often a link | High value when items are verified and the lever-removal rule is applied (see below). |
| Relocation-target pass (`tools/check-relocs.py`) | 350k-390k | 8-11 exact, real gameplay bugs, data gains | High value; ~300 flagged sites remain, mostly in big units. |
| Structural-residue pass (opcode-marker census) | 270k-350k | 4 exact, a third of them real bugs | Good; census entries above 170 unworked. |
| Re-pass with new rules over one/two-function units | 220k-340k | 1-3 links | Good the first time after a new rule; zero the third time. |
| Library closure / re-pass | 220k-480k | 0-5 links | Good once (region constants, one-arg levers); the remaining 17 library units are research residues. |
| Closure batch on units worked twice before | 300k-570k | 0-2 links | **Stop.** Every remaining one/two-function game unit is diagnosed as a known-open class. |
| Sonnet on script-driven sweeps | 240k | 0 exact | Same tokens per call as Opus, less result. Opus only. |
| Data pass (sub-100 sections, `objdump -r`, `$NNNN` static sections, rogue-include position) | 170k-250k | 3-5 units to 100% data, 1-4 exact as a side effect | Best value per token while sub-100 units with non-jump-table defects remain (none left on 2026-09-18). |

Tokens per tool call are ~2,300 for every agent regardless of policy; savings come from agents reading and rereading less (rules card, `--clusters`), not from the model thinking less.

## How to run for the fastest results under a usage cap

1. **Spend on research first when a class blocks several units.** A research batch that finds a rule is the only thing that has ever moved the wall. Give it a scratch TU with the game flags, a driver script, and the exact list of blocked sites to test the rule on.
2. **Keep the detectors running.** `tools/check-relocs.py --mode all` and the opcode-marker census (`tools/census/census_struct.py`; `census_depth.py` beside it finds sqrtf/TVec3 depth sites) find bugs objdiff cannot score. Work them in slices ranked by unit bytes-left.
3. **Header rounds only with verified items and the lever-removal rule:** a function that drops from 100% after a header binding usually carries a removable TU-local lever of the same size; remove it in the same commit instead of rejecting. Receiver bindings are destructive; argument bindings pay.
4. **Verify a queue item against the tree before spawning it** (two stale items cost a batch on 2026-09-18). One `--clusters` call per item is enough.
5. **Never a third closure pass on a unit whose TODO names a known-open class.** The classes with no legitimate spelling: the `a = b - c` allocation order (retail's 4-below/12-above geometry needs two dead bindings), the JGadget iterator block stride (8 retail vs 12 ours), volatile-register block trades, dead low regions with no legal carrier (no UNUSED callee, no empty-ctor class local), the three factory constructors retail calls (`TTelesaSlot`, `TFence`, `TSunGlass`), the `JUTColor` temp stride, `this`-vs-pool-base swaps whose named-scalar knob is exhausted. They wait for research, not for another agent.
6. **Four Opus agents was the user's choice; the budget cannot sustain it for a week.** If the user asks to cut cost, drop closure and re-pass batches first, keep research and detector passes, and run two agents.
7. **Landing is one bash call per step**; do not narrate between them. Refresh `PROGRESS.md` with `tools/refresh-progress.py` and the timeline after every landing, and report the percentage table to the user after every completion (they asked for it).
8. **Spawn messages are short**: worktree, units, scratch dir, the two or three rules that apply, the link-test procedure with the `.cpp` spelling and the ninja exit-code check. The briefs carry the rest.

## Open leads worth a batch (verified 2026-09-18, late night)

- **Links one function away**: JALModSe `JALSystem::append` (instruction-exact; the 0x68 hole is 3 x 16 + 7 x 8 in the `JALPrmSet`/`JADPrmS<f32>` prologue constructions, unnamed: a research batch on what real C++ in those two constructors reserves it); J3DModel `entryModelData` 99.92 (zero-frame r25/r24 vs r20/r21 rotation).
- **Research seeds with a measured payoff**: the `a = b - c` allocation order (+9 functions, needs a legitimate spelling for retail's (4, 12) geometry); the JGadget stride (eight functions; the lever is in the callers' enclosing levels, not `std-list.hpp`); `getMaxHitPoints()` as a statement-free address binding of `getSaveParam()->mSLHitPointMax` (bosstelesa +3, fireWanwan must stay exact); the constant 4-byte pad at the top of `TSunModel::calcAnim_`'s block; the emergent per-expansion inlining class (`TSelectMenu::perform` vs `initData`, the sharpest same-TU datum); `JPAGetRMtxTVecElement`'s FPR ranking (research 174); the frame-only census (functions with zero opcode markers and stack-offset residues) as a new detector for the research-208 block rule.
- **Data**: the remaining sub-100 units are jump-table owners needing code (GCConsole2 `perform` 14.7 KB rebuild, CardSave `execMovement_`, bossgesso `changeAttackMode`, CardLoad's two `perform` tables, CardManager `setCheckSum` via a sub-9-statement byte-exact `CalcCheckSum`); Koopa's `.sdata2` needs the `KoopaNeckCallBack` tail; LightUtil wants `System/DummyMactorString.hpp` at a bisected include position.
- **Exhausted, do not re-run without a new rule**: closure passes over the one- and two-function game tiers (54 units, batches 206/207) and the eight near-link library units (batch 202); header items on return types measured today (`isHolding`, `getColor`, `getMaxHitPoints`, `operator*` V2/V3, `SMatrix34C` ctor, `TNerveBase` ctor, `TVec2` copy ctor); reloc sites (264 left, nearly all compensated pairs or inlining artifacts); structural census rows (all 594 worked once).
- Symbol-order FAILs on linked Runtime units (ExceptionPPC 26, NMWException 8 MISSING UNUSED bodies): correctness, not links.

## State log

### Session codex-orchestrator (2026-09-22)

- Fixed `tools/worktree.sh` to configure new worktrees with the Python recorded by the main build, preventing false shared-tool download attempts.
- Landed and source-linked `System/EmitterViewObj`.
  The blocker was `.sbss` definition order: moving `gpMarioParticleManager` ahead of the three callback globals restored its retail address and 663 downstream SDA relocations.
  The linked DOL is byte-identical; All linked **497 -> 498**, Game linked **170 -> 171**, matched code stays **63.54%**.
- Landed `MoveBG/MapObjFlag`: `TMapObjFlagManager::initDraw` is exact via a direct aggregate `GXColor` call.
  Unit matched code **66.55 -> 74.21%**; All **63.54 -> 63.55%**, Game **55.96 -> 55.97%**, exact functions **11,461 -> 11,462**.
- Landed `Enemy/rocket`: `TRocket::calcRootMatrix` now uses the model setter shape, raising the unit's fuzzy match **99.17 -> 99.22%** with zero regressions.
- Landed `Enemy/yunbo`: restored the second `TMActorKeeper` temporary in `TYumbo::init` and named dancing-nerve scalars.
  Unit fuzzy match rose **99.25 -> 99.39%**; the repository-wide two-decimal totals did not move.
- Landed `MoveBG/MapObjRicco`: naming the first fruit-launcher registration result raised `TFruitLauncher::loadAfter` **98.18 -> 99.52%** and the unit fuzzy match **99.84 -> 99.94%**.
- Trialed the local `deepseek-flash` profile on the whole `MoveBG/MapObjPlane` unit after several Terra successes.
  It produced no retained improvement after an extended variant search, so the worktree was discarded and native Terra agents remain the default.
- Medium-effort Terra whole-file trial: the first six fresh TUs produced three verified improvements and three clean blocker audits, with no repeated file assignments.
  Landed `Camera/CameraBGCheck` (`execWallCheck_`, `execRoofCheck_`), `Map/MapWire` (`drawLower`, `drawUpper`), and `Strategic/ObjHitCheck` (`entryGroup`, `checkActorsHit`, `clearHitNum`).
  `Map/MapCheck`, `GC2D/SelectShine2`, and `MarioUtil/MtxUtil` returned clean; the latter two plus `MapWire` independently point to shared `JGVec2`/`JGVec3` inline-shape blockers, so use a header research round rather than reassigning those TUs.
- Continued the medium trial: `System/CardManager` returned clean and remains blocked by the shared checksum inlining split; landed `Camera/CameraNotice`, raising `calcNoticeTargetYrot_` **92.24 -> 93.88%**.
- Closed the `JGVec2<float>::sub` header lead with an xhigh research round.
  The exact weak 0x24 body emits only when `sub` is non-inline, which forces calls broadly and regresses both `SelectShine2` callers; the missing weak body is caller/per-expansion behavior, not a shared-header declaration fix.
- Landed `Player/Tongue`: corrected Yoshi target filtering, raising `findTarget` **74.77 -> 87.99%**; `movement`'s eight-entry jump table still holds the unit data at **66.08%**.
- Landed `Enemy/Amenbo`: corrected the water-entry nerve guard to require `!isChangedBlock()`; this is a semantic fix with a small fuzzy-score change.
- Landed `NPC/NpcBase`: corrected balloon message IDs to retail `0x52/0x53/0x54`, raising `moveObject` **93.81 -> 99.96%** with all body instructions exact.
  The remaining major `perform` mismatch has a concrete header lead: `getAnmOffDist_` is currently in-class/inlined, but the map requires a weak 0x104 out-of-line body and the sole caller invokes it.

### Session cursor-orchestrator (2026-09-20 morning)

- Session so far: MarioSpecial, bosspakkun +3, igaiga +3, bossManta +0, gesso (+1 then +0 structural then +2), fireWanwan (+2 then +0 structural), hamukuri (+2 then +2 then +1), chuuhana (+0 then +0 structural), bosstelesa +1, MapObjLib +3, bossgesso +1, MapObjMonte (+3 then +0 structural x2 then drawOneRope frame), bosseel (stale then yield), WaterGun (stale), popo +1, hinokuri2 (+0 structural then UNUSED restore), EventWatcher +0 structural, MapObjMamma +7, MapObjBase +3, MapObjPinna +1, telesa +2, conductor +1, NpcEvent +0 structural, smallEnemy +1, BossHanachanParts +5, killer (+1 then +0 structural), MapObjBall +0 structural, Option (+1 then +1), elecNokonoko +3, hanasambo +2.
- Landed `grupp` hamukuri ([hamukuri unit ladder](8e496982-fd7d-4bd4-82c8-ba7b3537638c)): `THamuKuri::isHitValid` exact via inlined `HamukuriSerialKill` binder; `generateBody` frame exact (TNerveBase `bl` still open). All **61.92→61.93**, exact **11,371→11,372**.
- Landed `grpuo` MapObjMonte drawOneRope frame; Option +1 (`setType`).
- Total: **61.93%** matched, game **53.93%**, linked **492 / 732**, exact **11,372**.
- Landed `grpva` hanasambo ([hanasambo unit ladder](bc539a39-5ccd-4d57-8312-e2062df59367)): structural dropLeaf/loadAfter/genEventCoin/checkGenCoin (fuzzy 99.08→99.54); +0 exact. All stays **62.04%**. Do not third-pass dropLeaf +4 / loadAfter +0xc / genEventCoin +0xc / attackToMario +0x10 without new lever.
- Landed telesa +2; elecNokonoko Collect +1; Option +1; MapObjBall +2.
- Landed `grpve` MarioJump ([MarioJump](baa4f7ad-fb76-44db-af1e-c79646c35b1f)): +2 exact. All **62.04→62.10**.
- Landed `grpvf` MarioSound ([MarioSound](2a194a92-6f92-4dfe-a316-431a2b56ea93)): +2 exact; unit **97.38%**. All **62.10→62.31**.
- Landed `grpvg` MarioReceiveMsg ([MarioReceiveMsg](80cd0104-162c-4497-b87a-c2b2eda658a1)): +0; park 120-byte frame.
- Landed `grpvh` CardSave ([CardSave](632b7ca0-c6f0-44e4-96cf-e041b2152c1b)): +2 exact; data **100%**. All **62.31→62.44**.
- Landed `grpvi` CardLoad ([CardLoad](569a7982-2eb3-48d4-9866-073ac899bb92)): +1 exact. All **62.44→62.47**.
- Landed `grpvj` BathWaterManager ([BathWaterManager](e7c65049-d748-4c20-9cdc-cd58e046e5c4)): +0 structural.
- Landed `grpvk` MarNameRefGen ([MarNameRefGen](cd56fcce-fa2e-4304-bf6f-0c4b4cc5ef33)): +4 exact. All **62.47→62.53**.
- Landed `grpvl` Guide ([Guide](4a9a5bc9-f616-4afd-9696-29615c0987f7)): +0 structural.
- Landed `grpvm` ShadowUtil ([ShadowUtil](750a29a9-2a94-4e96-9ddc-f428d2c6f57a)): +0 structural.
- Landed `grpvn` WaterGun ([WaterGun](2f0fd56b-1730-4240-82a2-5a72bb758094)): +1 exact. Game **54.69→54.70**.
- Landed `grpvo` MarioSpecial ([MarioSpecial](29a33d37-f740-4864-84e4-4ef8b6f4db7d)): +2 exact (`barWait`, `barClimb`) via /512 + named holder-y. Unit **17.26→26.00**. All **62.53→62.59**, Game **54.70→54.76**, exact **11,395→11,397**. Park specMain/pulling/getOnWirePosAngle/wireMove.
- Landed `grpvp` MSoundSE ([MSoundSE](d4b0dd3d-d95d-4f7d-bd30-4ae7d3f831c1)): +1 exact (`getRandomID` via TU-local `msWeightAt`); construct wait/shift immediates restored. Unit **15.95→21.16**. All **62.59→62.60**, Game **54.76→54.78**, exact **11,397→11,398**. Park construct/sinit frames, randPlay JAIActor, MSRand* ctors, getRandVol FPR.
- Landed `grpvq` bgtentacle ([bgtentacle](26e6d697-0ea8-46d1-9760-2aab04d18b89)): +1 exact (`TNode::calcPosition`); structural calc/moveRequest frames. Unit **39.69→43.40**. All **62.60→62.63**, Game **54.78→54.81**, exact **11,398→11,399**. Park calc TVec3::sub, moveRequest FPR, ctor JGadget, calcAttackGuideAnm.
- Landed `grpvr` launcher ([launcher](a8aa1e4a-402a-4a91-b4c9-c3620df547f6)): +1 exact (`TCommonLauncher::init` via named `getChildren()` list). Unit **87.37→100%**; **source-linked** (objects.json). All **62.63→62.65**, Game **54.81→54.84**, linked **492→493**, exact **11,399→11,400**.
- Landed `grpvs` MarioRun ([MarioRun](e21bc9c0-3924-43d8-9d37-fac520f13311)): +1 exact (`doRunning` via min-cap + TParamRT get forks); slippingBasic multiply operand fix. Unit **38.19→41.97**. All **62.65→62.67**, Game **54.84→54.86**, exact **11,400→11,401**. Park walkEnd/slopeProcess/doSliding/slideProcess +4 slots, getSlideStickMult, slippingBasic 0x18, stamp arg order.
- Yielded `grpvt` MapObjTree ([MapObjTree](8399365c-fb1d-4504-819b-e84d9af5243b)): +0; park `initMapObj` dead-range coalescing (r25 vs r26) — no third pass without research.
- Landed `grpvw` MovieRumble ([MovieRumble](0a0362a4-9a46-4134-9a79-dc5b1041db6c)): +1 exact (`init` via unk14 fork at dataExists). Unit **64.37→100%**; **source-linked**. All **62.67→62.68**, Game **54.86→54.88**, linked **493→494**, exact **11,401→11,402**. Park checkRumbleOff dont_inline / UNUSED sizes.
- Landed `grpvu` PollutionCount ([PollutionCount](be405692-5ec1-4a4e-840e-f6cded1e2772)): +1 exact (`countTexDegree` via later-statement accessors). Unit **94.35→100%**; **source-linked**. All **62.68→62.69**, Game **54.88→54.90**, linked **494→495**, exact **11,402→11,403**. Park UNUSED draw* sizes.
- Yielded `grpvy` riccohook ([riccohook](a1bf0083-7583-4f48-85e7-7f09dfa8c5da)): +0; park `init` JGadget inter-group gap (0xd0 vs 0xd8) — same class as Seal/MirrorActor/SDLModel.
- Landed `grpvx` MirrorActor ([MirrorActor](4be99a7c-da1b-4385-aa8b-422320c0b405)): +0 (TODO docs only); park `init` JGadget between-group gap — do not third-pass with riccohook/SDLModel/seal.
- Yielded `grpwa` wireBinder ([wireBinder](3d231004-d02a-4b71-82e3-98401cbc95fa)): +0; park `bind` a=b-c slot (0x3c vs 0x24).
- Yielded `grpvz` walkerEnemy ([walkerEnemy](7c8aea17-167b-4e81-a7ba-a5d27c4ee815)): +0; park moveObject/escape 40-byte dead frames (no honest +0x28 carrier).
- Landed `grpwb` MActor ([MActor](284db032-b0d3-428b-97bc-357b2bcc54e5)): +1 exact (`setLightData` via getNormal binder). Unit **76.32→78.42**. All **62.69→62.70**, Game **54.90→54.91**, exact **11,403→11,404**. Park ctor/setModel JGadget slots, calcAnm +8.
- Yielded `grpwc` liveactor ([liveactor](3f9e2143-e517-4eb1-9d87-8c8870b24542)): +0; park bind a=b-c, calcRideMomentum FPR (shared MsAngleDiff), initAnmSound/init get_ufloat_1 slots.
- Yielded `grpwd` MarioCheckCol ([MarioCheckCol](a22deb74-106d-4b53-b0f6-1f37f9f719cc)): +0; park `hitNormal` volatile-register coalescing (r3 vs r7) — no third pass without research.
- Yielded `grpwe` MarDirectorLoadResource ([MarDirectorLoadResource](3ac22209-5891-4f94-8352-d1508289c7e0)): +0; park loadParticle/loadResource dead-low-region (+24/+64) — no third pass without research carrier.
- Landed `grpwf` MarioMove ([MarioMove](f2de1f53-b069-4b8b-9fb5-8c8f6de9453d)): +3 exact (`changePlayerJumping`/`changePlayerTriJump` via UNUSED `setMissJumping`; `thinkParams` via getGroundPlane). Unit **46.12→52.71**. All **62.70→62.76**, Game **54.91→54.98**, exact **11,404→11,407**. Park checkRideMovement/checkGraffito/playerControl 4-byte, thinkYoshiHeadCollision, checkSink +0x48, checkWallPlane, warpRequest a=b-c, moveRequest.
- Landed `grpwg` Yoshi ([Yoshi](e768809f-d02c-4832-96e3-49eb925083ca)): +2 exact (`getOff`, `calcAnim`); init structural. Unit **18.66→28.96**. All **62.76→62.81**, Game **54.98→55.04**, exact **11,407→11,409**. Park thinkHoldOut +0x48, doSearch a=b-c, init +0x20.
- Landed `grpwh` MapObjBianco ([MapObjBianco](a9ca0bad-b858-4e3d-a2fb-c957f265fc6a)): +1 exact (`TLeafBoat::control` via getUnk18 + one-site water-gun binder). Unit **60.43→63.57**. All **62.81→62.82**, Game **55.04→55.05**, exact **11,409→11,410**. Note: `bind` fuzzy 99.39→99.28 with retail frame 0xe8 (accepted; matched_code up, DOL green). Park loadAfter/touchWater/Vertical ctor/BigWindmill known-open, touchActor vel+4, bind split.
- Landed `grpwi` graph ([graph](bb135643-d1c2-4358-88fa-3561c01ba37d)): +1 exact (`getRandomNextIndex`); initGraphGroup frame 99.9%. Unit **45.01→47.72**. All **62.82→62.83**, Game **55.05→55.07**, exact **11,410→11,411**. Park DirLimited loops, getPosAndRot, ctor frame, traceSpline/startIsEnd/perform ranking.
- Landed `grpwj` MSound ([MSound](5b6e3d54-793a-4e17-ba49-84372c052e17)): +5 exact (`startBeeSe`, `talkModeIn`/`Out`, `pauseOff`, `loadWaveBackword`) via gated MSoundSE return-bind. Unit **50.73→65.04**. All **62.83→62.88**, Game **55.07→55.13**, exact **11,411→11,416**. Park startSoundActorSpecial/startMarioVoice/exitStage/setParameterSeqSync/ctor.
- Landed `grpwk` MapObjMare ([MapObjMare](c31442e8-a6a5-42e3-8ba5-259f0952e819)): +1 exact (`TMuddyBoat::control`); calc 99.5%. Unit **52.06→56.45**. All **62.88→62.90**, Game **55.13→55.16**, exact **11,416→11,417**. Park WireBell/Cogwheel initDraw frames, touchPlayer, moveByWater FPR, bind/initMapObj.
- Landed `grpwl` Item ([Item](0619f5d5-a2be-46eb-83de-da0bd7c26c38)): +2 exact (`TEggYoshi::receiveMessage`, `TShine::calc`); touchFruit 99.6%. Unit **77.23→84.30**. All **62.90→62.94**, Game **55.16→55.20**, exact **11,417→11,419**. Park NozzleBox load 4-byte, loadBeforeInit, decideRandomLoveFruit, control frame, touchFruit FPR.
- Landed `grpwm` namekuri ([namekuri](c0e40c6e-b3a3-4c47-aeb2-f31ae67ea542)): +2 exact (`stateLaunch`, `moveObject`); JumpAttack 99.87%. Unit **59.33→64.88**. All **62.94→62.96**, Game **55.20→55.23**, exact **11,419→11,421**. Park setDeadAnm/setMeltAnm slots, init +0x28, FollowMario, calcRootMatrix, NameIndParCallback.
- Yielded `grpwn` NpcEvent ([NpcEvent](3ca161a2-42df-4374-ba25-aa1be5893e16)): +0; park slice/frame 4-byte and fruit +8 conversion-temp class — no third pass without research carrier.
- Landed `grpwo` tinkoopa ([tinkoopa](8332a0ed-b61e-4f5e-b869-a0d0948d26bb)): +3 exact (`hitParts`, Flame `receiveMessage`/`perform`); emitFlameEffects 99.9%. Unit **53.88→60.74**. All **62.96→62.99**, Game **55.23→55.27**, exact **11,421→11,424**. Symbol-order UNUSED setEular MISSING accepted (DOL/`changes_all` green). Park emitFlameEffects FPR, initTinKoopaPartsBase JGadget, killer-message, loadAfter, break/reset else-if.
- Landed `grpwp` igaiga ([igaiga](2be04de5-14d6-4e5d-a8d1-c8458791118e)): +0 exact; structural walkBehavior 99.7, generateByGateKeeper 99.95 frame-exact, initSetEnemies 99.3. Fuzzy **97.93→97.97**. All stays **62.99%**. Park flagJump slots, behaveToWater/moveObject FPR, TIgaigaManager initSetEnemies.
- Landed `grpwr` MapObjLib ([MapObjLib](9c1b7561-996d-4745-9858-a86c7f68ad0a)): +2 exact (`getDistance`, `throwObjToFront`); touchWater/FromPoint 99.9%. Unit **68.41→71.44**. All **62.99→63.00**, Game **55.27→55.29**, exact **11,424→11,426**.
- Landed `grpwq` bosspakkun ([bosspakkun](697b151e-a1a1-4478-ad2e-2353401ba80e)): +2 exact (`TNerveBPTouchDown`, `gotHipDropDamage`); setGroundCollision 99.99%. Unit **51.79→56.43**. All **63.00→63.05**, Game **55.29→55.35**, exact **11,426→11,428**. Park Hover/TumbleOut/Fly/Wait frames.
- Landed `grpws` conductor ([conductor](f664f9e3-e2ee-451a-8a78-72fdd5b1200c)): +1 exact (`makeEnemyAppear`); makeOneEnemyAppear 99.9%. Unit **57.49→63.49**. All **63.05→63.07**, Game **55.35→55.37**, exact **11,428→11,429**. Park JGadget iterator-pool, isBossDefeated switch pivot.
- Landed `grpwt` EmitterViewObj ([EmitterViewObj](da3893bb-09e9-468b-b138-fac6b377a3fb)): +7 exact; unit **48.18→100%** code/data/fuzzy 30/30. All **63.07→63.16**, Game **55.37→55.49**, exact **11,429→11,436**. Link test FAILED (DOL 50f1c298…); objects.json reverted — **link blocker**, needs structural pass.
- Landed `grpwu` enemyMario ([enemyMario](8d8dbe23-2e0b-4434-9995-b94a68a8cfb3)): +1 exact (`decideDoingAfterCarry`); emRunAway/checkController frames exact. Unit **23.92→25.43**. All **63.16→63.17**, Game **55.49→55.50**, exact **11,436→11,437**. Park invite/waiting/checkReturn/drawHPMeter/initModel, consider/ReplayJump.
- Landed `grpwv` cannon ([cannon](a833ad1e-2d7a-4489-bf95-ada642e6abd4)): +2 exact (`TNerveCannonShoot`, `TCannonDom::perform`); Damage 100% fuzzy frame-exact. Unit **40.02→45.37**. All **63.17→63.20**, Game **55.50→55.54**, exact **11,437→11,439**. Park Damage 4-high slots, moveObject/init/setKillerGoalPoint align, Search/Open, shoot/matrix rebuilds.
- Landed `grpww` enemy ([enemy](4b265578-892c-4ba3-aa1b-f0a2e5505d58)): +3 exact (`calcRootMatrix`, `resetToPosition`, `turnToCurPathNode`). Unit **30.66→38.48**. All **63.20→63.23**, Game **55.54→55.57**, exact **11,439→11,442**. Park goTo* family, doShortCut, calcTurnSpeedToReach, Spine update, walk/zigzag.
- Landed `grpwx` MapObjCorona ([MapObjCorona](3042da12-7e39-4ef1-9205-9c0f5641d65f)): +0 exact; getNextGrip/Juncture 99.5 frame-exact. Fuzzy **93.71→93.74**. All stays **63.23%**. Park getNearGrip FPR, perform r5/r6, quake TVec3, getDir inline floor.
- Landed `grpwy` MarioDraw ([MarioDraw](66313d46-0c78-4075-84bb-cd8ce1642241)): +0 exact; initModel fuzzy 98.96→99.43 (body ResTIMG-first, mModelFaceAngle, rail→torocco MTXCopy). Unit fuzzy **97.50→97.58**. All stays **63.23%**. Park drawLogic/addDamageFog GXColor slots, MarioWaistCtrl Mtx scope, considerWaist frame, calcAnim setMotionBlendRatio header, initModel remainder.
- Landed `grpwz` bosswanwan ([bosswanwan](54df676a-9843-43d3-8d70-6b6363042498)): +0 exact; init/Jump frames (z=y=x=0 chain, drop extra setCalc stb). Fuzzy **96.71→96.72**. All stays **63.23%**. Park Wakeup/Roll frames, shakeCamera FPR, slideToCurPathNode fmr, Die setMtx bl, Jump this-vs-pool, leash JGadget.
- Landed `grpxb` MapObjHide ([MapObjHide](bfee851b-a4d8-48a3-ab5d-18620aa0188a)): +3 exact (load/loadAfter/touchWater via model/matrix/MarDirector binders). Unit **71.65→86.27%**. All **63.23→63.27**, Game **55.57→55.63**, exact **11,442→11,445**. Park WoodBox +0x98, twin loadAfter/touchFruit 4-byte slots, afterFinishedAnim.
- Landed `grpxa` fishoid ([fishoid](139e10c8-ad9b-4f13-b9c3-7e7c8fd8cd3d)): +0 exact; restore loadItem for TPathNode/eventId slots. Fuzzy load 99.91→99.92. All stays **63.27%**. Park load +8 hole, calcRootMatrix +0x30.
- Landed `grpxc` MapObjRicco ([MapObjRicco](2ece43d1-5501-4251-84de-b63aa8fdf430)): +4 exact (fireObj two-arg sound+TFlagT(), CraneRotY/Watermill control binders, Watermill ctor handle clear). Unit **41.63→80.73%**. All **63.27→63.36**, Game **55.63→55.74**, exact **11,445→11,449**. Park receiveMessage +8, CraneUpDown fadds, loadAfter.
- Yielded `grpxd` MapObjCloud ([MapObjCloud](541cd44c-45a9-489d-91cf-c119c5c8f51a)): +0; TRideCloud::control 4-marker FPR colouring (two half-solved colourings cannot combine). Parked.
- Landed `grpxe` NpcCollision ([NpcCollision](1f62496c-56ad-4929-a904-d0c2568297f3)): +0; comment-only record of rejected consumed-inside helper on `TBaseNPC::bind` (a=b-c class). All stays **63.36%**. Parked pending header/research.
- Yielded `grpxf` NpcEffect ([NpcEffect](862d2604-2261-456b-af9e-30334c7c7277)): +0; emitParticle_ slot-layout / doEmit r29vsr30. Parked.
- Yielded `grpxg` BeeHive ([BeeHive](9449be99-78dd-4066-8dcc-4630c1866e77)): +0; receiveMessage +4-low, reset +16 getQuat, bind operator- pool, setRotate/Wait shared-header. Parked.
- Yielded `grpxh` PollutionLayer ([PollutionLayer](fc5f5fd9-6cbd-437b-bf11-6a0960ce0a5a)): +0; cleaned/initTexImage/draw dead frames, stampModel load-order. Parked.
- Landed `grpxi` sunmodel ([sunmodel](8b671e83-56a1-4751-b99a-0ec804ec0270)): +0; comment-only — perform instruction-exact, mtx 8-low pad in calcAnim_ (research 208). Parked.
- Landed `grpxj` CameraDemo ([CameraDemo](daadc148-c82c-4a0d-8485-8b146e409166)): +0; comment-only — updateDemoCamera_ pool-order + y/z hoist. Parked.
- Landed `grpxl` NpcAnm ([NpcAnm](a298cedc-5c5b-4be2-9bbf-f4e9cf08cc7f)): +1 exact (`npcMareStandIn` via getCurrentAnmKind scalar fork). Unit **55.73→57.86%**. All **63.36→63.37**, Game **55.74→55.75**, exact **11,449→11,450**. Park madding/walkAnmRate/requestNpcAnm/wetting leftovers.
- Landed `grpxk` Bird ([Bird](95c6731b-f95e-4d46-a667-412ba7a707df)): +0 exact; GraphWander anim/timer + ChangeToCoin type CSE. Fuzzy **97.17→97.24**. All stays **63.37%**. Park receiveMessage/load 4-byte slots, GraphWander +0x10, ChangeToCoin ranking, WaitOnGround.
- Landed `grpxm` NpcChange ([NpcChange](c313f83e-f9f0-433d-9199-f7effac41089)): +1 exact (`behaveToBeTrampled_` via getSpine site count); changeNerveProc_ talk-cube gate + sink Y. Unit **28.49→42.97%**. All **63.37→63.40**, Game **55.75→55.80**, exact **11,450→11,451**. Park sandBomb +4, FromTalk +8, Proc leftovers, HitObject/SinkBottom frames.
- Landed `grpxn` Application ([Application](7baa598b-b0e9-49fa-a635-2574ecf18873)): +1 exact (`crTimeAry`); Mtx44 ortho, post-destroy free, crTimeAry depth. Exact **11,451→11,452**. Park proc +104, setupThreadFuncLogo 4-low, checkAdditionalMovie +56, drawDVDErr/gameLoop/nlogo leftovers.
- Landed `grpxo` cameragc ([cameragc](b1888273-f53d-4efe-be9f-46145bb86685)): +1 exact (`JetCoasterDemoCallBack`); ctor/ctrlGameCamera_ frames. Unit **24.67→25.55%**. All **63.40→63.41+**, exact **11,452→11,453**. Park calcSlopeAngleX_ isThing, loadAfter frame, ctor/ctrl residues.
- Landed `grpxp` ModelWaterManager ([ModelWaterManager](06dce013-e34a-4671-8bbd-9e1627e50b35)): +1 exact (`makeEmit`); hit defaults 50/80. Unit **20.52→24.02%**. All **→63.43%**, Game **55.83%**, exact **11,453→11,454**. Park drawWaterVolume/Refrac 4-byte, EmitInfo TParamVec, loadAfter/perform.
- Landed `grpxq` MarDirectorSetup2 ([MarDirectorSetup2](d6006c7e-6b17-4d84-85ce-8824e4ee9881)): +1 exact (`__dt` via 3-of-5 getVolume binders). Unit **28.68→46.03%**. setup2 frame exact, TColor 4-byte parked. All **63.43→~63.44+**.
- Landed `grpxr` ScreenUtil ([ScreenUtil](c08f010b-9e7a-4cb4-bba2-ec204ce9745f)): +0; comment-only — perform r30/r31 ranking parked.
- Landed `grpxt` MenuDir ([MenuDir](4fe85367-3495-4fd7-b06a-fb96e70d5b1c)): +0 exact after rejecting unusedLabel[0x1c] fakematch on setFixedStageValue; kept direct/rsetup logic (inverted flag, wave id, shine 120, goto fallthrough). Fuzzy **98.62→98.90**. All stays **63.44%**. Park setFixedStageValue +0x20, setup +0x18, direct TFlagT, rsetup frame.
- Landed `grpxs` PauseMenu2 ([PauseMenu2](f9e551f0-1fa8-4674-865c-1368c9205447)): +0 exact; load tags/offsets, appear >=46, resume DISAPPEARING, setDrawStart binders. Fuzzy **99.41→99.43**. Park JUTColor stride, load/appear +0x20, disappearWindow +8.
- Landed `grpxu` JALModSe ([JALModSe](e8fd2116-9e35-433d-9cda-c34606548f19)): +0; TODO documents TU-local JADPrmSF+JALPrmSet defaulted-arg path that lands 100% but needs real ≤4-byte type names (not TUtil/TagN fakematches). Parked pending type ID.
- Yielded `grpxv` PerformList ([PerformList](c3987b1a-be53-4568-8580-610906c93743)): +0; JGadget between-group gap — shared begin/end only. Parked.
- Landed `grpxw` MapWireManager ([MapWireManager](7903d2bf-691e-4377-a4b1-602052aa24b1)): +1 exact (`doActorToWire` via checkTakingActor accessor pool); getPosInWire 99.3→99.6. Unit **78.15→89.80%**. All **63.44→63.45**, Game **55.84→55.86**, exact **11,455→11,456**. Park getPosInWire a=b-c.
- Landed `grpxx` liveinterp ([liveinterp](1ff6dc97-96c1-4e73-9999-1b9979c04c2b)): +0 exact; linGetSRT 99.8→99.9 via const f32& binders. Park pop() return slots.
- Landed `grpxy` MapStaticObject ([MapStaticObject](5db4e250-98b4-40c7-88a7-a223e3e179ec)): +1 exact (`initUnique`). Unit **44.98→57.52%**. All **63.45→63.47**. Park init setMtx, perform register carrier, SoundGroup MsPerpendic.
- Landed `grpxz` LightUtil ([LightUtil](5c489802-8e72-40fb-b3e7-9a78ac8f104d)): +2 exact (Player/Object makeDrawBuffer via bare mLight fork). Unit **68.54→78.10%**. All **→63.50%**, Game **55.91%**, exact **11,456→11,459**. Park perform +0x10, setLight CSE, loadAfter AmbColor header.
- Landed `grpyb` AnimalNerve ([AnimalNerve](37eed264-5dcc-4b68-bf38-b8b946fee931)): +1 exact → **100%**; source-linked (`Animal/AnimalNerve.cpp`). All **63.50→63.53**, Game **55.91→55.96**, linked **495→496**, exact **11,459→11,460**.
- Landed `grpya` ConsoleStr ([ConsoleStr](79d1457c-7c04-4fed-bb61-9a00eec52691)): +0 exact; processReady/load raised. Fuzzy **95.63→95.71**. Park load +0x20, processReady residues, sinit, perform/processGo.
- Landed `grpyc` JAIBasic ([JAIBasic](43c827f8-64c0-42a1-b2d0-f0ed765e0847)): +1 exact (`initDriver` via caller `rootHeap`); **source-linked**. All **63.53→~63.54+**, linked **496→497**.
- Yielded `grpyd` SampleCtrlNode ([SampleCtrlNode](4737966a-c2e3-46fd-b5e6-32511be72556)): +0; pinned-pool getAttnFn 4-byte (known-open). Parked.
- Landed `grpye` MarDirectorDirect ([MarDirectorDirect](3bad58e9-c274-43c4-a137-1f64250f072e)): +0 exact; scenario-then-stage RTL locals on demo/BGM calls. Fuzzy **95.89→95.92**. Park finalize/setMario/nextState frames, moveStage this-bind, decideNextScenario/Stage.
- Landed `grpyf` SelectMenu ([SelectMenu](20941e7c-78f4-4e5e-b4ec-45e5f52667a5)): +0 exact; Grad midB/G/R ranking, initData pArch + i+1. Fuzzy **93.59→93.66**. Park Grad AmbColor/zero-reuse, initData frame, Menu perform.
- Running: none. Session highlight: AnimalNerve + JAIBasic **linked** (495→497).
- Parked: gesso, bosseel, hamukuri generateBody, fireWanwan, chuuhana TVec3::sub, bossManta slots, MapObjMamma FPR, popo slots, MapObjBall control, Option mr-fold, elecNokonoko Turn/loadInit, telesa bind a=b-c, killer, MapObjPinna, MarioJump leftovers, MarioSound soundTorocco, MarioReceiveMsg frame, CardSave leftovers, CardLoad leftovers, BathWater leftovers, MarNameRefGen leftovers, Guide leftovers, ShadowUtil leftovers, WaterGun leftovers, MarioSpecial leftovers, MSoundSE leftovers, bgtentacle leftovers, MarioRun leftovers, MapObjTree initMapObj coalescing, MovieRumble UNUSED leftovers, PollutionCount UNUSED leftovers, riccohook init JGadget gap, MirrorActor init JGadget gap, wireBinder bind a=b-c, walkerEnemy 40-byte frames, MActor leftovers, liveactor leftovers, MarioCheckCol hitNormal coalescing, MarDirectorLoadResource dead-low-region, MarioMove leftovers, Yoshi leftovers, MapObjBianco leftovers, graph leftovers, MSound leftovers, MapObjMare leftovers, Item leftovers, namekuri leftovers, NpcEvent slice/frame, tinkoopa leftovers, igaiga leftovers, MapObjLib leftovers, bosspakkun leftovers, conductor leftovers, EmitterViewObj link blocker, enemyMario leftovers, cannon leftovers, enemy leftovers, MapObjCorona leftovers, MarioDraw leftovers, bosswanwan leftovers, MapObjHide leftovers, fishoid leftovers, MapObjRicco leftovers, MapObjCloud leftovers, NpcCollision leftovers, NpcEffect leftovers, BeeHive leftovers, PollutionLayer leftovers, sunmodel leftovers, CameraDemo leftovers, NpcAnm leftovers, Bird leftovers, NpcChange leftovers, Application leftovers, cameragc leftovers, ModelWaterManager leftovers, MarDirectorSetup2 leftovers, ScreenUtil leftovers, MenuDir leftovers, PauseMenu2 leftovers, JALModSe leftovers, PerformList leftovers, MapWireManager leftovers, liveinterp leftovers, MapStaticObject leftovers, LightUtil leftovers, ConsoleStr leftovers, SampleCtrlNode leftovers, MarDirectorDirect leftovers, SelectMenu leftovers.

### Session 019Bu5iwgrAjisV84bihvCHv (2026-09-19, batches 270-295, in progress)

- Frame ladders 270-290 closed ~110 functions on rung pricing (RULES: binder pricing, scalar-in-binder, per-site mixing, raw-global fork, unrolled loop +0x10, `ref()` sites); tell sweeps 272/283 and branch sweeps 289/291 fixed ~40 real gameplay bugs (right-to-left args, `TParamRT` index, branch-target reading, `!x` range fold). `MarioGamePad` linked (492/732; its `updateMeaning` still carries `u32 stackAlloc[83]`, an open debt). `objects.json` had two duplicate entries: the honest linked count was 491 before that link.
- Closed questions: the `a = b - c` reshape family (header round 47: retail's `operator-` is the stock header, residue is caller-side); `__sinit` `JALList` bases are node censuses; a losing weak-duplicate vtable caps `.data` below 100 without affecting the DOL.
- **Sonnet 5 trial (user request, pricing 2.5x cheaper than Opus 5)**: ladders at rough per-dollar parity when the prompt bans unused locals and parameter aliases explicitly; without the ban both Sonnet ladder agents invented padding (`self = p` aliases, dead `int`/`TVec3`/`Mtx` locals; 28 closures refused). Tell sweep on the same 93-function list: Sonnet 1 fix at 224k, Opus 6 at 162k. Data pass: one section plus a false vtable lead. Policy: Sonnet only for mechanical recipes with the ban and a padding diff scan at landing; Opus for sweeps, closures, research, headers.
- Batches 296-318 (same evening): the ladder/sweep tier kept paying on Opus (5-11 exact per ~180k) once the rung table grew: the reference-temporary family (`TParamT::get()`, `getPosition()`/`getRotation()`/`getVelocity()`, `SMS_GetMarioPos()` with 214 sites, `getVertexData()`), accessor levels inside inlined helpers (research 302), pointer-member chains through accessors at every site (non-monotone in site count), the 4-byte consumed-level rung and the frame law (research 312), the dead-compare construct (297, removed two `@hack`s), and header rounds 49/50 (one J3D wrapper, four accessor sets promoted). `ninja changes_all` is not a frame probe (ladder 317). Matched code 57.21 -> 58.38 over these batches. The Sonnet trial was concluded negative (memory: sonnet-experiment).
- Batches 319-325: unit-focused ladders (one Opus agent per dense translation unit, unit-specific rungs priced once and reused) outperform the general ladder now: bossgesso 25 -> 36% code and data 73 -> 100 (the tentacle predicate folds only through a named local copy), hamukuri 64 -> 68, fireWanwan 32 -> 38, bosseel 48.7 -> 50.6, each 3-8 exact per ~200k with real bugs found on the way. Rank units by near-miss count (99-99.99 plus 95-99) and work them in order; bosswanwan and bosspakkun are in flight, then WaterGun, bossManta, hanasambo, MapObjBall, bosstelesa, chuuhana, hinokuri2.
- Batches 326-330: WaterGun 18 -> 28%, bossManta 24 -> 41%, bosspakkun 44 -> 47%; header round 51 moved the shine tables into `include/GC2D/ShineTable.hpp` (they were a GC2D-scoped header misaligning 14 TUs' float pools) and found the honest `dont_inline` replacement (`TVec3::set` -> three stores, +2 statements). 32 files still carry banned `dont_inline` pragmas from earlier sessions: a sweep applying that rule is the next structural candidate. Game code crossed 50% (50.16).
- Batches 331-334: bosstelesa (`reset` 87 -> 100 on named `f32` locals for int-to-float arguments), chuuhana 38 -> 49%, hanasambo 49.5 -> 68%, MapObjBall 45 -> 64%. Total matched code 59.27, game 50.56. Unit queue after hinokuri2 and MapObjMare: cannon, koopajr, MapObjBianco, MarioMove, enemy, MarioRun, popo, tobiPuku; then the tree-wide `d *= d` FPR sweep and the `dont_inline` pragma sweep (32 files).
- Batches 333-336 (into 2026-09-20): hinokuri2 39 -> 44% (three real bugs, including wrong `PARAM_INIT` defaults invisible to the fuzzy score), MapObjMare 41 -> 50% (a by-value scalar fork colours the FPRs; a reversed `A - B` is two inlined calls). Total 59.34, game 50.65. cannon and koopajr in flight; then MapObjBianco, MarioMove, enemy, MarioRun, popo, tobiPuku, and the two sweeps.
- Batches 335-338: cannon 24 -> 40% (`fp_contract` on `member + literal * member`, unfused by a TU-local inline), koopajr 34 -> 39% (dropping a header wrapper at one site is -8; `TKoopaJr::init` closed). Total 59.47, game 50.81. MapObjBianco and MarioMove in flight; then enemy, MarioRun, popo, tobiPuku, and the `d *= d` / `dont_inline` sweeps.
- Batches 337-340: MapObjBianco 52 -> 60% (the setter-level +4; column-wise identity chains), MarioMove 44 -> 46% (five real bugs: swapped ternary arms read off the result register, a paired `TParamRT` index and member error). Total 59.52, game 50.88. Enemy/enemy (the shared graph helper, rungs inside it for all six callers) and MarioRun in flight; then popo, tobiPuku, and the `d *= d` / `dont_inline` sweeps.
- Batch 339 (Enemy/enemy): the shared graph helper `setGoalPathFromGraph` is exact and all six callers land their frames; the residue is the helper/caller region split (rungs inside a pasted helper add above its block, caller-side rungs below; accessor spellings must match on both sides of a paste). `TSpineEnemy::goToShortestNextGraphNode`'s `dont_inline` pragma is load-bearing (retail's body is 15+ statements to our 12): first-named target for the pragma sweep. Total 59.53, game 50.89.
- Batches 340-343: MarioRun 35 -> 38% (`getInput()` is a +4 level), popo 37 -> 46% (a shared ctor's `set` vs copy member write is chosen by the overload; a live `MtxPtr` before a named `TVec3` is a +8 top carrier). Total 59.59, game 50.97. tobiPuku and MapObjMamma in flight; then bgtentacle, tinkoopa, igaiga, elecNokonoko, enemyMario, and the two sweeps.
- Batch 342 (tobiPuku): 59 -> 75%, seven exact; a named `MtxPtr` blocks retail's `lfs` displacement fold (-0x30 dropped), and dropping a named local can paste a body into its caller (repay the statement). `TNerveTobiPukuLand::execute` (54.8) still holds the unit's data at 96.71: a 25-minute reconstruction is queued. Total 59.68, game 51.09.
- Batch 344 (bgtentacle): 20 -> 40%, six exact, three real bugs; an indexed-member accessor read raw is about -4 per site (a large negative rung for long frames) and its data gap is the zero-filled weak-vtable ceiling (lead closed). Total 59.81, game 51.24. tinkoopa in flight after MapObjMamma; then igaiga, elecNokonoko, enemyMario, and the two sweeps.
- Batch 343 (MapObjMamma): data 27 -> 100 by lifting the function that owned the blocking jump table (`TSandBombBase::control` 82 -> 99.5 on a named `f32 speed` before `setFrame(speed + getFrame())`); project matched data 95.58 -> 96.36. Total 59.81, game 51.25. igaiga and tinkoopa in flight; then elecNokonoko, enemyMario, and the two sweeps.
- Batch 345 (tinkoopa): 34 -> 54%, two exact (a 3 KB effects body on a `getModel()` binder priced at 12 bytes per site, subset-tunable). Total 59.91, game 51.37. igaiga and elecNokonoko in flight; then enemyMario, and the two sweeps.
- Batch 346 (igaiga): `TGorogoro::behaveToWater` 36 -> 99.6 by splitting a 17-statement body into a nested in-class helper (the map's `(func,global)` binding column says split, not `inline`); depth-1 budget re-measured at 14 statements. Total 59.92, game 51.38. elecNokonoko and enemyMario in flight; then the `d *= d` FPR sweep and the `dont_inline` pragma sweep (32 files; `TSpineEnemy::goToShortestNextGraphNode` first).
- Batch 347 (elecNokonoko): 53 -> 59%, four exact; the bool-materialisation tell marks an inline predicate with an explicit if/return, and `isWaterSurface()`'s compare grouping is inline depth (raw `mGroundPlane` vs `getGroundPlane()`), which also settles bossgesso/popo's `isPool` residue without touching `MapData.hpp`. Total 59.94, game 51.42. enemyMario and Item in flight; then MapObjPinna, smallEnemy, pakkun, and the two sweeps.
- Batch 348 (enemyMario): no closure, three real fixes (`checkReturn` 91 -> 99.8 on the two-level distance helper); the unit's data (63%) is one jump table owned by `TEnemyMario::consider()` (97.3), whose residues are a `bl sub`/`bl dot` pair and an inline chain one level too shallow (`canJumpToNode`'s UNUSED body 0x70 vs map 0x6c). Total 59.94, game 51.42. Item and MapObjPinna in flight; then smallEnemy, pakkun, and the two sweeps.
- Batch 350 (MapObjPinna): 59 -> 69%, four exact; a divide by a power of two folds to `fmuls value, recip` with the value first while both multiply spellings put the constant first (four sites in one TU). Total 59.98, game 51.47. Item and smallEnemy in flight; then pakkun and the two sweeps.
- Batch 349 (Item): 64 -> 77%, six exact (`TShine::appearWithTime` 86 -> 100: a reference-returning accessor on a call argument re-ranked the GPRs). **Total matched code crossed 60% (60.06), game 51.56.** smallEnemy and pakkun in flight; after them the unit queue thins (NpcEvent, MapObjTown, MarioDraw, graph, MapObjCorona, MapEventMare at 11 near-misses each), then the `d *= d` FPR sweep and the `dont_inline` pragma sweep (32 files).
- Batch 351 (smallEnemy): 54 -> 68%, seven exact, a second honest `dont_inline` replacement (N products as N `*=` statements plus a named result reach the 15-statement floor); 31 files still carry pragmas. Total 60.13, game 51.65. pakkun and NpcEvent in flight.
- Batch 352 (pakkun): 56 -> 64%, four exact; a `TVec3` built only to bind a reference argument is an unnamed copy-ctor temporary; measured shared-header need in `Enemy/PathNode.hpp` (the `TPathNode(THitActor*)` ctor's zero stores should be `unk4.set(0,0,0)`, which becomes the map's `bl set<f>` at inline depth 4 in `TStayPakkun::load`). Total 60.17, game 51.70. NpcEvent and MapObjTown in flight; then MarioDraw, graph, MapObjCorona, MapEventMare, the header round for PathNode.hpp, and the two sweeps.
- Batch 353 (NpcEvent): 50 -> 58%, four exact; a bare-`return` fork over a member is +4 and +8 with a named local inside (the whole difference); `push(TSpcSlice(v))` vs `push(v)` moves the slice +4; the `gpMarDirector` binder is super-additive (+0x20 at three sites). Total 60.19, game 51.73. MapObjTown and MarioDraw in flight.
- Batch 354 (MapObjTown): 64 -> 92%, ten exact, two real bugs (a shared empty switch label group read off one dead `b <default>`; `getInitialPosition().y` binding `&member.y` into a scratch GPR). Total 60.27, game 51.83. MarioDraw and Enemy/graph (with a tree-wide measurement of `TGraphTracer::popCurr`'s `int&` binding) in flight; then MapObjCorona, MapEventMare, the PathNode.hpp header round, and the two sweeps.
- Batch 355 (MarioDraw): 25 -> 33%, three exact (`boxDrawPrepare`'s `wpsave[5]` was one float short); two header fixes parked and now in header round 52: `marioAnimeFiles` has 86 wrong flag words (corrected table in the scratchpad; MarioDraw data 48 -> ~100), and `Mario.hpp` has the two chest joint ids swapped. Total 60.32, game 51.90. Enemy/graph in flight.
- Header round 52: `Mario.hpp`'s chest joint ids swapped to retail's (0x3C5 `jnt_chest`, 0x3C6 `chn_chest`, use sites re-pointed, no drops); the `marioAnimeFiles` flag lead and the PathNode.hpp `set<f>` lead were both refuted with evidence (the MarioDraw data residue is `.rodata` string-pool order on `MarioDraw.cpp`; the pakkun `set<f>` site is at inline depth 3 and every level added deepens all `TPathNode` sites). Total 60.32, game 51.90. Enemy/graph and MapObjCorona in flight; then MapEventMare, the `d *= d` FPR sweep and the `dont_inline` pragma sweep.
- Batch 356 (Enemy/graph): 39 -> 45%, two exact; `TGraphTracer::popCurr`'s wrong register pair is fixed by a reference-returning accessor (`int& getPrevIndexRef()`), which ranks the receiver below the derived address; `f(vec)` through `TVec3::operator Vec*` hoists the buffer address into a callee-saved GPR. **Fakematch debt census: 31 files still carry `#pragma dont_inline`, 22 carry `trash[]` pads** (graph.cpp has two). Both sweeps are queued after MapObjCorona and MapEventMare. Total 60.34, game 51.92.
- Batch 359 (MapEventMare): 39 -> 54%, five exact (reassigning an existing scalar local instead of naming a second recolours it into the argument register). Total 60.39, game 51.98. The unit queue below 11 near-misses is thin; the `dont_inline` pragma sweep (batch 360, 31 files, two honest recipes) has started, the `trash[]` pad sweep (22 files) follows, then the `d *= d` FPR sweep.
- Batch 358 (MapObjCorona): 35 -> 45%, three exact (`a *= k` puts the value first in the `fmuls`; a `const T&` argument temporary named at function scope is +8 block / +4 pool). Total 60.45, game 52.05. Both fakematch-debt sweeps are now running: `dont_inline` (batch 360, grpfh) and `trash[]` pads (batch 361, grpfg); the `d *= d` FPR sweep follows.
- Batch 360 (`dont_inline` sweep): 4 of 37 blocks removed honestly (a third replacement recipe: name one scalar product out, +2), the other 33 now carry their statement count and protected set in-source; MWCC does expand direct self-recursion one level; `-inline noauto` per object is not a substitute. Total 60.45, game 52.05. The `trash[]` sweep (361) and the FPR-colouring sweep with the bosseel `forceShedTears` lead (362) are in flight.
- Batch 361 (`trash[]` sweep): 8 of 20 pads deleted (four with honest carriers at unchanged bytes, four traded for TODOs in unlinked units at an honest -0.03 of matched code, as the rules require); four pads in source-linked units (JUTConsole, JUTRomFont, JKRDvdRipper, J3DMaterialAnm) hold the DOL and are priced as debt with their exact slot maps; batch 363 researches them. Total 60.42, game 52.02.
- Batch 362 (FPR sweep): callee-saved FPRs are assigned in descending declaration order, so retail's f31..f24 sequence is the source's declaration order (zero-byte fixes; `TWireBell::draw` exact); the volatile f1-f5 block is a separate class. `TBossEel::forceShedTears`'s pragma is gone honestly (fourth recipe: name a bool out of its store, a pointer out of an accessor chain). Batch 364 sweeps the declaration-order rule tree-wide. Total 60.43, game 52.03.
- Batch 364 (FPR order sweep): the strict callee-saved list was already closed; the residual FPR class is an inversion (our builds rank hoisted temporaries above named locals, retail below; an inlined callee's locals rank ascending). Total 60.43, game 52.03. Unit queue resumes with namekuri (365), then DrawUtil, gatekeeper, ShadowUtil, MapObjLib, MapObjMonte, MActorData, MapObjSirena, gesso, MarioSpecial (9-11 near-misses each).
- Batch 363 (linked-unit pads): five more `trash[]` pads removed with honest carriers at unchanged DOL bytes (JUTConsole x2 via a named `u32 output` in the shared va helper, two JKRDvdRipper forwarders via a named forwarded result, J3DMaterialAnm via a new +8 rung: a fork that binds the intermediate block and returns the indexed accessor). Five live pads remain tree-wide (JUTRomFont x2 and JKRDvdRipper's main overload x2 are priced as unreachable by pool rungs, plus J3DMaterial.cpp:791/JASChGlobal.cpp:26 unmeasured). Total 60.43, game 52.03. namekuri and DrawUtil in flight.
- Batch 365 (namekuri): 55 -> 59%, two exact (a matrix's translation column written as an ascending `m[2][3] = m[1][3] = m[0][3] = 0` chain before the rows is a store-order and FPR tell; a two-local binder over `getObj(i)->getMActor()` is +0x10). `TNameKuri::calcRootMatrix` (83.6, needs +0x38 and two callee-saved FPRs kept live as named `f32`s) is a batch on its own. Total 60.45, game 52.05. DrawUtil and gatekeeper in flight; then ShadowUtil, MapObjLib, MapObjMonte, MActorData, MapObjSirena, gesso, MarioSpecial.
- Batch 366 (DrawUtil): 32 -> 60%, five exact, data 67 -> 100 (MWCC does not constant-propagate into a compound assignment, so `f32 x = K; x -= b;` keeps the literals a folded `K - b` loses: the unit's whole data deficit). Total 60.52, game 52.15. gatekeeper and ShadowUtil in flight; then MapObjLib, MapObjMonte, MActorData, MapObjSirena, gesso, MarioSpecial.
- Batch 368 (ShadowUtil): no closure, but the unit's whole deficit is one cause outside it: MWCC's `$NNNN` id counter is seeded by `include/SMS.pch`, ours is 1074 ids short of retail's before any TU code, and ShadowUtil's six byte-exact local classes are mangled with the wrong ids (4116 bytes of `.text`, 96 of `.data`). Header round 53 (batch 369) is measuring which header set lands +1074. Total 60.52, game 52.15.
- Batch 367 (gatekeeper): 46 -> 84%, nine exact, three real bugs; which sites carry a raw-global fork matters (the last two of four clean, the first two rotate argument registers); dropping an accessor inside a map-UNUSED helper lowers the pasting caller for free. Total 60.72, game 52.40. Header round 53 (PCH ids) and MapObjMonte in flight; then MActorData, MapObjSirena, gesso, MarioSpecial.
- Batch 370 (MapObjMonte): 27 -> 40%, four exact (a named `f32` copy of a static member shifts every volatile down one: read the static at its use; `fp_contract` folds `m -= a * b` into `fnmsubs`; string-literal `@NNNN` order fixes the if/else fallthrough arm; calling a map-UNUSED helper reverses callee-saved FPR order). Total 60.79, game 52.48. Header round 53 (PCH ids) and MapObjSirena in flight; then MActorData, gesso, MarioSpecial.
- Header round 53: the `@NNNN` counter counts anonymous entities only; PCH 191 vs retail 1457, the deficit is the thin `dolphin/`/MSL headers, unreachable by an honest include set (J3DJoint class order fixed, zero movement). Batch 371 MapObjSirena 57.75 -> 70.84 (4 exact, `fmadds` first-operand colouring, unrolled-for blocks base CSE, `SMSGetMarDirector()` fork). Batch 372 MActorData 21 -> 48 (six template instances by one named-step reader, TODO-flagged; lead: fruitsboat.cpp:78 `bcks->mAnmNum` raw). Total 60.90, game 52.63. hamukuri and fireWanwan in flight; queue bosstelesa 11, hinokuri2 11, bossgesso 10, bosseel 9, bosspakkun 9 untried near-misses.
- Batches 373-375 (hamukuri 4 exact, fireWanwan 3, bosstelesa 3): u8 fork +4, `BOOL`-through-`bool` tell, manager-cast fork reranks GPRs free, loop bound before the `bool`, clamp in place not through a `u8` local, `m *= k` member first, pasted-helper rungs tunable separately. Total 61.01, game 52.77. hinokuri2 and bossgesso in flight; queue WaterGun 10, bosseel 9, bosspakkun 9, MarioSpecial 9, igaiga 8, bossManta 8 untried near-misses.
- Batches 376-377 (hinokuri2 7 exact 44 -> 66%, bossgesso 3 exact): a TU-local binder returning the cast `getSaveParam()` pointer is the params rung and restores the `TParamRT` displacement fold (reverses the per-site-cast note); caller-level local = +8 named block vs binder = +8 pool; bare fork +4 only over an accessor call; `inSightAngle` UNUSED body 168 short marks a missing inline level in three bossgesso callers. Total 61.21, game 53.02. WaterGun and bosseel in flight; then bosspakkun 9, MarioSpecial 9, igaiga 8, bossManta 8.
- Open leads: 25 priced dead-frame bodies in `scratchpad/ladder_tried.txt` (all instructions exact, frame need measured), the shared `TMapObjBase` helper in MapObjLib, 54 zero-delta slot-ordering functions, the unfused `bcc next; b epilogue` early-return class, bossgesso's emergent per-expansion folding.


- **2026-09-18 late night** (session `110dc638`, batches 182-208, ~5.6M agent tokens): 489/732 linked (no new links), 54.43 -> 54.86% matched, ~60 functions to exact, ~25 real gameplay bugs fixed, matched data 92.8 -> 95.2% (eleven units to 100% data). Yield per type this session: data passes 240k for 3-5 units to 100% data (best value while units remained); header rounds 110-190k for 1-4 exact plus data; structural passes 320-410k for 5-9 exact and 3-5 bugs, no links; closure re-passes over the one- and two-function game tiers (54 units) and the eight near-link library units: **zero closures**, every residue a recorded known-open class; research 184/186/193/208 each produced a rule (JGadget stride not in `std-list.hpp`; the statement-count table with the named-local +1; MSound's fabricated gate; an inlined callee's class-object locals form their own block above the caller's). The user is at 40% of the weekly budget 12 hours into the week and chose two agents, yield-first; with the cheap tiers exhausted the remaining yield is research only. Open leads: JALModSe `append` is instruction-exact and its 0x68 hole is positioned in the `JALPrmSet`/`JADPrmS<f32>` prologue constructions (3 x 16 + 7 x 8) but unnamed: naming it links the unit; J3DModel `entryModelData` 99.92 (zero-frame r25/r24 vs r20/r21 rotation) links the unit; `TSunModel::perform` has a constant 4-byte pad at the top of `calcAnim_`'s block; `getMaxHitPoints()` wants a statement-free address binding of `getSaveParam()->mSLHitPointMax`; CardManager `setCheckSum` needs a `CalcCheckSum` body under 9 statements that stays byte-exact; the remaining sub-100 data units are jump-table owners (GCConsole2 `perform` is a 14.7 KB rebuild). Worktrees grpfg/grpfh/grpfi/grpfj/grpfk exist, all landed, some behind HEAD (rebase + `ninja` + `ninja baseline` before use). Census scripts take `SMS_ROOT` or the cwd.
- **2026-09-18 night, final** (session `7a7a015e`): 489/732 linked, 54.43% matched, tree quiet. Every batch through 181 is landed; no agent is running. One spare worktree `grpff` exists (branch `wt/grpff`, no commits, built a few commits behind HEAD: `git rebase local/decomp-progress`, `build/venv/bin/ninja`, `build/venv/bin/ninja baseline` before using it). The last four batches (reloc III, structural III/IV, 4-byte re-pass II) lifted ~25 functions and closed 8 but linked nothing: the structural and reloc passes are now the main vein for exact functions, and links come from research results. Land routine for the next session: `tools/worktree.sh land <name> <units>`; `objects.json` conflicts via `python3 tools/resolve-objects-conflict.py config/GMSE01/objects.json`; catalog conflicts keep both hunks.
